<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出配置 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Serenity BDD 内部库的日志级别配置 - 抑制INFO级别的警告 -->
    <logger name="io.cucumber" level="WARN"/>
    <logger name="net.serenitybdd" level="WARN"/>
    <logger name="io.github.bonigarcia" level="WARN"/>

    <!-- 项目代码日志级别：root 默认 INFO；VerboseLogging 会按 serenity.logging(VERBOSE→DEBUG / TRACE→TRACE) 在运行时提升至对应级别 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
