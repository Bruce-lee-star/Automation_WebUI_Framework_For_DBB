() => { var out = { urls:[], names:[] }; var roots = window.__regionSelected || []; for (var i=0;i<roots.length;i++){   var r = roots[i]; if (!r || !r.querySelectorAll) continue;   var fs = r.querySelectorAll('iframe, frame');   for (var j=0;j<fs.length;j++){     var el = fs[j];     var src = el.getAttribute && el.getAttribute('src');     if (src) out.urls.push(src);     if (el.name) out.names.push(el.name);     if (el.id) out.names.push(el.id);   } } return out;}