# 架构评审整改执行跟踪（二次评审）

> 来源：`docs/architecture-review/00~09`。本文件记录整改进度，按"是否合理→任务→修复"推进。
> 状态图例：✅ 已修复并验证 / 🔧 修复中 / ⏳ 待处理 / ⚠️ 需架构决策（暂缓）
> 详细验收标准见 `08_整改任务总表.md` 与 `09_补充实证发现与增量任务.md`。

---

## 一、P0 阻断级（必须最先做）

| 任务 ID | 模块 | 问题 | 是否合理 | 状态 | 说明 |
|---|---|---|:--:|:--:|---|
| **ENG-P0-1** | 工程化 | 无 CI 执行 build/test，四道门禁从不触发 | ✅ 属实 | ✅ 已修复 | 新增 `.github/workflows/build.yml`（push/PR→JDK21+缓存→`mvn -B test`→归档报告→required status check）。分两期：一期只跑 `test`，门禁基线就位后再开 `verify` |
| **ENG-P0-2** | 工程化 | 父 POM `<dependencies>` 全量继承，模块边界失效 | ✅ 属实 | ✅ 已修复 | 根 `<dependencies>` 全部移入 `<dependencyManagement>`，7 模块按实际使用显式声明依赖；core 仅 HikariCP+logback-classic（≤5）；Enforcer（requireUpperBoundDeps + banDuplicatePomDependencyVersions）通过；全护盾零回归。CORE-P0-1（Serenity 接口下沉）仍独立待办 |
| **CORE-P0-1** | core | core 编译期强耦合 Serenity（地基污染） | ✅ 属实 | ✅ 已修复 | core 抽 `ConfigResolver` 接口（依赖倒置点），`ConfigSource` 经 `ServiceLoader.load(ConfigResolver.class)` 发现；web 下沉 `SerenityConfigResolver`（`SystemEnvironmentVariables` 合并源）+ `META-INF/services` 注册；core 0 个 Serenity import、pom 无 serenity 依赖（jackson 显式声明）。全护盾 470 绿，core 纯净底座成立 |
| **CORE-P0-2** | core | `ThreadLocalTestContext` 非线程安全假设脆弱 | ✅ 属实 | ✅ 已修复 | core 抽 `ThreadContextRegistry`（`WeakHashMap<Thread,TestContext>` 容器）+ `InheritableThreadLocal` 开关；`ThreadLocalTestContext` 内部改 `ConcurrentHashMap`；全护盾 476 零回归，G1 架构门禁保持 |
| **CORE-P0-3** | core | `ShutdownCoordinator.runAll()` 一次性不可复位 | ✅ 属实 | ✅ 已修复 | `CopyOnWriteArrayList` + 执行后可重注册 + `reset()` |
| **WEB-P0-1** | web | 共享 Browser 锁用错（per-thread 而非进程级） | ✅ 属实（已读 :690/:742） | ✅ 已修复 | 两处改 `browserLock(SHARED_BROWSER_MODE)`；新增并发单测 |
| **WEB-P0-2** | web | 全静态、零 DI，不可测 | ✅ 属实 | ✅ 已修复 | 抽 `BrowserProvider`/`ContextProvider`/`PageProvider` 三接口 + 组合接口 `RuntimeProvider`；`DefaultRuntimeProvider`（薄适配，委托 `realGetXxx`，不搬运 77KB 逻辑）为默认实现；`PlaywrightManager` 4 个 getter 退化为委托 + `setProvider/getProvider/resetProvider` 测试 seam（volatile 原子换引用，`setProvider(null)` 抛 IAE）；`BasePage()` 构造不再强制 `FrameworkCore.initialize()`。新增 `WebRuntimeSeamTest`（3 例：mock 注入生效 / `resetProvider` 复位默认 / `setProvider(null)` 被拒）；全护盾 479 零回归。详见 `13_整改专项设计_WEB-P0-2_可测试性DI seam.md` §8 |
| **API-P0-1** | api | `ConfigProvider` 全局配置跨 Scenario 串扰 | ✅ 属实（已读 :61） | ✅ 已修复 | `config` 改 `ThreadLocal<Config>`，消除跨线程覆盖；保留 `getConfig()` 线程内读取 |
| **ROUTE-P0-1** | route | 断言失败仅落日志+标志，不 rethrow、未上报 Serenity | ✅ 属实（已读 :261-267 / StepFailureAggregator :93-137） | ✅ 已修复 | 根因修订：Phase 5 后 `getCurrentCapture()` 永不返回 null（无绑定时回退非空 `SHARED`），故旧「`==null` 早退」已失效；真实漏检=route 事件线程在上下文失效时经 `RouteUtil.captureContext` 回退 `getCurrent()` 把失败标志落在 `SHARED`，而测试线程（已绑定 per-context）经 `getCurrentCapture()` 只读 per-context 实例→漏检。修复：`RouteLifecycle` SPI 增 `resolveFailureCapture()`（优先 per-context，否则 `SHARED` 兜底），`StepFailureAggregator` 两处失败检查改用之；`ApiCaptureContext` 增 `getShared()`。新增 `AssertionFailureSurfaceTest`（2 例）复现+固化，全护盾 BUILD SUCCESS；2026-09-08 NPE 收尾：StepFailureAggregator 两处补 get() null 守卫（route 未注册时 get() 返回 null 抛 NPE 被委托监听器吞掉），未注册安全跳过，全护盾 470 绿（commit 954f02b） |
| **CG-P0-1** | codegen | 代码生成覆盖写，无冲突检测 | ✅ 属实（已读 :463） | ✅ 已修复 | 生成标记 + 冲突拒绝覆盖 + `--backup`/`.bak` + 临时文件原子移动 |

---

## 二、P1/P2 增量小项（已确认、低风险、本次顺带修复）

| 任务 ID | 模块 | 问题 | 状态 | 说明 |
|---|---|---|:--:|---|
| **CORE-P1-N4** | core | `ContextKey.equals/hashCode` 仅按 name，忽略 type → 同名不同类型互相覆盖 | ✅ 已修复 | 纳入 type；补单测 |
| **CORE-P1-N5** | core | `ShutdownCoordinator` 并发 register + 一次性闩 | ✅ 已修复 | 见 CORE-P0-3 |
| **CORE-P2-N6** | core | `AsyncPool.getCompletedTaskCount()` 重复计数 | ✅ 已修复 | 仅保留 `POOL.getCompletedTaskCount()` |
| **CORE-P1-N3** | core | `ConfigCipher` 主密钥校验缺失（可静默降级 AES-128） | ✅ 已修复 | 强校验 64 hex；解密失败 ERROR 含 key 名 |
| **CORE-P1-N7** | core | `AsyncPool` 直读 `ASYNC_*`，第三套配置 | ✅ 已修复 | `getEnv*` 改走 `ConfigSource.resolve`（env 名映射零变更，新增 `-D`/serenity.properties/ENC() 覆盖） |
| **ROUTE-P1-N1** | route | DB 方言与 DDL 不匹配（Oracle/SQLServer 静默落 MySQL） | ✅ 已修复 | `init()` 前置 fail-fast：未知方言（不在 SUPPORTED_DIALECTS）直接 `initFailed` 退回并清晰告警，杜绝错配建表；ROUTE-P1-N2 落地后 Oracle/SQLServer 已纳入 SUPPORTED_DIALECTS（各自 `db/migration/<dialect>/V1__*.sql` 已按库语义对齐），仅真正未知类型被拒 |
| **ROUTE-P1-N2** | route | Schema 无版本治理（DDL 内嵌 Java，无迁移/回滚/审计） | ✅ 已修复 | 引入 Flyway（`flyway-core` 9.22.3）；DDL 移出 Java 落地 `route/src/main/resources/db/migration/{mysql,postgresql,h2,oracle,sqlserver}/V1__create_route_monitor_record.sql`（按各库语义对齐类型：MySQL/PG/H2 保留 `IF NOT EXISTS`，Oracle/SQLServer 依赖基线跳过）；`ApiMonitoringRepository.migrateSchema` 按解析方言选迁移目录 + `baselineOnMigrate(true)`/`baselineVersion("1")`：新库执行 V1、旧库（已有表无历史）自动基线零丢数据；SUPPORTED_DIALECTS 扩展为 MySQL/PostgreSQL/H2/Oracle/SQLServer 5 种（MSSQL 别名归一 SQLSERVER）。`ApiMonitoringRepositoryE2ETest` 新增 Flyway 历史存在 + 旧库基线零丢数据 + Oracle/SQLServer 方言路由共 4 用例；受影响的 route 模块及上游编译零报错、E2E 6 例全绿 |
| **ROUTE-P1-N8** | codegen/工程化 | codegen 缺 `spotbugs-exclude.xml`（根 POM :453 对所有模块引用） | ✅ 已修复 | 补齐 `codegen/spotbugs-exclude.xml` 基线 |
| **WEB-P1-N9** | web | `BasePage.PAGE_SWITCH_LOCK` 等全局静态锁串行化 | ✅ 已修复 | `BasePage.PAGE_SWITCH_LOCK` 全局静态锁→per-context 弱引用锁映射（WeakHashMap<BrowserContext,Object>）+ 全局兜底锁（ctx 不可得时）；`ensurePageValid()` 按 context 取锁，不同 Context 页面切换真并行、同 Context 仍串行。补锁序文档（per-context 锁为最外层→管理器全局锁 PAGE_LOCK 在内，无反向路径防死锁）。新增 `BasePagePageSwitchLockConcurrencyTest`（5 例）固化，全护盾 BUILD SUCCESS |
| **CORE-P2-N10** | core | `TestContext` 不向子线程传播 | ✅ 已修复 | 新增 `CapturedContext` + `TestContextHolder.capture/restore/runWithContext/callWithContext` 传播桥；`AsyncPool.submitTask` 携带提交线程上下文（执行后复位，隔离保留） |
| **WEB-P2-N11** | core/route | `RouteLifecycle` 14 方法胖接口 | ✅ 已修复 | 拆 `CaptureControl`/`RegistryControl`/`EngineControl` 三接口（ISP）+ `RouteContextHandle` 标记接口 + no-op 适配器；`RouteLifecycle` 继承三者零破坏现网 |
| **CG-P2-N12** | codegen | 内嵌 JS 外置为资源 + 语法校验 | ✅ 已修复 | 内嵌 JS 全部外置为 `core/src/main/resources/scan/js/*.js`（85 个资源，含 PANEL_CORE/PICKER_CORE 拆分）；`RolePickerScripts` 经 `loadScript`/`loadScripts`/`concat` 纯引用，Java 零内嵌脚本；构建期 `tools/validate_picker_js.js`（core validate 阶段）+ `RolePickerScriptsJsValidationTest`（`node --check` 全量）校验语法；head/tail 分段为共享模块（START_SCRIPT / merge-key-shim）的 DRY 组合，不合并。全护盾 BUILD SUCCESS 零回归 |
| **ENG-P2-N13** | 工程化 | 构建告警清零（maven/javac/jacoco） | ✅ 已修复 | 父 pom `maven-install-plugin` 钉 3.1.1（消 8 处插件版本缺失告警）；`test-automation/pom.xml` 删 jacoco report 不支持的 `classfiles` 参数；`ConfigProvider`/`TestContextBridgeTest`(2)/`PageFrameShadowTest`(3) 共 6 处 javac unchecked 局部 `@SuppressWarnings` 或显式变量；全护盾 470 例绿，total_WARNING=0 |
| **WEB-P3-N14** | web | 收尾期日志误报：① 框架主动关闭浏览器被记 `[browser-disconnected] Browser disconnected unexpectedly` ERROR；② 关闭 BrowserContext 的时序竞态使下载保存抛 `TargetClosedError` 记 ERROR | ✅ 已修复 | ① `PlaywrightManager` 新增弱引用 `CLOSING_BROWSERS` + 收口方法 `closeBrowserInstance(Browser)`，5 处 `browser.close()`（`existingBrowser`/`oldBrowser`/清理 entry/崩溃重建 `current`/`cleanupAll` 遍历）全部改经收口（仅前置登记，异常仍交调用点既有 try-catch，行为零变更）；`onDisconnected` 据此区分「主动关闭→DEBUG」与「崩溃/被杀→ERROR」。② `PlaywrightContextManager` 新增包级私有 `isContextClosedError(Throwable)`：沿 cause 链识别 `TargetClosed`（类名或 "Target page, context or browser has been closed" 消息）降级为 DEBUG，真实保存失败（磁盘满/权限）仍 ERROR。新增 `DownloadSaveErrorClassifierTest`（4 例）固化边界。验证：全护盾 487 例绿、0 跳过；日志 ERROR 行 31→25，`browser-disconnected` ERROR 7→0（全部转 DEBUG 7） |

| **WEB-P1-N13** | web | BasePage/PlaywrightManager 多线程强化：线程归属守卫 + 禁止依赖 `playwright.impl` 的 ArchUnit 闸门 + 并发隔离单测 | ✅ 已修复 | `PageContextState` 新增 `ownerThread` + `assertOwningThread()`（跨线程访问抛 `IllegalStateException`）；`ArchitectureTest.mustNotDependOnPlaywrightImpl()` 固化 impl 禁令；`BasePageThreadOwnershipConcurrencyTest`（3 例）证同线程通过/跨线程拒绝/并行通过。另补设计 `17` §6.2 待办②：新增 `SharedBrowserContextIsolationE2E`（实浏览器 E2E，仅 public API；经 `ConcurrentScenarioExecutor.prepareSharedBrowser()` 预热共享 Browser，两并发线程各取 `getPage()`，断言「不同 Context + 同一共享 Browser + Cookie 双向隔离」四条不变式）+ `shared-browser-e2e` Maven profile（独立 fork 开 `serenity.playwright.shared.browser.enabled=true`）；本地 chromium 验证 `Tests run: 1, Failures: 0`（15.26s）。全护盾 487 零回归。详见 `17_设计_BasePage与PlaywrightManager_多线程与三大特性.md` §6.1/§6.2 |
| **WEB-P1-N14** | web | 共享 Browser 只读契约标注 + `validateConfigIdShape` 非法 configId 防御（设计 `17` §6.1①） | ✅ 已修复 | `BrowserRegistry` 类级/方法级 `@apiNote` 固化「共享 Browser 只读 + 可变操作持 `browserLock`」契约（未引 `@GuardedBy`：本仓 classpath 无 jsr305/checkerframework，强引编译风险，故用文档契约等价替代）；`realGetBrowser()` 入口新增 `validateConfigIdShape(String)` 对缺失 `'_'` 分隔符的非法 configId 抛 `IllegalArgumentException`。配套 `BrowserRegistryConfigIdValidationTest`（3 例，无浏览器同包白盒） |
| **WEB-P1-N15** | web | DI seam 防绕过 ArchUnit 闸门 `frameworkCodeMustNotMutateRuntimeSeam`（设计 `17` §6.1③） | ✅ 已修复 | `ArchitectureTest.frameworkCodeMustNotMutateRuntimeSeam()` 禁止生产 framework 代码调用 `PlaywrightManager.setProvider`（扫描 framework 主代码、排除 tests），守住「门面 → provider → 协作者实现」替换链路（对齐 WEB-P1-6） |
| **专项 19** | web | `SessionManager` per-sessionKey 共享层并发未表征（既有仅单线程缓存/淘汰 + per-thread Feature 标记隔离） | ✅ 已收口（无改动） | `SessionManagerSharedConcurrencyTest`（3 例，无浏览器）：并发读一致 / 删除×读取竞争终态稳定不损坏 / 过期并发驱逐幂等；`SessionManagerRestoreSingleFlightTest`（2 例，WEB-P0-2 mock provider 无真浏览器）：restoreSession 同 key 单飞（仅 1 leader 登录、follower 复用）+ 命中并发复用（STORAGE_CONTENT_CACHE 单飞）；共 5 例全绿、零异常、无生产代码改动。表征暴露 Windows 并发删过期 `.json` 偶发 `AccessDeniedException`，已被 `evictIfExpired` catch 降级 WARN（良性、不抛不损坏、幂等），佐证并发安全。详见 `19_整改专项设计_会话缓存并发治理.md` |
| **WEB-P1-3** | web | 监听器发现靠全量 `Class.forName`，单点失败全盘失败（W-16） | ✅ 属实（已读 `ListenerRegistry`:39-67/121-130/155-157，`initialize` 整段 `try/catch` 抛 `InitializationException` 中止） | ✅ 已修复（无接线行为变更） | `FrameworkListener` 标记接口 + `ServiceLoader` 主发现（`discoverViaServiceLoader`，逐 provider 容错）+ 4 处 `Class.forName` 扫描包成逐类 `try/catch` 跳过并 `WARN`；`initialize` 移除全盘 abort（仅基础包解析等致命配置错误才抛）；`initialized` 改 `volatile`（W-10 线程安全）；`ThucydidesStepsListenerAdapter implements FrameworkListener` 经 `META-INF/services` 声明（等价原扫描构造，零 Serenity 接线变更）。新增 `ListenerRegistryResilienceTest`（2 例）全绿；全 web 套件 18 例 0 失败/0 错误 BUILD SUCCESS。详见 `20_整改专项设计_WEB-P1-3_监听器SPI.md` |
| **WEB-P1-4** | web | 崩溃重跑白名单过宽（评审问题 W-15：按 `PlaywrightException` 类名模糊匹配，元素超时/断言失败也判崩溃→自动重跑掩盖真实缺陷；且重跑事件未写入 Serenity 报告） | ✅ 属实（已读 `BrowserCrashGuard.isCrash`:128-147，`CRASH_SIGNATURES` 含歧义的 `execution context was destroyed`/`browser process`） | ✅ 已修复（生产行为收敛：纯白名单收窄 + 报告标注，无新增失败路径，正常失败零重跑） | ① `isCrash` 移除异常类名模糊匹配（`playwrightexception` 子类如 `TimeoutError` 不再误判）；`CRASH_SIGNATURES` 收窄为明确崩溃信号（`target crashed`/`browser has been closed`/`target closed`/`connection closed`/`browser disconnected`/`browser crashed` 等），移除 `execution context was destroyed`（正常导航竞态）与 `browser process`（与启动消息重叠）；② 恢复门槛 `shouldAttemptRecovery` 显式纳入 `isHandleCorruption`（`__adopt__`/`previewUpdated` 句柄损坏仍触发强制重建重跑，不受类名移除影响）；③ 重跑事件经 `ContextTaskResult.replayed(...)` 携带 `recoveryType`/`firstFailureMessage`，`ConcurrentContextExecutor.runAll` 在**编排线程**调 `SerenityBusBridge.recordCrashRecoveryIfAny` → `Serenity.recordReportData()` 在报告中显式标注（工作线程不碰总线，守 9.3 桥接原则；无 Serenity 环境 try/catch 降级）。验收：① `BrowserCrashGuardTest.playwrightExceptionWithoutCrashSignatureIsNotCrash`/`doesNotFlagNormalFailuresAsCrash`（含 `TimeoutError`、断言、`Execution context was destroyed` 导航竞态均不触发重跑）；② `crashTaskIsReplayedOnceAndSucceeds` 断言 `isReplayed()==true` + `getRecoveryType()=="crash"`（数据前提，编排线程据此标注报告）。配套 `BrowserCrashGuardTest` 更新 + 新增 6 例共 16 例全绿；`ConcurrentContextExecutorTest`/`ContextTaskResultTest`/web(188) 全绿、零回归。详见 `21_整改专项设计_WEB-P1-4_崩溃重跑白名单.md` |
| **WEB-P1-5** | web | web 单测覆盖不足（核心类无 web/src/test 单测，行覆盖低） | ✅ 已修复（无生产行为变更，仅补测试） | ✅ 已修复（验收 ①、② 均达成） | `web/pom` 补 `mockito-core`(4.5.1) 测试依赖；**达 27 个测试类 / 188 例全绿**（全 web 套件 BUILD SUCCESS，**验收 ①「≥25 测试类」与 ②「JaCoCo 行覆盖 ≥25%（实测 25.10%）」均达成**）：新增 `PageNavigationTest`(8)/`CookieManagerTest`(9)/`PageViewportTest`(5)/`PageAccessibilityTest`(1)/`PageWaitsTest`(8)/`LocatorFactoryTest`(7)/`PageFrameShadowTest`(16)/`PageInteractionsTest`(20)/`ListenerPerfStatsTest`(4)/`PageDebugControlTest`(2)/`RoleCodegenBridgeRegistryTest`(3)/`ConcurrencyPartitionKeyTest`(12)/`WebExceptionsTest`(9)/`ListenerGuardTest`(7)/`ElementRectTest`(3)/`ConcurrentContextOptionsTest`(7)/`ContextTaskResultTest`(5)/`PageElementTest`(9)（全部无浏览器、Mockito 隔离）+ 既有种子 `ConcurrencyGateTest`(4)/`NLSUtilsTest`(3)/`ListenerRegistryResilienceTest`(2)；另补 Serenity 监听器脚手架 `ThucydidesStepsListenerAdapterTest`(13)/`PlaywrightListenerTest`(16)（discovery 守卫 + 反射置 `screenshotStrategy=DISABLED` + mock `RouteLifecycle` 隔离浏览器）。测量：`mvn -o -pl web -am test` 后 `mvn -o -pl web jacoco:report`，读 `web/target/site/jacoco/jacoco.csv`：line covered 2044 / total 8143 |

---

## 三、暂缓项（需架构决策或体量过大，建任务但不盲目改）

- **ENG-P0-2 / CORE-P0-1 / CORE-P0-2 / WEB-P0-2**：依赖下沉 + core 去 Serenity + DI seam，属地基级重构，需团队评审与分阶段 `mvn verify` 回归。
- **上帝类拆分**（WEB-P1-1 `PlaywrightManager` 1490 行 / WEB-P1-2 `BasePage` / RPT-P1-1 `SummaryReportGenerator`）：先做可测试性 seam 与单测，再拆。
- **CVE 基线治理（ENG-P1-1）**：属管理决策（8 CRITICAL 需安全团队书面签字），非纯技术修复。
- **CG-P1-1 Freemarker 化 / CG-P1-2 Golden Test**：高价值但需先"锁定现状"再迁移。
- **模块拆分 test-automation（ENG-P1-4）/ 包名对齐（ROUTE-P1-2、CG-P1-3）**：批量重命名，单独 PR。

---

## 四、验证方式

- 单模块编译/测试：`mvn -o -pl <module> -am test`
- 全护盾回归：`mvn -o -pl test-automation -am test`
- 门禁（基线就位后）：`mvn -o verify`
