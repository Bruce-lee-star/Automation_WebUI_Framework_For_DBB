# 架构评审整改计划（执行清单）

> 依据：`docs/architecture-review/00~10`（二次评审全量 96 项 / 260.5 人日）。
> 本文是**逐项修复执行清单**：每完成一项，勾选 ✅ 并同步更新 `10_整改执行跟踪.md`。
> 验证纪律（每次改动必跑）：
> - 单模块：`mvn -o -pl <module> -am test`
> - 全护盾回归：`mvn -o -pl test-automation -am test`
> - 门禁（基线就位后）：`mvn -o verify`

---

## 一、已修复（✅ 绿，BUILD SUCCESS 全护盾验证）

| 任务 | 模块 | 修复 | 验证 |
|---|---|---|---|
| ENG-P0-1 | 工程化 | 新增 `.github/workflows/build.yml`（push/PR→JDK21+缓存→`mvn -B test`→归档→required status check） | 文件已建 |
| WEB-P0-1 | web | `PlaywrightManager:690/742` 由 `perThreadBrowserLock()` 改 `browserLock(SHARED_BROWSER_MODE)` | 编译+并发单测 PASS |
| CG-P0-1 | codegen | `RoleElementPageGenerator` 生成标记+冲突拒绝覆盖+`.bak`+原子移动 | 编译通过 |
| CORE-P0-3 / N5 | core | `ShutdownCoordinator` 改 `CopyOnWriteArrayList`+执行后可重注册+`reset()` | 编译通过 |
| CORE-P1-N4 | core | `ContextKey.equals/hashCode` 纳入 `type` | 编译通过 |
| CORE-P2-N6 | core | `AsyncPool.getCompletedTaskCount()` 去除内部计数器重复计数 | 编译通过 |
| CORE-P1-N3 | core | `ConfigCipher.hexToBytes` 强校验 64 hex，杜绝 AES-128 静默降级 | 编译通过 |
| API-P0-1 | api | `ConfigProvider` 全局 `config` 字段改 `ThreadLocal<Config>`（per-thread 隔离） | 编译+全护盾绿 |
| ROUTE-P1-N1 | route | 方言前置 fail-fast：未知方言拒绝回落 MySQL；Oracle/SQLServer 已纳入 SUPPORTED_DIALECTS（各自 V1 迁移 SQL 已对齐） | 编译+全护盾绿（E2E 6 例） |
| ROUTE-P1-N8 | codegen/工程化 | 补齐 `codegen/spotbugs-exclude.xml`（根 POM 对所有模块引用） | 文件已建 |
| ENG-P2-N13 | 工程化 | 构建告警清零：父 pom 钉 `maven-install-plugin` 3.1.1；jacoco report 删无效 `classfiles`；6 处 javac unchecked 局部 `@SuppressWarnings` | 全护盾 470 绿，total_WARNING=0 |
| CORE-P0-1 | core | 去 Serenity：抽 `ConfigResolver` 接口 + SPI，Serenity 实现下沉 web | 全护盾 470 绿，core 0 个 Serenity import |

---

## 二、待逐项修复

### A. 可立即安全落地（小步、低风险）— 本轮推进

- [x] **CORE-P1-N7**｜core｜`AsyncPool` 直读 `ASYNC_*` 环境变量 = 第三套割裂配置体系 ✅
  - 修复方案：`AsyncPool.getEnvInt/Long/Double` 改调 `ConfigSource.resolve(key, null)`（env 名映射 `toEnvKey("ASYNC_CORE_THREADS")=="ASYNC_CORE_THREADS"` 对现网零变更，且新增 `-D`/serenity.properties/ENC() 覆盖）。
  - 验收：`mvn -o -pl test-automation -am test` → BUILD SUCCESS（配置统一，现网 env 仍生效）。

### B. 需先建单测 / Seam 或需 Serenity E2E（中风险，谨慎推进）

- [x] **ROUTE-P0-1**｜route｜断言失败未置用例 FAIL → ✅ 已修复
  - 真实根因：`getCurrentCapture()` 永不返回 null（Phase 5 后回退非空 `SHARED`），旧「`==null` 早退」已失效；漏检实为 route 事件线程经 `RouteUtil.captureContext` 回退 `getCurrent()` 把标志落在 `SHARED`，而测试线程只读 per-context 实例。
  - 修复：`RouteLifecycle` 增 `resolveFailureCapture()`（优先 per-context，否则 `SHARED` 兜底），`StepFailureAggregator` 两处检测改用之；新增 `AssertionFailureSurfaceTest`（2 例）固化。
  - 2026-09-08 NPE 收尾：`StepFailureAggregator` 两处 `RouteLifecycleRegistry.get().resolveFailureCapture()` 补 `get()` null 守卫（route 未注册时 `get()` 返回 null 抛 NPE，被 `ThucydidesStepsListenerAdapter` 吞掉掩盖）；未注册安全跳过，行为等价、日志干净。全护盾 470 例绿（commit 954f02b）。
- [x] **WEB-P1-N9**｜web｜`BasePage.PAGE_SWITCH_LOCK` 等全局静态锁串行化并行场景 → ✅ 已修复
  - 修复：`BasePage.PAGE_SWITCH_LOCK` 全局静态锁→per-context 弱引用锁映射（WeakHashMap<BrowserContext,Object>）+ 全局兜底锁；`ensurePageValid()` 按 context 取锁，不同 Context 真并行、同 Context 仍串行；补锁序文档（per-context 锁为最外层→管理器全局锁在内，无反向路径）。新增 `BasePagePageSwitchLockConcurrencyTest`（5 例）固化。
- [x] **CORE-P2-N10**｜core｜`TestContext` 不向子线程传播 → ✅ 已修复
  - 处置：新增 `CapturedContext` + `TestContextHolder.capture/restore/runWithContext/callWithContext` 传播桥；`AsyncPool.submitTask` 携带提交线程上下文（执行后复位，隔离保留）。见 `10_整改执行跟踪.md`。

### C. 需架构决策 / 大体量（暂缓，单独 PR，不盲目改）

> 属地基级重构，需团队评审 + 分阶段 `mvn verify` 回归。

- ENG-P0-2 ✅ 依赖下沉（根 POM `<dependencies>`→`dependencyManagement`，7 模块均已显式声明实际依赖；core 仅 HikariCP+logback-classic 2 个；Enforcer 通过；全护盾绿）
- CORE-P0-1 ✅ 已修复（core 抽 ConfigResolver 接口 + SPI，SerenityConfigResolver 下沉 web；core 0 个 Serenity import，详见 10 跟踪）
- CORE-P0-2 上下文线程模型（`WeakHashMap`+`InheritableThreadLocal`+ArchUnit 禁新增 static ThreadLocal）—— **✅ 已修复**：见 `12_整改专项设计_CORE-P0-2_线程模型.md`（全护盾 476 零回归）
- WEB-P0-2 ✅ 已修复：可测试性 DI seam（拆 `PlaywrightManager`/`BasePage` 上帝类前置）—— Provider 三接口 + 组合 `RuntimeProvider` + 薄适配默认实现 `DefaultRuntimeProvider` + `PlaywrightManager` 门面委托 + `BasePage` 构造去全局初始化；`WebRuntimeSeamTest` 3 例；全护盾 479 零回归（详见 `13_整改专项设计_WEB-P0-2_可测试性DI seam.md` §8）
- ROUTE-P1-N3 系列（route↛web 解耦与包名对齐）：
  - ROUTE-P1-1 ✅ 解耦完成（HikariConfigFactory / MonitorConfig / LanguageState 下沉 core，route/pom 移除 framework-web；全护盾 467 绿）
  - ROUTE-P1-2 ✅ 包名 framework.web.route → framework.route（已完成，全仓 86 文件 + ArchUnit 同步）
  - ROUTE-P1-3 ✅ ArchUnit 模块级「route 不得依赖 web」规则（L6，全护盾通过）
- WEB-P1-1 ✅ 拆 `PlaywrightManager`（1553 → 363 行，三条验收全绿，2026-09-08）
- lifecycle 跨子包封装治理专项 ✅ 已落地（详见 `16_整改专项设计_lifecycle跨子包封装治理.md`）：Phase 0–4（公开字段收敛 + ArchUnit L7 守护 / 状态根接口化 `LifecycleState` / 锁中介 `LifecycleLockMediator` 私有锁 / `CustomOptionsManager` 内部面收敛）+ 按职责子包拆分（10 类迁入 `state`/`lock`/`config`/`event`/`provider`/`concurrent` 子包）；全护盾 507 例绿、0 违规。专项收尾额外清理：迁入文件补 `@apiNote`、删 `ContextTaskResult.diagnostics` 死数据、`ConcurrentContextOptions.hardCap` 死分支、收口 `ProxyConfigResolver` 7 个场景别名方法（冗余间接层，9 调用点重定向 + 1 死代码删除）。残留偏差：A 锁顺序单测 `LifecycleLockMediatorTest` 已补（6 例，2026-09-10，覆盖规范序/逆序单线程嵌套 + 规范序多线程竞争 + Browser 双锁交叉竞争无死锁 + 锁字段 private 守护）；B/C 并入「浏览器选项治理」专项（`18_整改专项设计_浏览器选项治理.md`：偏差 C ✅ 已收口，偏差 B 治理中）。
- 浏览器选项治理专项（doc16 偏差 B/C）✅ 已收口（详见 `18_整改专项设计_浏览器选项治理.md`，2026-09-10）：偏差 C ✅——`CustomOptionsManager` 三处 flag 直写收口为单一入口 `setCustomContextOptionsFlag`（`CustomOptionsManagerConcurrencyTest` 绿）；偏差 B ✅——Phase 1 表征测试 6 例（FeatureModeSessionPreservationTest 4 + OptionModeParityTest 2，全护盾 519 绿）固化 Feature/Scenario 模式契约；B-1 残留缺口经运行时会话语义澄清确认良性（Feature 存活 context 已含 session、重建经 restoreSession 重应用）→ 无需行为修复、仅固化契约（§4.1/§4.3）；并新增 §4.4（flag 四重含义）、§4.5（并发语义：per-thread 隔离 + SessionManager per-sessionKey 单飞协调）固化契约。
- 会话缓存并发治理专项（专项 19，doc18 §4.5 识别的唯一残叉并发风险）✅ 已收口（详见 `19_整改专项设计_会话缓存并发治理.md`，2026-09-10）：新增 `SessionManagerSharedConcurrencyTest`（3 例，无浏览器纯文件/缓存层）固化 per-sessionKey 共享层并发契约——并发读一致 / 删除读取竞争终态稳定不损坏 / 过期并发驱逐幂等，BUILD SUCCESS、零异常；表征中暴露 Windows 并发删过期 `.json` 偶发 `AccessDeniedException` 已被 `evictIfExpired` catch 降级为 WARN（良性、已处理）；**专项收口后全护盾高负载下暴露过期驱逐 `evictIfExpired` 读盘竞争风暴（活性缺陷，场景 C 偶发 30s 超时），已于 Phase 3 根治（生产代码修复：`invalidate`→粘滞化 `META_CACHE.put(ABSENT_META)` + 去冗余二次读盘，commit a8602c4 @ DEV-V3），全护盾 528 例现 0 失败**。
- 全仓 `@Deprecated` 废弃代码清除 ✅（2026-09-10）：删除 route `RouteLiteralPathCache`（死代码类及测试 `RouteLiteralPathCacheTest`）、`DelayHandler`（无调用方，计算已上提 `RouteDelay`）、`CapturedApiCall` 4 个恒 false 方法（`isXhr/isFetch/isApiType/resourceTypeName`）、`MonitorFailureCollector.reset()`/`ApiMonitorOrchestrator.reset()`、`api` `ApiFrameworkConfig` 3 个 Selenium 遗留 ConfigKey + 3 个 getter、`RestJobProvider.setEndPoint/getEndPoint`；全护盾 524 例绿、BUILD SUCCESS、零回归。注：`PlaywrightManager` 静态门面未标 `@Deprecated`（兼容层非废弃），不在本次范围。
- WEB-P1-2 ✅ 拆 `BasePage`（1477 → 481 行、公开方法 107 → 39，三条验收全绿）：Phase 1–6 全部完成（by*→LocatorFactory、cookies→CookieManager、frame/shadow→PageFrameShadow、PageViewport/PageAccessibility、PageInteractions、状态机→PageContextState/PageDebugControl、删委托壳）；验收① ≤500 行、② 公开方法 ≤40、③ ArchUnit 全护盾通过。详见 `16_整改专项设计_WEB-P1-2_拆分BasePage.md`（原「公开方法≤40 与 API 零变更不可兼得」决策已以策略 C 落地：先内聚 + 兼容期 `@Deprecated` 委托 + 迁移后删委托达标）
- WEB-P1-6 ✅ DI 二期：静态门面 → 实例 `PlaywrightRuntime` + 协作者接口化（补齐「继承 / 运行时多态」，兼容策略 B：静态门面保留为薄兼容层使 53 个调用文件零改动）—— 详见 `15_整改专项设计_WEB-P1-6_DI二期_实例与接口化.md`
  - **Phase 1–3 全部 ✅（2026-09-09）**：Phase 1 接口提取（6 角色接口 + 6 无状态 `*Impl` 实现，全护盾 495 例）；Phase 2 实例门面 `PlaywrightRuntime`（组合根持有 6 角色 + 状态根，全护盾 497 例）；Phase 3 可替换性 / 真多态（`setInstance`/`resetInstance` seam + `DefaultRuntimeProvider` 经组合根委托，全护盾 503 例，每接口 ≥2 实现：生产 `XImpl` + 测试替身）。Phase 4 调用方迁移为可选/渐进（兼容层 `@Deprecated` 并最终移除，不阻塞验收）。
  - ⚠️ **ID 冲突待裁决**：`02_web模块评审.md`(L275) / `08_整改任务总表.md`(L104) 把 WEB-P1-6 复用为「解除全局 `CONTEXT_LOCK` 串行（分段锁）」(3d)，与上方 DI 二期重号。建议将"全局锁"任务重命名为独立 ID（如 WEB-P1-8）或并入多线程专题 `17`。注：全局锁分段化已由 doc16「锁中介 `LifecycleLockMediator`」实质承接（锁私有化 + 集中锁序）。
- WEB-P1-3 ✅ 监听器注册改 SPI（ServiceLoader 主发现 + 容错回退，根治 W-16 单点失败全盘失败）：`FrameworkListener` 接口 + `META-INF/services` + 4 处 `Class.forName` 逐类容错跳过 + `initialize` 移除全盘 abort；`ListenerRegistryResilienceTest`（2 例）全绿，全 web 套件 18 例 0 失败。详见 `20_整改专项设计_WEB-P1-3_监听器SPI.md`（2026-09-10）
- WEB-P1-5 ✅ web 单测补充（一期）：`web/pom` 已补 mockito-core；**已达 27 个测试类 / 188 例全绿（验收 ①、② 均达成）**（全 web 套件 BUILD SUCCESS、0 失败 0 错误 0 跳过）。本轮新增 11 个（全部无浏览器、Mockito 隔离 `BasePage/Page/Frame/Locator`）：`PageNavigationTest`(8) / `CookieManagerTest`(9) / `PageViewportTest`(5) / `PageAccessibilityTest`(1) / `PageWaitsTest`(8) / `LocatorFactoryTest`(7) / `PageFrameShadowTest`(16) / `PageInteractionsTest`(20) / `ListenerPerfStatsTest`(4) / `PageDebugControlTest`(2) / `RoleCodegenBridgeRegistryTest`(3) / `ConcurrencyPartitionKeyTest`(12) / `WebExceptionsTest`(9) / `ListenerGuardTest`(7) / `ElementRectTest`(3) / `ConcurrentContextOptionsTest`(7) / `ContextTaskResultTest`(5) / `PageElementTest`(9)；加既有种子 `ConcurrencyGateTest`(4)+`NLSUtilsTest`(3)+`ListenerRegistryResilienceTest`(2)。**验收 ①（≥25 类）已达成**，**验收 ②（JaCoCo 行覆盖 ≥25%）实测 25.10%，已达标**（`mvn -o -pl web -am test` 后 `mvn -o -pl web jacoco:report`，读 `web/target/site/jacoco/jacoco.csv`：line covered 2044 / total 8143；instruction ~18%、branch ~9%）。达成路径：补 Serenity 监听器测试脚手架——`ThucydidesStepsListenerAdapterTest`(13) 全量委派转发契约 + `PlaywrightListenerTest`(16) 以「discovery 阶段守卫（默认 `discoveryPhaseCompleted=false` 使 `testStarted` 提前返回、不初始化浏览器）+ 反射置 `screenshotStrategy=DISABLED`（跳过所有截图路径）+ 注入 mock `RouteLifecycle`（消 `RouteLifecycleRegistry.get()` 返回 null 的 NPE）」三件套隔离浏览器，覆盖 `testStarted`/`testSkipped`/`stepStarted`/`stepFinished`/`stepFailed`/`testFailed`/`testFinished`/`testRunFinished` 等完整生命周期；并用适配器 `clearDelegateListeners()` 隔离真实 `PlaywrightListener`/`AxeCoreListener`，`@After` 反射复位 `discoveryPhaseCompleted` 防污染、`RouteLifecycleRegistry.register(null)` 复位注册表。注：`lastStepFailed` 第 472 行 `takeScreenshotAndRegister("FINAL_FAILURE")` **无条件**触发（不受策略守卫），故未纳入无浏览器测试。关键坑：① `NavigateOptions`/`ClearCookiesOptions` 无 `getWaitUntil`/`getName` getter，改用 `any`/`eq` 选项对象断言；② `Page.waitForTimeout` 返回 **void**，禁用 `when(...).thenReturn(...)`（javac 报"lambda not allowed"），只能 `verify`；③ 基本类型 `double` 参数须用 `anyDouble()`，误用对象 `any()` 会抛 `InvalidUseOfMatchers` 并污染同 JVM 后续测试（表现为无关类连带失败）；④ `AriaRole` 在 `com.microsoft.playwright.options` 子包；⑤ Frame 分支须用 `Frame.GetByRoleOptions`（非 `Page.*`）；⑥ `Optional.empty()` 是 JVM 单例，不可断言 reset 前后"非同一实例"；⑦ `ElementOperationException(operation, selector, message)` 的 operation/selector 是**独立字段不并入 message**，须经 `getOperation()/getSelector()` 断言
- WEB-P1-4 ✅ 收窄崩溃重跑白名单（白名单收窄 + 报告标注）：`isCrash` 移除 `playwrightexception` 类名模糊匹配，仅按消息特征匹配明确崩溃信号（`target crashed`/`browser has been closed`/`target closed`/`connection closed`/`browser disconnected`/`browser crashed`，移除歧义的 `execution context was destroyed` 与 `browser process`）；恢复门槛 `shouldAttemptRecovery` 显式纳入 `isHandleCorruption`（`__adopt__`/`previewUpdated` 句柄损坏仍触发强制重建重跑）；重跑事件经 `ContextTaskResult.replayed(...)` 携带 `recoveryType`/`firstFailureMessage`，`ConcurrentContextExecutor.runAll` 在编排线程调 `SerenityBusBridge.recordCrashRecoveryIfAny` → `Serenity.recordReportData()` 在报告显式标注。`BrowserCrashGuardTest`(16)/`ConcurrentContextExecutorTest`/`ContextTaskResultTest`/web(188) 全绿、零回归。详见 `21_整改专项设计_WEB-P1-4_崩溃重跑白名单.md`（2026-09-10）
- WEB-P1-7 ✅ 配置体系收敛（双 FrameworkConfig 改名与口径统一）：web 枚举 `FrameworkConfig`→`WebFrameworkConfig`；api 类 `FrameworkConfig`→`ApiFrameworkConfig`（消除同名类冲突，验收①）；core 抽出共享 `ConfigKey(key/默认/描述)` 记录（构造强校验），api 侧全量配置项改为 `ConfigKey` 三元组常量、getter 经 Typesafe 解析语义逐字等价，web 枚举新增 `configKey()`/`allConfigKeys()` 暴露同一规范（验收②）；新增 `ConfigUnificationTest`(3) 固化两条验收。全护盾 528 例曾 1 例 `SessionManagerSharedConcurrencyTest` 时序偶发——实为专项 19 `evictIfExpired` 读盘竞争风暴（非本改动引入），已于专项 19 Phase 3 根治，全护盾现 528 例 0 失败。详见 `22_整改专项设计_WEB-P1-7_配置体系收敛.md`（2026-09-10）
- API-P1-1~P1-4 配置统一 / 解耦 Serenity / 单测 / correlationId
- CORE-P1-1/CORE-P1-2/CORE-P1-3/CORE-P1-4 配置统一 / AsyncPool 可重建 / 防调度器泄漏 / core 单测
- RPT-P1-1~P1-3 / CG-P1-1~P1-5 / ENG-P1-1~P1-7 / 全部 P2（CVE 治理、Checkstyle 扩规、JaCoCo、文档治理、JS 外置等）

> 见 `08_整改任务总表.md`（完整 96 项工时/依赖图）与 `09_补充实证发现与增量任务.md`。

---

## 三、执行顺序（依赖图，来自 08 第三节）

```
ENG-P0-1(CI) ✅ ── 解锁所有验证
  ├─ ROUTE-P0-1（需 Serenity E2E）
  ├─ API-P0-1 ✅ / WEB-P0-1 ✅ / CG-P0-1 ✅（并行 P0 已清）
  └─ CORE-P0-1 → ENG-P0-2（core 去 Serenity 后依赖下沉）
        └─ CORE-P0-2（✅ 已修复）/ CORE-P0-3 ✅
  └─ WEB-P0-2（DI seam，拆上帝类前置）
```

## 四、本轮增量小项进度（来自 09）

已清：N3✅ / N4✅ / N5✅ / N6✅ / N1✅(未知方言 fail-fast + Oracle/SQLServer 已支持) / N8✅ / N7（CORE-P1-N7 ✅）/ N9（WEB-P1-N9 ✅ per-context 锁）/ N10（CORE-P2-N10 ✅ 传播桥）/ N11（WEB-P2-N11 拆 RouteLifecycle 胖接口 ✅）/ N12（CG-P2-N12 JS 外置 ✅）/ N2（ROUTE-P1-N2 Flyway ✅ 含 Oracle/SQLServer）＋ 既有 P0 四项 ✅
剩余增量：无（本轮 12 项增量全部完成 ✅）；2026-09-08 新增 ENG-P2-N13 构建告警清零 ✅（见上表，commit 954f02b）；WEB-P1-N13/N14/N15（设计 `17` §6 ①②③：多线程强化 + 共享 Browser 只读契约/非法 configId 防御 + DI seam 防绕过 ArchUnit，含 §6.2 ② 共享 Browser 模式 Context 隔离实浏览器 E2E）✅（见 10 跟踪）

---

## 五、全护盾后复核与剩余（2026-09-10）

### 已完成（本次复核纠偏，此前记录部分已过时）
- **RouteEngine 拆分**：`route/.../core/RouteEngine.java` 实测 **395 行**（<400 目标已达成），原「🔶 部分未达成」记录已过时 → 标记 ✅。
- **lifecycle 锁中介 `LifecycleLockMediator`**：`web/.../lifecycle/lock/LifecycleLockMediator.java` 已存在（main+test），Phase 3 实际已落地 → 标记 ✅。
- **WEB-P0-2 验收②**：`ArchitectureTest.frameworkCodeMustNotMutateRuntimeSeam` 已禁 `setProvider` 调用；本次新增 `frameworkCodeMustNotDependOnLifecycleImpls` 补全「禁依赖 `*Impl` 实现类」→ 验收② ✅（commit 4dc6d3b @ DEV-V3）。
- **并发方案 9.5 虚拟线程**：`WebFrameworkConfig.PLAYWRIGHT_CONCURRENT_USE_VIRTUAL_THREADS` + `ConcurrentContextOptions.defaults()` 接入配置开关（默认 false）→ ✅ 可配置驱动（commit 57a20f2 @ DEV-V3）。

### 无效 / 移除
- **route P6 `PassiveMonitorRegistry.process()` 触发源**：全库搜索 `PassiveMonitor` 零命中，该类在当前代码库**不存在**（既往整改已移除/未落地），从待办移除，非回归。

### 剩余（F4/F5/F6，按优先级）
- **F4（可选/渐进，非阻塞）WEB-P1-6 Phase 4 调用方迁移**：53 个外部文件静态 `PlaywrightManager.*` 调用尚未迁移为实例调用；兼容层 `@Deprecated` 保留，不阻塞验收，按计划渐进，暂不做。
- **F5 并发方案 R2/R7**：
  - R2 梯度压测硬上限校准（4→8→16）：当前硬上限固定 16，需真实 Browser 实测校准；
  - R7 共享 Browser 崩溃 replay 端到端：需 CI 真实 Browser/Server，**本环境无法落地**，记入待办。
- **F6 文档/管理项（复核结论：均无代码动作）**：
  - ID 重号：复核 `08` 后不成立。`WEB-P1-6`（DI 二期）与 `WEB-P1-N9`（全局静态锁串行化，即「全局锁分段」所指）为**不同 ID**，无重号，无需改名。
  - ArchUnit `DoNotIncludeTests`「缺口」：属设计内。12 条 framework 内部面规则用该选项（业务测试允许用 internal API，不削弱守护）；L7 两条刻意不用以捕获业务越界（`ArchitectureTest` 316-317 行注释已说明）。无缺口、无需改代码；如需更严可另加「业务不得依赖 internal API」规则，但非必需。
  - WEB-P1-2 Phase 5：已并入 Phase 6，`WEB-P1-2` 整体完成（Phase 6 删委托复测全护盾 487 绿），无残留。

> 说明：以上「已完成」项此前在对话清单中误列为未完成，本次以代码实况复核纠正。
