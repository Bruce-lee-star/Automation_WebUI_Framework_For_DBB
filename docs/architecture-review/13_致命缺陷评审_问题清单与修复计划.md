# 致命缺陷评审（逐模块 · 逐文件）—— 问题清单与修复计划

> 评审时间：2026-09-17（二轮，深查版）
> 方法：5 路并行逐文件精读 28 个最大/最关键文件，聚焦 **FATAL/CRITICAL**；明确排除风格、行数、命名、注释类"为降行数而评"的表面问题。
> 结论基调：框架工程质量整体很高（AES-256-GCM、PreparedStatement 参数化、try-with-resources、WeakHashMap 防泄漏、关闭编排、并行锁顺序纪律、脱敏主路径均已落实）。真正的致命点集中在三处：**并行模型本身、录制器并发、CI e2e 缺口 + route 事件线程阻塞**。

---

## 0. 与一轮评审的差异（本轮逐文件核实后纠正的早期误判）

| 一轮评审论断 | 本轮核实结论 | 证据 |
|---|---|---|
| P0：凭据明文入库（serenity.conf 明文密码/主密钥） | **已不存在**：密码/OTP 改为环境变量注入，文件内明文已移除 | `serenity.conf:130-144` `password: ${?DBB_TEST_PASSWORD}`、`token.security.url: ${?DBB_OTP_URL_O63}`，注释:130 明确"已从本文件移除，由环境变量注入" |
| P0：API 日志 `log().all()` 绕过脱敏 | **已闭合**：RestAssured LogConfig 指向 `SanitizingPrintStream` | `AbstractRestJob.java:289-290、153、240`，`SanitizingPrintStream.java:64` 经 `SensitiveDataSanitizer` 出口脱敏 |
| MonitorHandler 吞断言异常→监控失败静默通过 | **已修复**：监控失败会正确导致用例失败 | `MonitorHandler.java:693-762` 断言失败置标志→`ApiAssertionException` 上抛→步骤结束必抛 AssertionError |
| 默认只跑 `@test1` 一个场景 | **不准确**：默认 `not @skip`（跑全部未跳过场景） | `pom.xml:85`、`<tags>not @skip</tags>`、`test-automation/pom.xml:197` `-Dcucumber.filter.tags=${tags}`；`@test1` 仅出现在测试注释 |
| build.yml 只跑 `mvn -B test` | **不准确**：主门禁 `mvn -B verify -Psca -DskipITs`，另起 e2e job | `build.yml:35`、`build.yml:72 continue-on-error:true`、`build.yml:92-95` e2e job |

**遗留真问题**：主门禁 `-DskipITs` 跳过全部 IT + e2e job 配置错误（见 CI-C1/CI-C2），浏览器回归在 CI 上仍等于 0 实质覆盖。

---

## 0.1 复核勘误（2026-09-18，结合代码逐行核实）

| 项 | 原评审 | 复核结论（结合代码） | 关键证据 |
|---|---|---|---|
| **WEB-F2 定性与严重度** | FATAL：多 JVM 并行各自登录 → 服务端单会话互踢 | **下调为 CRITICAL（条件性）**。① 本项目并行模型是**单 JVM 多线程**：根 / `test-automation` pom 均硬编码 `forkCount=1`，`junit-platform.properties` 明文「不启多 JVM」铁律，`-Pparallel` 也不改 forkCount；② `restoreSession` **缓存优先**——先 `hasSession()` 读 META_CACHE→磁盘 `.json/.meta`（磁盘即跨 JVM 真相源），命中即 `applyStorageState` 复用落盘 storageState 并 `return true`，**根本不进登录单飞**；仅「磁盘无有效 session」（冷启动首登 / TTL 过期被驱逐）才走单飞；③ 同身份另有框架层 `ConcurrencyGate` 前置串行。故**稳态并行零登录、零互踢**；评审把「冷启动首登一次」误作「并行常态」 | `pom.xml:478`、`test-automation/pom.xml:179`、`test-automation/.../junit-platform.properties`、`SessionManager.java:456-528`、`ConcurrencyGate.java:14-29` |
| **P0-2 修复优先级** | P0 必做（跨 JVM 文件锁） | **降为 P2 级（可选/暂缓）**：单 JVM 模型下「缓存优先 + per-JVM 单飞 + ConcurrencyGate」三层已覆盖，跨进程文件锁无收益；仅当未来确要启用 `forkCount>1` 时再补 | 同上 |
| **WEB-C2 机理/严重度** | CRITICAL：并发写 HashSet → HashMap 结构损坏/死循环 | **机理更正 + 降为休眠态**：JDK 21 下「结构损坏/死循环」不成立（JDK8+ 已重写 `HashMap`，消除并发扩容成环），实际至多丢条目/偶发 CME；且触发前提「引擎级并行」默认关闭（仅 `-Pparallel`），并有 `testSuiteFinished` 置位兜底 | `PlaywrightListener.java:110,117,899-906,936`、`pom.xml:27-28,478`、`ThucydidesStepsListenerAdapter.java:45,71` |
| **CG-C3 机理/严重度** | CRITICAL：stop/abort/done/scan 均不清理 → 蓝框与监听器常驻 | **机理更正 + 降为 HIGH**：`cmdStop/cmdAbort/cmdDone` 经 `stop()`→`STOP_SCRIPT`（`picker-stop.js` 检测 `__regionSelecting` 时调 `__roleEndRegionSelect` + 移除监听 + 清高亮）**已等价补偿**，「常驻至页面关闭」不成立；仅 `cmdScan` 未补偿（且 UI 在选区态置灰扫描按钮） | `picker-stop.js:5-14,62`、`RoleElementPicker.java:358`、`RolePickerCommandEngine.java:152-164,206-211` |
| **RT-C3 措辞** | `currentScenarioKey`/`currentScenarioDir` 为「全局 static volatile」 | **措辞更正（机制成立）**：二者及 `counters` 为**单例实例字段**（非 static），因 `INSTANCE` 单例 + private 构造 → 等效单 JVM 全局；跨场景清 counters 的隐患成立 | `FileStoreMonitorCallback.java:80,98,104,107,126,239-252` |
| **CI-C1 因果链** | chromium-only 安装 → 找不到本地 Chrome | **因果更正（结论方向成立）**：GitHub `ubuntu-latest` 预装 Google Chrome，`channel:"chrome"` 未必失败；真正致命因子是 `headless:false`（托管 runner 无显示器）。「e2e 跑不起来 + `continue-on-error` 吞红 → CI 绿」仍成立 | `serenity.conf:44,58`、`build.yml:72,92,95` |
| **CORE-C4 措辞** | 监控数据「静默丢弃」 | **措辞更正（机制成立）**：丢弃有计数 + warn/error 日志 + 报告红条（`MonitorDataLossReporter`→`MonitorFailureReportWriter`），非纯静默；AsyncPool 级丢弃未进测试报告；`runOnMonitorCallbackThread` 当前无生产线调用者；丢弃不参与用例 PASS/FAIL | `ApiMonitoringRepository.java:77,256-265,332-351`、`AsyncPool.java:80-97,127-135`、`MonitorDataLossReporter.java:39-50` |
| **CORE-C3 补充** | 全局 `globalLang` 污染 | **成立 + 精确化**：override 半侧已经 `TestContextHolder`→`ScenarioContext` 场景级绑定；但 `globalLang` 半侧仍进程级，且 `reset()` 亦全局写入并占更高序号，会用空值压掉并行邻居的 override（比原判更强） | `LanguageState.java:29-34,59-61,71-77,84-92` |
| **CORE-C7 补充** | 疑似假绿 | **成立（潜在，未复现）**：机制成立、未发现规避；时序不依赖 `@After` 内部排序（所有 Cucumber `@After` 均早于 `testFinished`）；仓库当前无「收软断言失败却不 `assertAll()`」用例 | `FrameworkHooks.java:43-51,63-66`、`StepFailureAggregator.java:110-116`、`PlaywrightListener.java:1114,1183` |
| **CI-C3 行号** | `LoginSteps.java:266-267,219` | **行号更正**：实际 `:267-269,220`（结论成立） | `LoginSteps.java:267-269` |

> 复核方法（两轮，2026-09-18）：仅对**经代码逐行核实**的条目作修正，未核实项保持原判。已抽查并**确认原判成立**：WEB-F1、WEB-C1、CORE-C1/C2/C5/C6、RT-F1/F2/C1/C2/C4、CG-F1/C1/C2/C4/H1/H2、CI-C2/C4/C5/M1；已更正：WEB-F2、WEB-C2、CG-C3、RT-C3、CI-C1、CORE-C4（措辞）、CORE-C3/C7（补充）、CI-C3（行号）。证据见各条。

---

## 1. 问题清单（按模块）

### 1.1 Web 生命周期与并发（web 模块）

| 编号 | 严重度 | 文件:行 | 缺陷 | 致命机理 | 触发条件 |
|---|---|---|---|---|---|
| **WEB-F1** | FATAL | `web/.../page/factory/PageObjectFactory.java:178,319-333` + `page/engine/PageContextState.java:211-221` | 默认 `SINGLETON` 生命周期与 per-instance `ownerThread` 跨线程守卫冲突 | `singleInstances` 是 JVM 静态 Map，所有线程 `getPage(X)` 返回同一实例；首次访问把 `ownerThread` 绑当前线程，他线程访问即抛 `cross-thread access denied`。并行下除首线程外所有线程对每个页面类必抛异常 | 单 JVM 多线程并行（forkCount=0、threads>1） |
| **WEB-F2** | CRITICAL（条件性）▼ | `web/.../session/SessionManager.java:65-70` | 登录单飞守卫不跨 JVM（稳态并行缓存优先、零登录；仅 `forkCount>1` 冷启动首登窗口才咬人） | `loginGuards` 是 JVM 静态 `ConcurrentHashMap`，注释自承"FileChannel 锁无法跨 JVM"。forkCount>0 时各 JVM 同 sessionKey 都判"不存在"→各自真实登录→互踢（▼ 复核更正：稳态并行走缓存命中、零登录；仅 `forkCount>1` 冷启动窗口成立，详见 §0.1） | 仅人为将 `forkCount` 调至 >1（违反「不启多 JVM」铁律）且冷启动首登窗口 ▼ |
| **WEB-C1** | CRITICAL | `web/.../lifecycle/LifecycleLockMediator.java:21-25,50-53` | Context/Page 锁为进程级静态锁（仅 BROWSER_LOCK 为 per-thread） | 并行下所有线程的页面/上下文操作被同一把全局锁串行化，并发收益被吞；长持锁会饥饿其余用例 | 并行度>1 且大量页面切换/上下文创建 |
| **WEB-C2** | CRITICAL（休眠态）▼ | `web/.../listener/PlaywrightListener.java:117,901-906` | `seenStoryNames` 非线程安全 `HashSet` + `discoveryPhaseCompleted` 复合写 | 单例 `StepListener` 被所有并发场景共享（`ThucydidesStepsListenerAdapter` 实例字段，无同步保护）；并发写 `HashSet` 可致丢条目/偶发 CME。▼ 复核更正：JDK 21 下「结构损坏/死循环」不成立（JDK8+ 已消除并发扩容死循环），且触发前提「引擎级并行」默认关闭（仅 `-Pparallel` 开启） | 并行且多 Story 几乎同时启动 |

### 1.2 Core 内核（config/context/security/async/persistence）

| 编号 | 严重度 | 文件:行 | 缺陷 | 致命机理 | 触发条件 |
|---|---|---|---|---|---|
| **CORE-C1** | FATAL | `core/.../common/security/SensitiveDataSanitizer.java:891-912,833-846` + `common/logging/SanitizingMessageConverter.java:54` | `%msg` 出口泄露行内 `key=value` 凭据 | `%msg` 出口走 `sanitizeFreeText`，其 `maskFreeTextLine` 只取行内首个 `:`/`=` 前片段作 key；若该片段非敏感词（如 `Login failed: password=s3cr3t`）就退到只认 `Bearer/JWT` 与 `?key=`/`&key=` 的正则，**不认任意位置 `password=`/`token=`**。异常栈出口（`sanitizeLine`）已修此问题，两出口行为不一致 | 任何含"非敏感词:"前缀的日志拼入凭据（极常见） |
| **CORE-C2** | FATAL | `core/.../common/logging/SanitizingMessageConverter.java:53-58` | `%msg` 出口脱敏异常时回退原文 | 脱敏逻辑抛异常时 `catch` 直接 `return message`（明文）。异常栈出口刻意用 `[LOG SUPPRESSED]` 抑制，方向相反；一旦脱敏器在出口抛错，敏感值直接落盘 | 脱敏器自身异常（ReDoS/超大输入/编码问题） |
| **CORE-C3** | CRITICAL | `core/.../common/context/LanguageState.java:30,50-77` | 跨并发场景语言态污染（全局可读） | `globalLang` 为静态 `AtomicReference`，`getLanguage()` 当线程无 override 时返回进程级全局值；并行下后写者覆盖先写者，未显式 `setLanguage` 的场景线程读到别处刚设的语言（▼ 复核补充：override 半侧已经 TestContextHolder→ScenarioContext 场景级绑定，但 `globalLang` 半侧仍进程级，且 `reset()` 亦全局写入并占更高序号，会用空值压掉并行邻居的 override） | 并行 scenario 语言不一致、且部分场景未调用 `setLanguage` |
| **CORE-C4** | CRITICAL | `core/.../common/async/AsyncPool.java:80-97,457-463` + `route/.../persistence/ApiMonitoringRepository.java:256-265,344-351` | 监控数据在压力下被丢弃（有计数/日志/报告红条，非纯静默） | `ApiTrafficLogger.record` 经 `AsyncPool` 落盘；队列满用 `DiscardOldestPolicy` 丢任务、MONITOR 串行队列满直接 drop；route 成功路径也按 `PENDING_HARD_CAP` 丢最旧（`MonitorDataLossReporter` 计数并进报告红条）；AsyncPool 级丢弃未进测试报告、`runOnMonitorCallbackThread` 当前无生产线调用者；丢弃不参与用例 PASS/FAIL ▼ | 接口量大、DB 刷库变慢或慢回调堆积 |
| **CORE-C5** | FATAL | `core/.../common/security/SecretValue.java:65-78,109-117` + `common/security/ConfigCipher.java:74-86` | 裸 base64 启发式 + 严格模式可致整套启停失败 | `framework.secret.allow-bare-base64=true`（默认）下，任意"恰好合法 base64 且 >12 字节"的普通配置值都会被 GCM 尝试解密；认证失败→`IllegalStateException`（严格模式默认）→框架启动即崩 | 任一普通配置值恰为较长 base64 串 |
| **CORE-C6** | CRITICAL | `core/.../common/security/SensitiveDataSanitizer.java:494-519,608-617,974-983` | 畸形/截断 JSON 在兜底路径泄露 | `{"password":"secretval` 解析失败→`sanitizeFreeText`，`maskFreeTextLine` 取首个 `:` 得 key `{"password"`（`normalizeKey` 只去 `_-.空格`，保留 `{`/`"`）→不匹配→值明文漏出 | 超长/流式/截断响应体、含破坏 JSON 的控制字符 |
| **CORE-C7** | CRITICAL(待实跑确认) | `web/.../listener/StepFailureAggregator.java:110-133` + `tests/glue/FrameworkHooks.java:43-51,63-66` | 软断言"收口 vs 清理"顺序风险（疑似假绿） | `@After(order=MAX_VALUE)` 先 `SoftAssertions.clearForCurrentThread()`；若 Serenity `testFinished` 聚合晚于该 `@After`，则聚合器看到 `hasFailures()==false` 直接返回→场景判 PASS（▼ 复核：机制成立、未发现规避；但时序不依赖 @After 内部排序——所有 Cucumber @After 均早于 testFinished；且仓库当前无「收软断言失败却不 assertAll()」用例，属潜在而非已复现） | 存在未显式 `assertAll()` 的软断言场景 |

### 1.3 Route 模块（API 录制/回放/监控）

| 编号 | 严重度 | 文件:行 | 缺陷 | 致命机理 | 触发条件 |
|---|---|---|---|---|---|
| **RT-F1** | FATAL | `route/.../handler/MonitorHandler.java:288,475` | MonitorHandler 在 Playwright 事件线程上同步阻塞 | `page.waitForResponse`（20s）+ `future.get(budgetMs)`（30s）直接在 `handle` 内阻塞事件线程；该 context 下所有其它 API 路由分发被串行化→高并发请求排队→级联超时、防重桶膨胀 | 单 context 内 API 密度高（列表页/并发接口）、后端响应慢 |
| **RT-F2** | FATAL | `route/.../persistence/FileStoreMonitorCallback.java:177` | FileStore 在事件线程同步写盘（注释与实现不符） | Javadoc 声称"写入在 AsyncPool 异步线程"，实际 `Files.write` 在 `onResponse`（事件线程）内执行，叠加到 RT-F1 的阻塞上进一步拖垮吞吐 | 开启 `monitor.file.store.enabled=true` 且高 API 量 |
| **RT-C1** | CRITICAL | `route/.../handler/MonitorHandler.java:303-313,317-326` | 监控观测静默失败→用例假绿（fail-open） | `waitForResponse` 超时/异常后 `safeResume`+`fallbackResponse(req)`；若返回 null 直接 `return`——本次 API 完全不被断言/存储/报错，即便后端 5xx 也通过 | 并发/后端抖动使 waitForResponse 撞预算、响应体瞬时 null |
| **RT-C2** | CRITICAL | `route/.../persistence/ApiMonitoringRepository.java:256-265,332-351` | 监控数据压力下静默丢弃，不影响测试结论 | 超 `PENDING_HARD_CAP` 即 `poll()` 丢最旧；flush 重试 `MAX_ATTEMPTS=3` 后 `droppedCount++` 丢弃；测试仍 PASS，CI 上 API 回归数据缺失无人知晓 | 高并发监控 + 慢 DB / 连接池饱和、JVM 关闭窗口残留 |
| **RT-C3** | CRITICAL | `route/.../persistence/FileStoreMonitorCallback.java:104-107,239-250` | FileStore 全局 scenario 状态→并行场景串目录/序号错乱 | `currentScenarioKey`/`currentScenarioDir`/`counters` 实为**单例实例字段**（非 static，但 `INSTANCE` 单例 + private 构造 → 等效单 JVM 全局）；并行 Scenario 各自线程调用 `onResponse`，B 场景切换把全局计数器清零，A 场景正在写的文件序号被打断/写进错误目录 ▼ | Serenity 并行（threadCount>1）且开启文件存储 |
| **RT-C4** | CRITICAL | `route/.../core/capture/ApiCaptureContext.java:197-210,129-154` | 共享 BrowserContext（feature 模式）下 reset 跨场景污染 | 多个 Scenario 复用同一 BrowserContext→共享同一 `ApiCaptureContext`/`ResponseStore`；某 Scenario 结束触发 `resetCurrent()` 清空**正在运行**的另一 Scenario 的采集与断言标志 | 共享 Context + 并行/快速连续 Scenario |

### 1.4 Codegen 与 Page（录制器 + 代码生成）

| 编号 | 严重度 | 文件:行 | 缺陷 | 致命机理 | 触发条件 |
|---|---|---|---|---|---|
| **CG-F1** | FATAL | `codegen/.../web/page/scan/RolePickerCommandEngine.java:133,195,702` | `javaPickBySig` 读端未加锁，与写端并发致 `ConcurrentModificationException` | 写端 `RolePickerBridgeRegistry`/`RolePickerPanelSync` 用 `synchronized(map)` 保护；但命令引擎在面板主循环线程对 `javaPickBySig.values()` 做 `for` 遍历**完全没加锁**，Playwright `onPick` 回调在事件线程并发 `put`→CME→录制会话崩溃 | 点停止/开始瞬间仍有拾取回传，或多页/多 Context 同时录制 |
| **CG-C1** | CRITICAL | `codegen/.../web/page/scan/RolePickerPickParser.java:87-89` | `_pageClass` 缺失时去重键退化，跨页归类串味 | `getPageClass()` 与 `_pageClass` 均 null 时 `pc=""`，role 分支返回裸 `sigKey`，两页同名元素（如各页"关闭/下一步"）合并成同一条 entry→A 页元素数据被 B 页覆盖 | `window.__rolePageName` 尚未写入时的空窗（弹窗/about:blank/急遽导航）、`reconcileTrackedPages` 重算前 |
| **CG-C2** | CRITICAL | `codegen/.../web/page/scan/RolePickerCommandEngine.java:139,186,455,685,722` | `pageNames.keySet().iterator().next()` 在空/竞态 Map 上抛 `NoSuchElementException` | `pageNames` 为 `ConcurrentHashMap`，仅取首个键代表全会话；reconcile 与命令执行间若唯一被跟踪页被关闭/移除，`next()` 直接抛异常→命令处理器崩溃 | 并发录制中某页被快速关闭后立刻点开始/停止 |
| **CG-C3** | HIGH（▼ 复核下调） | `codegen/.../web/page/scan/RolePickerCommandEngine.java:152,159,320,442-464,553` | 区域选择态显式清理仅在 `cmdRegionScanned`；`cmdScan` 缺补偿（stop/abort/done 已等价补偿） | `START_REGION_SELECT_JS` 挂载蓝框与 click/mousemove 监听；显式 `END_REGION_SELECT_JS` 仅在 `cmdRegionScanned`。▼ 复核更正：`cmdStop/cmdAbort/cmdDone` 经 `stop()`→`STOP_SCRIPT`（`picker-stop.js` 检测 `__regionSelecting` 时调 `__roleEndRegionSelect`+移除监听+清高亮框）**已等价补偿**，不存在「常驻至页面关闭」；仅 `cmdScan` 未补偿 | 进入区域点选态后未点中业务区即关闭/停止/中止 |
| **CG-C4** | CRITICAL | `codegen/.../web/page/scan/RoleElementPageGenerator.java:487-512` | 重新生成整体覆盖、不合并人工修改 | `write` 检测文件存在即备份 `.bak` 后整体 `Files.move` 替换；生成的页面类是人工扩展入口（step 方法、定制 locator），重扫会整文件抹掉人工改动 | 对已在使用的页面类再次跑 picker 生成 |
| **CG-H1** | HIGH | `codegen/.../web/page/scan/RolePickerCommandEngine.java:360-435` | 区域 iframe 归属用子串匹配（`cUrl.contains(...)`）→误判 | frame 是否属于"选中区域"靠 URL 末段/name 子串包含判定；嵌套/同名 frame 会漏扫或错扫 | 嵌套 iframe 且名称/路径存在子串包含关系 |
| **CG-H2** | HIGH | `codegen/.../web/page/scan/RoleElementPicker.java:430-449` | 导航中断时注入脚本清理依赖 `onFrameNavigated` 自愈 | 未见显式 `removeInitScript`/监听器注销；复杂 SPA 不触发该事件则旧监听悬空、拾取态丢失 | 复杂 SPA 不触发框架导航事件 |

### 1.5 API · 测试 · CI · 报告

| 编号 | 严重度 | 文件:行 | 缺陷 | 致命机理 | 触发条件 |
|---|---|---|---|---|---|
| **CI-C1** | FATAL | `test-automation/.../resources/serenity.conf:44,58` + `.github/workflows/build.yml:72,92-95` | e2e 浏览器测试在 CI 上根本无法启动且被吞 | `headless: false` + `channel: "chrome"`（强制本地 Chrome）；CI 仅 `install --with-deps chromium`。▼ 复核更正：GitHub `ubuntu-latest` 预装 Google Chrome，`channel:"chrome"` 未必失败，真正致命因子是 `headless:false`（托管 runner 无显示器）；`continue-on-error: true` 把全红吞掉→CI 绿 | e2e job 跑 `mvn verify` 无 headless 覆盖 |
| **CI-C2** | FATAL | `.github/workflows/build.yml:35` | 主门禁 `-DskipITs` 跳过全部 IT | 所有 `@Suite` IT（真实浏览器/API E2E）被跳过；CI 的"回归"实际只跑 unit+静态扫描+CVE，**浏览器回归在 CI 上等于 0 执行** | 每次 push/PR 的主门禁 |
| **CI-C3** | CRITICAL | `features/web/login_dbb.feature:5` 与 `login_dbb1.feature:5` + `LoginSteps.java:267-269,220` + `HomeSteps.java:46-48` | 同名 Scenario + 同 sessionKey 串扰 | 两文件都含同名 Scenario 且 `@regression @web @login`，默认 `not @skip` 同时执行；`sessionKey=env+"_"+username` 共用同一 storageState 文件互相覆盖/读取→登录态串扰、Serenity 按 scenario 名聚合报告错合并 | 默认全量跑或并行 |
| **CI-C4** | CRITICAL | `CucumberTestRunnerIT.java:20` + 4 个专用 Runner | 同一 feature 被双跑 | 主 Runner `@SelectClasspathResource("features")` 跑全部；4 个专用 Runner 各自重选子集（如 `concurrent_logon_dbb.feature`）→整仓 `mvn verify` 跑两遍：双份浏览器启动 + 双份 session 写 + 报告互相覆盖 | 整仓 verify（CI e2e） |
| **CI-C5** | HIGH | 4 个专用 Runner（`CucumberConcurrentLogonRunnerIT.java:50` 等） | 专用 Runner 重新硬编码 `cucumber.filter.tags`，与单一事实来源冲突 | 主 Runner 已删除硬编码，改由 failsafe `-Dcucumber.filter.tags` 驱动；这 4 个仍 `@ConfigurationParameter(key="cucumber.filter.tags", value=...)`→`-Dtags` 对它们不生效，CI 分档（smoke/regression）静默失效 | 用 `-Dtags` 做分档编排 |
| **CI-M1** | MEDIUM | `serenity.properties:183` | `serenity.report.url=${JENKINS_URL}job/...` 在 GitHub Actions 下未定义 | `JENKINS_URL` 在 GHA 环境为空，"View Full Report" 链接失效 | GHA 环境 |

> 安全脱敏链路、报告聚合（`SummaryReportGenerator.java` 2121 行仅"大"非"纠缠可变状态"、有 golden 测试守护）、持久化（PreparedStatement+try-with-resources）经逐文件核实为**健康**，不列入缺陷。

---

## 2. 修复计划列表（按优先级）

> 工作量单位：人日。依赖标注前置项。验收标准以"实跑/单测"为准，杜绝"看起来修好"。

### P0 — 致命，须在开启并行 / 启用 CI e2e 前修完

| 编号 | 关联问题 | 修复方案 | 工作量 | 依赖 | 验收标准 |
|---|---|---|---|---|---|
| **P0-1** | WEB-F1 | `PageObjectFactory` 默认生命周期由 `SINGLETON` 改为 `THREAD_ISOLATED`（每线程独立实例），保留 `ownerThread` 守卫 | 1 | 无 | 并行场景各自拿到隔离页面实例；无任何 `cross-thread access denied` |
| ~~**P0-2**~~ ▼ | WEB-F2 | **已按 §0.1 复核降级为 P2 级（可选/暂缓）**：单 JVM 模型下「缓存优先 + per-JVM 单飞 + ConcurrencyGate」已覆盖，跨进程文件锁无收益；仅当未来启用 `forkCount>1` 时再补 `FileChannel.tryLock` 哨兵文件 | 暂缓 | — | 仅在多 JVM 场景验收 |
| **P0-3** | RT-F1 | 将 `MonitorHandler.handle` 的 `waitForResponse`/body 读整体移出事件线程，改 `CompletableFuture` 链，不在事件线程 `get()` 阻塞 | 2.5 | 无 | 事件线程零阻塞；单 context 高 API 密度下路由不再串行化/级联超时 |
| **P0-4** | RT-F2 | `FileStoreMonitorCallback.onResponse` 改为投递到专用异步 executor（与 DB sink 同机制），修正 Javadoc | 1 | P0-3 | 事件线程无同步磁盘写；高 API 量下吞吐不受写盘拖累 |
| **P0-5** | CG-F1 | `for (... javaPickBySig.values())`（:133,195,702）与无锁快照 `new ArrayList<>(javaPickBySig.values())`（:483,525,630）共 6 处包进 `synchronized (javaPickBySig)` | 0.5 | 无 | 多页/多 Context 同时录制无 `ConcurrentModificationException` |
| **P0-6** | CI-C1 | serenity.conf 增加 CI profile（`headless:true`、去掉 `channel:"chrome"` 改用 Playwright chromium）；e2e job 加一个真实启动浏览器的冒烟 feature；稳定前保留 `continue-on-error`，稳定后移除并设为 required | 1.5 | 无 | e2e job 在 ubuntu 上跑通至少一个真实浏览器 scenario 且绿 |
| **P0-7** | CI-C2 | 主门禁保留 `-DskipITs`（无 SIT 凭据），但 **e2e job 必须可独立 required**；或拆分"静态门禁"与"浏览器门禁"两条流水线 | 0.5 | P0-6 | 浏览器回归在 CI 上有可验证的绿灯（非被吞的红） |
| **P0-8** | CORE-C1/CORE-C2 | `SanitizingMessageConverter` 的脱敏入口由 `sanitizeFreeText` 改为与异常栈一致的 `sanitizeLine`；异常时统一 `[LOG SUPPRESSED]` 抑制而非返回原文 | 0.5 | 无 | 单测：`"Login failed: password=s3cr3t"`→掩码；脱敏器异常→抑制不泄露 |
| **P0-9** | CORE-C5 | `looksLikeCiphertext` 收窄：仅当显式 `ENC(...)` 前缀（或独立标记）才尝试解密；或严格模式失败时 `warn+保留原文` 而非抛 `IllegalStateException` | 0.5 | 无 | 一个普通长 base64 配置值不再导致框架启动崩溃 |

### P1 — 严重，并行/录制/隔离上线前修完

| 编号 | 关联问题 | 修复方案 | 工作量 | 依赖 | 验收标准 |
|---|---|---|---|---|---|
| **P1-1** ✅ | WEB-C1 | **已修复（2026-09-21）**：`CONTEXT_LOCK`/`PAGE_LOCK` 由进程级 `static final` 单例改为 **per-thread**（复用 `BROWSER_LOCK_KEY` 同款 `ContextKey` 机制；`LifecycleLockMediator` 内一处改动，`ContextRegistryImpl`/`PageRegistryImpl` 调用点零改动）。未采用 `ConcurrentHashMap<ContextKey, ReentrantLock>`：资源本就每线程独立，按"线程"取锁即可，无需按 Context 建表（无泄漏面与淘汰问题） | 1.5 | 无 | ✅ **达标（实跑）**：`-Pparallel -Dparallelism=16` 下 8 个 scenario 的浏览器启动由"跨 24~38s 逐个排队"变为 **0.61s 内同时启动**；IT 墙钟 **~57s → 24.97s**；每线程 `CLEANUP→CTX-CLOSE` 由最长 **38s**（末尾统一爆发）回到 **0.27~0.82s**（"哪个线程执行完就关它自己的"恢复即时）；全量护盾 **774/0/0/0**，新增 3 例 per-thread 隔离守护 |
| **P1-2** | WEB-C2 | `seenStoryNames` 改 `ConcurrentHashMap.newKeySet()`；`discoveryPhaseCompleted` 用 `AtomicBoolean` + 单例同步 | 0.5 | 无 | 多 Story 并发启动无 HashMap 损坏、无重复清理 |
| **P1-3** | CORE-C3 | `LanguageState` 收敛为用例级（ScenarioContext 绑定），去掉进程级 `globalLang` 可读共享；默认语言显式注入而非回退全局 | 1 | 无 | 并行、部分场景未 `setLanguage` 时不再读到别处语言 |
| **P1-4** | CORE-C4 | 监控落盘降级策略：队列满时拒绝新任务并返回失败信号（而非静默丢弃）；或明确"丢弃即告警并计入失败指标" | 1 | 无 | 高负载下监控丢失可被观测/告警，不静默 |
| **P1-5** | CORE-C6 | `sanitizeJson` 解析失败路径对 `{`/`"` 做 `normalizeKey` 预清洗；或统一走与主路径相同的键值识别 | 0.5 | 无 | 截断/畸形 JSON 体中的 `password=` 值仍被掩码 |
| **P1-6** | CORE-C7 | 复核 `FrameworkHooks.@After` 与软断言聚合先后；将 `clearForCurrentThread` 移出 `@After`，交由 `StepFailureAggregator` 统一收口；实跑确认无假绿 | 0.5 | 无 | 未显式 `assertAll()` 的软断言场景能正确判红 |
| **P1-7** | RT-C1 | `fallbackResponse` 返回 null 时必须记录一次"未断言"失败（或 fail-fast），杜绝 fail-open 假绿 | 1 | P0-3 | 后端 5xx/响应未捕获时该 API 必产生失败标志 |
| **P1-8** | RT-C2 | 监控落库超硬上限/重试耗尽时升级为测试失败信号（而非仅日志） | 1 | P0-4 | 数据丢失场景 CI 可见失败，而非 PASS |
| **P1-9** | RT-C3 | `FileStoreMonitorCallback` 的 scenario 状态按线程/Context 隔离，去掉全局 `currentScenarioKey` | 1 | 无 | 并行开启文件存储时跨场景目录/序号不错乱 |
| **P1-10** | RT-C4 | 共享 Context 模式下 `resetCurrent()` 仅清"已完成" Scenario 的标志，运行中 Scenario 的采集/断言标志受保护 | 1.5 | 无 | 共享 Context 下快速连续 Scenario 不互相清空断言标志 |
| **P1-11** | CG-C1 | `_pageClass` 缺失时用页面 URL/title 兜底归类，禁止退化到空串共享键 | 0.5 | 无 | 弹窗/about:blank 空窗期内两页同名元素不互相覆盖 |
| **P1-12** | CG-C2 | `pageNames` 取首个键前判空；或持有显式"当前主页面"引用而非 `iterator().next()` | 0.5 | 无 | 唯一被跟踪页被关闭后立刻点开始/停止不抛 NSE |
| **P1-13** | CG-C3 | `cmdStop/cmdAbort/cmdDone/cmdScan` 补偿 `END_REGION_SELECT_JS` 清理；统一在 `stop(page)` 内收尾 | 0.5 | 无 | 任意退出区域点选态的路径均移除蓝框与监听器 |
| **P1-14** | CG-C4 | 代码生成改为"合并而非整体覆盖"：解析已有文件保留人工 step/定制 locator，仅增量更新拾取元素；`.bak` 仅作兜底 | 2 | 无 | 重扫已在使用的页面类不丢失人工改动（diff 可验证） |
| **P1-15** | CI-C3 | 删除 `login_dbb1.feature`/`baidu1.feature` 等近重复文件，或将差异合并回主文件；统一 `sessionKey` 命名避免同 key 冲突 | 0.5 | 无 | 默认 `not @skip` 跑时无同名 Scenario 串扰、报告不错合并 |
| **P1-16** | CI-C4 | 4 个专用 Runner 改为 `@Cucumber` 标签筛选（与主 Runner 共用 `@SelectClasspathResource("features")`），禁止重复选取子集 | 0.5 | 无 | 整仓 `mvn verify` 每个 feature 仅跑一次 |

### P2 — 高/中，健壮性打磨

| 编号 | 关联问题 | 修复方案 | 工作量 | 依赖 | 验收标准 |
|---|---|---|---|---|---|
| **P2-1** | CG-H1 | 区域 iframe 归属改用精确匹配（全 URL 相等 / 显式 data-attribute 标记），弃子串包含 | 1 | 无 | 嵌套/同名 frame 不被漏扫或错扫 |
| **P2-2** | CG-H2 | 增加显式 `removeInitScript`/监听器注销路径，降低对 `onFrameNavigated` 自愈的依赖 | 1 | 无 | 复杂 SPA 不触发导航事件时拾取态不悬空 |
| **P2-3** | CI-C5 | 4 个专用 Runner 删除 `@ConfigurationParameter(cucumber.filter.tags)`，统一收口到 failsafe `-Dcucumber.filter.tags` | 0.5 | P1-16 | `-Dtags` 做 smoke/regression 分档对全部 Runner 生效 |
| **P2-4** | CI-M1 | `serenity.report.url` 增加 GHA 环境占位（如 `${GITHUB_SERVER_URL}${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}`） | 0.5 | 无 | GHA 下"View Full Report"链接可用 |

---

## 3. 执行顺序建议（强依赖，勿跳步）

```
Wave 1 安全止血 + CI 开窍（P0-6/7/8/9 + P0-1 先不做亦可）
   └─ CI e2e 能真跑、脱敏最后一公里闭合、启动不被 base64 误伤
Wave 2 并行前置（P0-1 WEB-F1、P1-1/2、P1-3）  ← 不开并行不要做这些之外的并行改造（原 P0-2 已按 §0.1 复核降级）
   └─ 确认并行模型可用：每线程隔离页面、锁按 Context、语言用例级（跨 JVM 不互踢仅在启用多 JVM 时相关）
Wave 3 录制器并发 + route 事件线程（P0-3/4/5、P1-7~13）
   └─ 录制器不再 CME、route 不再阻塞事件线程、监控不再 fail-open/静默丢
Wave 4 测试隔离与 CI 一致性（P1-14~16、P2-3/4）
```

**关键原则**：
- **P0-1（WEB-F1）完成前不要开并行**——默认 `SINGLETON` 页面实例 + per-instance `ownerThread` 守卫在并行下会必抛 `cross-thread access denied`。原 P0-2（WEB-F2）经 §0.1 复核仅在 `forkCount>1` 多 JVM 下成立，单 JVM 并行模型无需，已降级。
- 上一轮评审的"SEC 类明文凭据/API 日志绕过脱敏/MonitorHandler 吞异常"三项 P0 经本轮核实已修复，**不要再按旧清单整改**，避免无效返工。
- `SummaryReportGenerator`（2121 行）、`WebFrameworkConfig`（1740 行）等大文件**仅是可维护性问题，非运行时致命缺陷**，不要在"降行数"上投入；真正的并发/泄漏风险在上面的具体行号，按 P0/P1 改。

---

## 4. 修复落地记录（2026-09-19 起）

> 按 Wave 3 顺序逐项落地并标记完成，避免与 §2 计划列表脱节。每项落地以"单测 + 真浏览器压测"双验证为准。

| 编号 | 关联问题 | 状态 | 落地日期 | 要点 |
|---|---|---|---|---|
| **P0-3** ✅ | RT-F1 | **已落地** | 2026-09-19 | 事件线程零阻塞（无延迟即时 `safeResume`）+ 二次 resume 幂等收敛（根除 `Cannot find parent object request@...` 级联污染）+ 读体池兜底默认 2→16 对齐；真浏览器 50 并发压测 4 例全绿。技术细节见 `06_route模块.md` §5.6.3 |

| **P0-4** ✅ | RT-F2 | **已落地（代码核查）** | 2026-09-19 | `FileStoreMonitorCallback` 已实现 `WRITE_EXECUTOR` 单线程异步写盘执行器（`onResponse` 投递 `writeFile`，队列饱和计数丢弃并告警），事件线程零同步磁盘 IO；javadoc 已标注 P0-4 |
| **P1-7** ✅ | RT-C1 | **已落地** | 2026-09-19 | `recordUnavailable` 落 status=0 降级快照后 `signalFailFast`，杜绝观测不可用 fail-open 假绿；新增 `MonitorHandlerFailOpenTest` 固化（观测不可用 → `hasAssertionFailures=true`） |
| **P1-9** ✅ | RT-C3 | **已落地** | 2026-09-19 | FileStore 落盘按 `scenarioKey` 隔离（`scenarioStates` 映射 + 每场景独立计数器/子目录），去掉全局 `counters`/`currentScenarioKey` 与 scenario 切换时的 `counters.clear()`，根除并行 scenario 串号/串目录；新增 `FileStoreMonitorCallbackScenarioIsolationTest`（3 例）。验证：route 模块 57 例全绿 |

**下一阶段（Wave 3 续）**：
- **P1-8**（RT-C2）：监控落库超硬上限 / 重试耗尽时升级为测试失败信号（而非仅日志）—— 用户本地有 MySQL，可端到端验证（依赖 P0-4 已落地）
- **P1-10**（RT-C4）：共享 Context 模式下 `resetCurrent()` 仅清"已完成" Scenario 标志，运行中 Scenario 的采集 / 断言标志受保护
- **P0-5**（CG-F1）：录制器 `javaPickBySig` 6 处无锁快照包进 `synchronized`
- **P1-11~13**（CG）：录制器 `pageNames`/`_pageClass`/`cmdStop` 等补偿
