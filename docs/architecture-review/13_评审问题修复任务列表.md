# 评审问题修复任务列表

> 来源：`docs/architecture-review/01`~`12` 共 12 份评审文档。
> 生成方式：4 个并行代码探索子代理逐文件提取"问题/风险/待办"条目，并核查工作区当前代码现状（基线 `e11a847`）。
> 判定口径：
> - **合理**：确为真实技术债/缺陷/风险。
> - **已修复/误判**：代码现状已覆盖或文档基线过时（不再需要修）。
> - **处置**：`直接修`=框架内低风险可独立完成；`待决策`=涉及 CI 策略、密钥管理、架构大重构、需压测或用户拍板。

## 〇、进度快照（2026-09-12 · 代码核查结论，非猜测）

> 本快照基于工作区相对**新评审基线 `e11a847`** 的未提交改动，**逐文件实际核查**后得出（非文件名推测）。
> 核查纠正了初版猜测：① `WebFrameworkConfig.java` 实际改的是共享 Browser 默认开启（对应 `W-7`，非 `W-9`）；
> ② `serenity.conf` 改的是凭据明文→环境变量（对应 `D-1/D-2/D-3`，非 `B-1/B-2`）；③ `serenity.properties` 改的是截图策略（对应 `W-2`，非 `B-1/B-2`）。
> §二~§四 现状列已据本快照同步更新；本批改动**尚未 commit**。全护盾验证：曾报 `ConcurrentContextNoDeadlockTest` 2 例失败并被误判为"本机 headless Chromium 并发不稳定"；后续根因定位实为 W-7 将 `PLAYWRIGHT_SHARED_BROWSER_ENABLED` 默认置 true → Playwright for Java 对象非线程安全、并发共享单 Browser 损坏客户端对象注册表（`__adopt__`/`pausedStateChanged`），与 W-4/W-9/W-10 无关。已将默认回退 false（共享模式回归 opt-in），全护盾 **642 例全绿**；并发线程名 `dbb-ctx-` 已按评审意见改为 `ctx-`（`ConcurrentContextExecutor` 定义 + `ConcurrentContextExecutorTest.threadNamesArePoolScoped` 断言同步，10 例全绿）。

> **2026-09-16 全量重审**：6 个并行代码探索子代理对照当前代码逐文件核实本表 §二~§四 各「现状」列，结论见下方单元格修订 + 底部「仍未启动」修订。重大纠偏：`L-2`(=C-5)、`X-7`(=W-7)、`G-6`、`P-6`、`R-5`、`G-9`、`C-11`、`E-6` 实为**已修复**；`P-4`/`M-4` 实为**未修复**；`R-4`/`G-1` 实为**部分修复**。doc 12 §〇 已同步重审。

### 代码核查结论（证据确凿）
| 合并ID | 级别 | 现状 | 证据 |
|---|---|---|---|
| C-3 | P1 | 已修复 | `SecretValue.java` 默认严格模式抛 `IllegalStateException`，`-Dsecurity.secret.strict=false` 降级 |
| W-7 | P2 | **已闭环：共享 Browser 模式从框架彻底移除**（非仅默认 false） | 配置项 `PLAYWRIGHT_SHARED_BROWSER_ENABLED`、`enableSharedBrowserMode` / `isSharedBrowserMode`、`keyFor` 的 `shared:` 分支全部删除；并发统一为每线程独立 Browser（`keyFor` 恒 `threadId:configId`）。根因：Playwright Java 非线程安全，共享单 Browser 跨线程损坏对象注册表 |
| D-2 | P0 | 已修复 | `serenity.conf` 主密钥注释改为「严禁入库」 |
| D-3 | P0 | 已修复 | `token.security.url` 改 `${?DBB_OTP_URL_Oxx}` 环境变量 |
| W-2 | P0 | 已修复 | `serenity.properties` 截图策略 `AFTER_EACH_STEP→AFTER_FAILING_STEP` + fullpage false |
| CI-1 | P0 | 已修复 | `build.yml` 升 `mvn -B verify` + 新增 `e2e` job 灰度 |
| CI-2 | P0 | 已修复 | `build.yml` 加 `playwright install --with-deps chromium` |
| CI-3 | P0 | 已修复 | `build.yml` 归档 `if-no-files-found: error` |
| CI-4 / A-6 | P0 | 已修复 | `build.yml` 装 OSV 二进制 + 运行 `cve-gate.ps1`（`OSV_CVE_GATE_REQUIRED=true`） |
| CI-5 / E-1 | P0 | 已修复 | `serenity-report-push.yml` 加凭据 grep 扫描 + `.sanitized` 校验 `exit 1` |
| CI-7 | P2 | 已修复 | `doc-drift-check.yml` 改 `pwsh check-doc-drift.ps1` + setup-powershell；**2026-09-20 进一步修复门禁失效**（根 README 于 aa02d27 误删致 `exit 2`）→ 恢复 README + 修正失效 `junit.version` 映射 + 补 `junit.platform.version` 校验，实测 10 项全 OK |
| M-5 | P2 | 已修复 | `docs/` 重建 13 份架构评审文档 |
| D-1 | P0 | 已修复 | `serenity.conf` password 改 `${?DBB_TEST_PASSWORD}` 环境变量注入去明文；`SecretValue` 双格式透明解密（带/不带 `ENC(...)` 的密文均默认解密，用户决策） |
| P-2 | P0 | **已闭环** | `api` 测试覆盖推进：`EntityBuilderTest`(5)`SanitizingPrintStreamTest``SecurityAuditTest``JsonSchemaValidatorTest``BaseStepJsonPathTest``BaseStepSchemaTest``AbstractRestJobRetryTest``AbstractRestJobRetryLoopTest`；`AbstractRestJobTest`（6 例：stripHtmlWrapper/applyResponse 回写/响应回环/静态配置装配）于 2026-09-17 补齐；api `JaCoCo check` 门禁已落地（BUNDLE LINE 下限 0.23 → **2026-09-20 上调至 0.40**，仅升不降）；**2026-09-20 达标闭环**：新增 6 个测试类（HttpStatus / JsonUtils / ApiLogSanitizer / FileReader / EnvironmentUtils / ConfigProvider，共 46 例）+ 2 个测试资材，实测 LINE 覆盖率 **23.8% → 43.1%（792/1836）**，`mvn -pl api verify` 实测通过 |
| C-1 | P0 | 已修复 | `ScenarioContext` 已接入 `FrameworkHooks`(`@Before`→`begin`/`@After`→`end`+`assertUnbound`) 与 `ConcurrentContextExecutor.runOnce`；`TestContextHolder.get()` 切到用例级 `ScenarioContext.current()`（线程键保留为无绑定兜底）|
| D-6 | P1 | 已修复 | 框架 `ScenarioDataNamespace` 落地（每用例唯一数据 `uniqueId` + 命名空间清理 `registerCleanup`，由 `ScenarioContext.end` 机器强制，语义同 `assertUnbound` 保险丝）；`RouteDemoServiceSteps.resetDemoData` 注册「用例结束再重置基线」钩子；route-demo 套件因 reset 幂等 + 回显断言天然并行安全（新增 `ScenarioDataNamespaceTest` 5 例）|
| X-3 | P1 | 已修复 | route 进程级单例改 context 级（R-2 / R-3）：`ApiMonitorOrchestrator.registeredPatterns` 由进程级单例改为 per-`BrowserContext` 去重（并行下不同 context 各自独立注册，消除缺口型跨 context 串扰），`patternToContext` 移除、`deregisterContext` 仅释放该 context；`ApiCaptureManager` 切换探测节流 `lastResolve` 改 per-`BrowserContext`（`lastResolveByContext`），不再跨 context 互相限频（G3 的 per-context `contextStores` 隔离路径不变）；新增 `ApiMonitorOrchestratorTest` 3 例（per-context 独立注册 / 同 context 去重 / 注销仅清本 context）|
| X-1 | P0 | 已修复 | 引擎级并行接线完成（CON-1）：新增 `test-automation/src/test/resources/junit-platform.properties`（`cucumber.execution.parallel.config.strategy=fixed` + `fixed.parallelism=4` + `fixed.max-pool-size=8` + `@isolated` 标签独占 `resource:global` 串行保护机制），`test-automation/pom.xml` 的 `parallel` profile 增强为注入可调并发度旋钮（`-Dparallelism=N -DmaxPoolSize=M`）；默认 `mvn test/verify` 仍严格串行，仅显式 `-Pparallel` 才开启引擎级并行（与 `ConcurrentContextExecutor` 用例内并发互补）。并发度硬上限 X-6/W-6 已可配（`ConcurrentContextOptions.resolvedParallelism` 读 `WebFrameworkConfig.PLAYWRIGHT_CONCURRENT_MAX`）。压测护栏（梯度 4→8→16）须在 nightly/CI 用真实浏览器+后端+内网验证（本地无法跑；2026-09-14 实跑：`-Pparallel` 下 `parallel_logon_dbb` 4 scenario 在 `thread-1~4` 并发生效，本地仅因 SIT 后端 `qualityassurance-amh-gbb-sit...hsbc.com.hk` 不可达，CONFIG_MASTER_KEY 非阻塞）|
| C-6 / M-2 | P1 | 已修复 | `ConfigKeys` 注册表已加重复键 fail-fast 静态校验 + `ConfigKeysRegistryTest`(3/3)；**2026-09-20 全量收敛完成**：Web / API / Monitor 三侧键定义（184 键）全部迁入 `ConfigKeys` 作为唯一事实来源，三侧退化为读取门面（`WebFrameworkConfig` / `ApiFrameworkConfig` / `MonitorConfig`）；新增 `ConfigKeysGoldenTest`（golden 快照）与 `ConfigUnificationTest#facadesMustDelegateToRegistry`（门面逐字委托校验）两道守卫 |
| P-1 | P0 | 已修复 | `SanitizingPrintStream` 已挂接 `AbstractRestJob` 的 `RestAssuredConfig.logConfig`（出口强制脱敏）；`ApiFrameworkConfig.API_REQUEST_RESPONSE_LOGS_ENABLED` 默认改 `false`（安全基线），`ConfigKeys` 键名对齐为 `api.request.response.logging.enabled`（消除死常量） |

### 本批框架设计收敛（2026-09-16 · 设计评审补充，非编号评审项，commit e167d54）

> 本轮为 alert / 文件选择器 / 上传解析的框架设计收敛，与 01~12 文档的编号评审项同源（同一设计评审范畴），但非具体某条编号项；全部改动已随 commit e167d54 入库，未引入新配置键、无硬编码默认目录。
> 设计约束（用户拍板）：不加配置键、不硬编码默认目录；监听器默认不干预 Playwright 默认语义；失败处置交回业务既有方法。

| 改动点 | 现状 | 说明 |
|---|---|---|
| WebFrameworkConfig | 已收敛 | 删除本轮曾新增的 5 个配置键（DIALOG_POLICY / FILE_CHOOSER_ENABLED / _DIR / _GLOB / NAV_TRAIL_MAX），全仓零残留引用 |
| PageInteractionMonitor | 已闭环 | 仅注册 onFrameNavigated（导航轨迹）+ onPopup（未受管弹窗）两类纯诊断；onDialog 不注册（alert 交回 PageInteractions.acceptAlert/dismissAlert 的 onceDialog）；onFileChooser 改为程序化 seam（setFileChooserUploader，注入才注册，无监听则不 setFiles 规避 headless 卡住），无配置/无硬编码；新增 `resetForThread()` 在 `PlaywrightListener.fireAfterScenario` finally 显式清轨迹/弹窗，防 worker 线程复用跨用例堆积 |
| PageInteractions | 已还原 | acceptAlert/dismissAlert 还原为 Playwright onceDialog 实现，alert 处置完全交回业务既有方法 |
| PageElement.uploadFile | 已闭环 | resources（classpath）首选 → 用户指定路径兜底 → 都不存在抛 ElementOperationException（含两端候选路径）；逐文件 Files.exists 校验；跨系统分隔符归一化（classpath 名 `\`→`/`、用户路径按 `File.separatorChar` 归一化）；多文件 varargs → Locator.setInputFiles(Path[]) |
| 测试 | 已补 | PageElementTest 新增 5 例（资源命中 / 用户路径兜底 / 两端皆缺 / 反斜杠资源名 / 用户路径分隔符归一化），共 15 例全绿；PageInteractionMonitorTest 7 例全绿（新增 `resetForThread` 清空导航轨迹+未受管弹窗 / 幂等+null 安全 2 例）；新增测试资源 test-upload/sample.txt |

**当前进度分析（截至 2026-09-16）**：本轮设计收敛已全部落地（含 `resetForThread` 接缝 + 2 例新增测试，待提交），无遗留待办；监听策略、上传解析、跨系统分隔符、重置接缝均闭环，测试全绿。该批工作独立于 §二~§四 的编号评审项，属「监听可观测性 / 上传解析」子系统的设计加固，不新增技术债。**已在 01~12 文档补登对应条目**：归到 doc 03 §2.10「监听可观测性 / 上传解析」，编号 LO-1~LO-3（已闭环），不计入去重后的约 70 项待修 backlog（第 3 步暂不执行，仅记进度）。

### 仍未启动（2026-09-16 全量代码重新核查结论）

> 由 6 个并行代码探索子代理逐文件核查（对照本表 §二~§四 各「现状」列），纠正初版快照偏差：
> - `L-2`(=C-5)、`X-7`(=W-7)、`G-6`、`P-6`、`R-5`、`G-9`、`C-11`、`E-6` 实为**已修复**（初版误标未修复/部分）；
> - `P-4`(无 `SecurityAudit` 类)、`M-4`(空目录/根垃圾仍在) 实为**未修复**（初版标部分过宽）；
> - `R-4`(写库失败计数) 已于 2026-09-17 修复（计数 + 报告尾部红色提示落地）；`G-1` 定位器注入面已于 2026-09-17 修复（byTestId 主属性走 `page.getByTestId`+转义兼容属性，iframe 先已参数化）。两者均移出部分修复清单。

P0（仍开）：无。（`PAR-5`/B-6 已于 2026-09-17 核实为**已落地**——core `RequiresCleanState`/`CleanStateRegistry`/`StateResolver`/`StateResidueException` + test-automation `CleanStateHooks`/`CleanStateRegistryTest` 均存在，「未落地」系 doc 未同步，已纠正。注：`RUN-3`/A-2 maven-install 污染 .m2 已于 2026-09-17 移入 `sca` profile 修复，移出仍开清单。）
P0（部分）：无。（`PAR-7`/P-2 已于 2026-09-17 闭环，见 P-2 行与「净效果」新增条目。）
P1（仍开）：无（`C-7`/`M-3` 的“整类策略链拆分”作为独立后续项另行排期，核心缺陷已消除）。
P1（部分）：无（`C-6`/`M-2` 已于 **2026-09-20 全量收敛闭环**——见本文件 §〇 快照与 §三 C-6 行）。（注：`G-1`/`R-4`/`P-4` 已于 2026-09-17 修复，移出本清单。）
P2：**无仍开项——全部条目已修复或决策闭环**。`G-5` 已决策**不拆分**（论证见 doc04 G-5 行：Java 枚举无法跨文件拆分 / 拆分将丢失 `values()` 并需人工聚合 = 重新引入「漏登记」失败模式 / 文件长度是 132 个声明式键的映射），其陈述危害改由 `ConfigUnificationTest#configKeysAreGloballyUnique` 关闭；`R-7` 已决策**保留 `route` 不重命名**（`route` 即 Playwright 平台词汇、本仓无「用例路由」构造、改名牵涉 61 个 java/模块/pom/标签/示例/文档而收益为 0），危害由 `route/package-info.java` 术语定位说明关闭。其余 P2 条目已全部修复或定性（`A-7`/`C-12`/`M-4` 空目录/根垃圾、`A-9` 依赖冗余、`G-7`/`R-6` 产物策略/入口决策表、`E-7` ZIP 白名单、`B-7`/`B-8` 标签/文档、`D-8`/`D-9`/`D-10` ssl 默认/夹具标注/残留断言、`C-8` 语言态隔离、`P-7` 响应 schema 校验）。
P2（部分/已修复）：`E-5`(reportStep 占位→intentional no-op)、`W-8`(ThreadLocal 收口)、`M-5`(docs 重建)、`W-7`/`X-7`(共享 Browser 移除)。

> **验证期间附带修复（2026-09-17，非评审清单项）**：`mvn verify` 此前**必然失败**，汇总报告从未真正产出（E-3/E-6 的交付物实际不可用）。两处既有缺陷：
> ① 汇总报告 exec 用 `classpathScope=compile`，而本模块 `serenity-model` 显式声明为 `<scope>test</scope>`（ENG-P0-2），Maven「最近声明优先」使其不再以 compile 作用域传递 → 运行期 `NoClassDefFoundError: net.thucydides.model.environment.SystemEnvironmentVariables`；改为 `classpathScope=test`。
> ② `route` 的 SPI 注册文件 FQCN 写错（`…framework.web.route.monitor.MonitorFailureReportWriterSink`，而实现已随 ENG-P0-2 迁至 `…framework.route.monitor.*`）→ `ServiceLoader` 抛 `Provider … not found`（`Error` 不被 `catch (Exception)` 捕获，直接中断 exec）；修正 FQCN。
> 修正后 `mvn verify` 全链路 GREEN（`Tests run: 124, Skipped: 124` 的 Runner 亦正常解析 124 个 scenario），汇总报告与 `trend-history` 正常产出。
>
> **附带修复 2（checkstyle 计数门禁 + 配置镜像漂移，2026-09-17）**：
> ① **CI 的 checkstyle「只减不增」门禁此前失败**（`current 1325 > baseline 1321`，即 build.yml 里 `grep -c '<error'` 的计数比对）。核对确认**非本次引入**——新增的 `JsonSchemaValidator.java`、`route/package-info.java` 的 checkstyle 文件块均为**空**（0 违规）；超额来自 ENG-P0-2 包搬迁导致的**违规重新归属**（`web/page/base/* → web/page/engine/*`）与会话新建文件。已按「新代码零容忍」修复会话新建/搬迁文件中的 10 条违规（`MonitorDataLossReporter` NeedBraces×1；`SerenityBasePage`/`SerenityBasePageImpl` UnusedImports 各×1；`RecordingPageProxy` EmptyCatchBlock×1 + NeedBraces×2；`SerenityRecorder` NeedBraces×4）。现 **current=1315 ≤ baseline=1321**，门禁通过且留 6 条余量（`failOnViolation=false` 使本地 verify 不卡，故此前未被发现）。
> ② **配置镜像默认值漂移 2 处（真实缺陷，已被新守卫抓到）**：`ConfigKeys` 注册表与真实枚举不一致 —— `serenity.screenshot.strategy` 注册表 `AFTER_FAILING_STEP` / 真实 `AFTER_EACH_STEP`；`playwright.screenshot.fullpage` 注册表 `false` / 真实 `true`。两者都会误导审计与文档生成者。已校正，并新增守卫 `ConfigUnificationTest#mirrorConfigKeysMustAgreeOnDefaults`（逐条比对 `MonitorConfig`/`ConfigKeys` 与 Web/API 枚举的默认值——`MonitorConfig` javadoc 本就承诺「键名与默认值与 FrameworkConfig 逐字一致」）。
>
> **SpotBugs 门禁治理（2026-09-17 发现，同日闭环）**：`mvn verify` 的 **SpotBugs 门禁**在 `framework-reporting` 失败（26 项）。`framework-core` 的 1 项（`ShutdownCoordinator$Task` 定义了 `compareTo` 却用 `Object.equals`，`EQ_COMPARETO_USE_OBJECT_EQUALS`）已修：**移除被误用的 `Comparable`，改为注册处显式 `Comparator`**（任务顺序只由 `order` 决定，身份语义仍是对象同一性，二者不应共用 `Comparable` 契约）。reporting 的 26 项构成：生产代码 `RunSummary` 的 `EI_EXPOSE_REP/REP2`（4）、`SummaryReportGenerator` 的 `CT_CONSTRUCTOR_THROW`（2）、`NP_NULL_ON_SOME_PATH_FROM_RETURN_VALUE`（3）、`REC_CATCH_EXCEPTION`（2）、`SF_SWITCH_NO_DEFAULT`（1）、`VA_FORMAT_STRING_USES_NEWLINE`（2）、`URF_UNREAD_FIELD`（1），以及**测试类**若干（`File.mkdirs()` 返回值忽略等）。`reporting/spotbugs-exclude.xml` 当前是空基线（模块原本 0 项），故按该文件既定口径「新代码零容忍、存量走基线抑制」分开处置：生产代码优先真修，测试类与已接受模式入基线。
>
> **闭环结果（2026-09-17）**：SpotBugs 门禁链**全模块打通**，`mvn verify -DskipITs`（CI 形状）**8 模块全部 SUCCESS**。
> - `framework-core` 1 项 → **真修**（`ShutdownCoordinator$Task` 误用 `Comparable` → 注册处显式 `Comparator`）。
> - `framework-reporting` 26 项 → **全部真修，零排除**：`RunSummary` 防御性拷贝（`EI_EXPOSE_REP/REP2`×4）、`SummaryReportGenerator` 声明 `final`（`CT_CONSTRUCTOR_THROW`×2）、3 处 `getFileName()/getParent()` 判空（`NP`×3）、2 处 catch 收窄（`REC`×2）、`switch` 补 `default`、2 处 format 串换行移出（`VA_FORMAT`×2）、删除未读字段 `scenarioId`（`URF`）、9 处测试 `mkdirs()` 返回值处理 + 2 处测试 `getParent()` 判空。
> - `framework-web` 13 项 → **10 真修 + 3 设计内保留**。真修：`AxeRunResult` 防御性拷贝×3、`DownloadRegistry` 改 lazy-holder 单例（`MS_EXPOSE_REP`）、`getLastDownloadFileName` 判空、2 处 `finally` 冗余判空移除、3 处测试修复（其中一处**补上了"反斜杠从未被断言"的漏断言**——原断言两侧是同一表达式）。保留并注明理由：`BasePage.attachManagedPage`/`SerenityBasePageImpl` 的 `EI_EXPOSE_REP2`（组合式受管 Page/BasePage 注入即本设计本身）、`SerenityPageRecorder.recorder()` 的 `EI_EXPOSE_REP`（框架内部 seam，测试亦依赖其"每页独立"语义）。
> - `framework-codegen` 50 项 → **重新登记**：RolePicker 族随 G-2/D6-1 从 web 物理迁入时，web 基线的对应 suppress 被当"过时"删除却**未在本模块重登**，使既有债务以"新违规"暴露；已按原 owner/SLA 重新安家，并标注 20 条 `DE_MIGHT_IGNORE` 建议单列清理。
> - `framework-route` 95 项 → **1 真修 + 重新冻结**：`RouteRule.hashCodeCached/cachedHashCode` 是**真实可见性竞态**（RouteRule 作 `ConcurrentHashMap` 的 key，hash 缓存字段非 volatile）→ 已改 `volatile`；其余按 T1-3 基线机制重新冻结（该文件原仅 9 条，模块已远超其规模），并**显式标注** `AT_*` 并发族（配置期写/派发期读）需专项复核并发模型。
> - `test-automation` 18 项 → **3 真修 + 10 登记 + 8 条陈旧条目清理**：真修（B-4 遗留的**死方法** `assertNeverCaptured` 3 参重载——由本轮自查发现、测试 `pattern` 改 static 常量、`registerRejectsNull` 改 `assertThrows`）；登记 10 条测试侧可接受模式（故意传 null 的断言库用例、H2 空口令、并发用例持锁 sleep 等）；清理 8 条无效冻结（对应违规已真修或类已更名）。
>
> **净效果**：`mvn verify` 由"必然失败"变为 **CI 形状全绿**（8 模块 SUCCESS；**1069** 项单测〔含 route 资源生命周期 4 + 重试策略 4、web 并发闸门 3 + Browser 键不变式 3 + trace 保留 6 + trace 命名 5 + PageObject 作用域 5、本会话新增 RouteRule 能力可见性 5 + CapturedApiCall 截断 6 + AbstractRestJob 6，此前为 1027〕；**api 模块 JaCoCo LINE 门禁（下限 0.23，实测 23.5%，目标 0.40）已生效**（2026-09-20：补测后实测 43.1%，下限已上调至 0.40）；各模块 SpotBugs `0 bug`；Checkstyle 达标）。
>
> **Route 并发 / 线程 / 资源清理专项评审（2026-09-17，按用户关切追加）**：详见 doc06 §5.6。核对结论：**竞态** 真修 3 处（`PerContextEngine.contextId` 用 identityHashCode 作 per-context 调度器 key → 碰撞致跟踪失真，改「序号 + hash」；`CapturedApiCall.bodyTruncated/originalBodyBytes` 发布后可变 → 改 volatile 并**同步清理**对应基线条目；`RouteRule` hashCode 缓存 → 已改 volatile），`NN_NAKED_NOTIFY` 判定为**误报**并记录理由（notify 与「检查+wait」同锁互斥，无漏唤醒）；**线程释放**链路完整（5 处 ShutdownCoordinator 登记 + AsyncPool 全池兜底 + context 清理链），但发现 DELAY 任务被「保留到原定延时后才执行」→ 池与线程**延后释放且移出跟踪表后不可观测**，已改为「趁池 RUNNING 取出待发任务 → 立即执行 → 再关池」（全局与 per-context 同口径），并修掉拆除后仍新建 per-context 引擎的复活路径；**资源即时清理**上限齐备（ResponseStore 四组上限 / MonitorFailureCollector 500 LRU / DISPATCHED_ROUTES 500 / 流量日志 100MB），弱键使用得当。**新增守卫** `RouteEngineResourceLifecycleTest`（4 例：唯一性 + 释放归零 + 关闭不丢 DELAY 任务 + 排水器异常隔离），把「是否即时释放」从靠审查变为靠断言。
>
> **Route 评审复核（第 2 轮，2026-09-17，按用户「1,2,3」指示）**：① 纠正 doc12/doc13 中 `PAR-5`(B-6) 的过期状态——`@RequiresCleanState` 机制**已完整落地**（core `RequiresCleanState`/`CleanStateRegistry`/`StateResolver`/`StateResidueException`；test-automation `CleanStateHooks`/`CleanStateRegistryTest`），「未落地」系文档未同步；② 三项零风险真修并**同步清理基线条目**：`PerContextEngine.state` 由 `public volatile` 收为 private + 只读 `state()`（PA_PUBLIC_PRIMITIVE_ATTRIBUTE 移除）、`DelayScheduler.scheduleDeferred` 去 `catch NPE` 改为显式 null/`isShutdown` 判定 + 只捕获 `RejectedExecutionException`（DCN_NULLPOINTER_EXCEPTION 移除）、`ApiCaptureContext` 三处「谓词状态变更移入 `completionLock` 监视器」+ `signalFailFast` 按调用方既有契约补齐 `hasAssertionFailures` 标志（NN_NAKED_NOTIFY 移除）、`ApiMonitorConfig.setFeatures` 去冗余 `synchronized`（UG_SYNC_SET_UNSYNC_GET 移除）；实证：route SpotBugs `BugInstance size is 0`、12/12 测试通过；③ **web 模块并发/资源首轮评审**已执行（详见 doc03 §5.7）：修 2 处真缺陷（`ConcurrentContextExecutor` 的执行窗口标记移入 try，避免池构造异常致清理门控永久卡死 → 孤儿浏览器进程；`BrowserOverrideManager.globalOverrideMap` 双轨残留会「复活」上一 scenario 的浏览器覆盖 → 收敛单轨），其余清单（`ConcurrencyGate` 淘汰窗口缺口、浏览器生命周期纵深、静态实例表复合操作、trace 落盘完整性、跨模块 bodyReadScheduler 共享）列为下一轮。
>
> **web 第二轮（2026-09-17，承接首轮清单第 1 项）**：`ConcurrencyGate` 闸门淘汰的**串行化缺口已真修**——淘汰判据「许可是否全空闲」无法区分"刚取到、尚未占许可"的闸门，窗口内淘汰会让同一身份键**并发进入**（SSO 分区串行化破裂），且 `release(key)` 按 key 重新查表会把许可**错记**到被重建的条目上。改法：引入「在途持有计数」`GateEntry`，取用+计数递增在同一次 `compute` 内完成、释放在 `computeIfPresent` 内「归还 + 递减，归零即移除」，并**整体删除 `MAX_GATES=4096` + 淘汰机制**（条目随最后一个持有者释放即移除 → Map 大小 == 在途身份数，天然有界）；新增 3 例守卫（条目仅在持有期存在 / 5000 身份不残留 / 持有期 churn 下仍串行化），web 测试 268 → 271。另纠正 `LifecycleLockMediator` 类文档「Context/Page 锁始终 per-thread」的**文档与实现不符**（实为进程级 `static final` 锁，并行下 Context/Page 创建/关闭全局串行 = 吞吐税），核对确认全部调用点均操作本线程数据、跨线程关闭由 `isConcurrentModeActive()` 断言拦截，故「改 per-thread」列为**待决策项**并已写入该类 javadoc。剩余：浏览器生命周期纵深复核、静态实例表复合操作、trace 落盘完整性、跨模块 `bodyReadScheduler` 共享。
>
> **按用户 4 项指示续做（2026-09-17）**：① **共享 Browser 收口**（#1）——确认「共享 Browser 模式」已彻底移除（Playwright for Java 非线程安全：`PLAYWRIGHT_SHARED_BROWSER_ENABLED`/`enableSharedBrowserMode`/`keyFor` 的 `shared:` 分支均不存在），清理残留**误导痕迹**：`WebFrameworkConfig` 中该配置项的**无主 javadoc 块**、`BrowserRestartImpl` 的 `[shared-browser]` 日志前缀（→`[browser-rebuild]`）、6 处过期注释；新增 `BrowserInstanceKeyInvariantTest`（键必须含 threadId、不含 shared、跨线程必不同）把不变式固化为断言。② **MonitorHandler body 读取重试治理**（#4，详见 doc06 §5.6.1）——单线程共享调度器 → 可配线程池（默认 4）；尝试数/间隔/**等待预算上限**全可配（默认 3/50ms/**30s**）；**用例结束即取消**在途重试（登记表下沉 core `RouteContextState`，由 `stopContextEngine` 统一取消）→ 等待方立即走兜底、不再"用例跑完等 timeout"；重试链受 `deadline` 约束不产生残链。首版曾因 `RouteEngine → MonitorHandler` 被 ArchUnit `routeCoreMustNotDependOnRouteHandler` **拦下**，已改为「能力下沉 core + handler 只登记」——架构门禁实际生效。新增 `MonitorHandlerRetryPolicyTest`（4 例）、`BrowserInstanceKeyInvariantTest`（3 例）。
>
> **用户 #2 已完成（PageObjectFactory 专项，2026-09-17，详见 doc03 §5.8）**：先确认 **Playwright for Java 无官方 PageFactory**（命令式 `Page`/`Locator` 模型；Selenium 的 `PageFactory` 属 WebDriver 体系，不适用于 Playwright 对象）→ 走"优化现有实现"。修 4 处：① `getPage` 的**检查-创建竞态**（读→建→存回）改为 `putIfAbsent` 竞争发布（落败者返回已发布者），刻意不用 `computeIfAbsent` 以免用户钩子在桶锁内触发 CHM 递归更新；② 请求作用域键由**线程名**改为**用例级身份**（`ScenarioContext.currentScenarioId()`，回退线程），并在用例收尾调用 `endRequestScope()` 回收（否则实例随用例数累积并持有 Page/Context 引用）；③ 供应商路径漏计创建次数 → 统一计数；④ 构造器内部异常被裸抛丢根因 → 显式捕获 `InvocationTargetException` 并带 cause 报错。新增 `PageObjectFactoryScopeTest` 5 例（并发同实例 / 作用域随用例 / 收尾回收 / 原型新建 / 线程隔离）。web 285 → **290** 例。
>
> **用户 #3 已完成（trace 分段 + 落盘治理，2026-09-17，详见 doc03 §5.7）**：核实「trace 区间 = Context 存活期 ≠ case 执行期」→ `feature` 模式下一个 trace 覆盖多个 case 且文件名/报告名实不符、scenarioId 可能退化为 `unknown`。**方案 A**：用 Playwright 原生 `startChunk()/stopChunk(path)` 在**用例边界**切段，每用例一个 `trace-<id>-<start>-<end>-PASS|FAIL.zip`（报告含覆盖时间窗），scenarioId 显式传参，超时/失败**删残片**；接线于 context create/close + `cleanupForScenario`（共同收口）+ 监听器（开段/带结果收段，幂等）；逃生开关可回退整段。**落盘治理**：`ArtifactRetention`（总磁盘/文件数/保留期，`playwright.artifacts.retention.*`），**只清理早于本次 run 起点的遗留物**（trace 已按用例切分，误删即删证据），超限时只 WARN。新增守卫 11 例（保留策略 6 + 命名 5）。
>
> **另记一处 flaky（非本次改动）**：全量运行中 `BasePagePageSwitchLockConcurrencyTest.sameContextStillSerializes` 偶发 1 次失败；单独连跑 2 次均 **5/5 通过**，且与本次改动面无交集（上一次全量亦通过）→ 判定为负载下的时序 flaky，值得单独立项加固。
>
> **2026-09-20 已加固闭环（按用户指示"小、可本地验证"优先执行）**：根因为**时序假设**——原用例用 `Thread.sleep(50)`「确保 t1 已持锁」，再用耗时下界 `waited >= 300` 断言「确实等够了」；高负载时持锁线程可能尚未被调度，等待方反而先拿到锁 → 耗时下界假失败（与产品缺陷无关）。已改为三段确定性断言：① **握手**——持锁线程进入临界区当即 `CountDownLatch.countDown()`，主线程 `await` 到信号才继续（不再用睡眠猜调度）；② **互斥不变式**——持锁期间等待方**绝不可**进入临界区（等待方在尝试进入前先发「就绪」信号，使观测窗口有意义，排除"等待方尚未被调度"的假通过）；③ **无永久阻塞**——释放后等待方**必须**立即进入。全部绝对耗时下界已删除，超时上界仅用于防挂死、不作通过判据；`parallelContextsDoNotSerialize` 同口径加固。**验证**：连续 10 轮 **10/10 通过**；两处**负向探针**均命中（本仓既定做法）——把 `PageContextState.pageSwitchLockFor` 改为每次返回新锁 → `sameContextReturnsStableLock` 失败；把等待方改用另一把锁 → `sameContextStillSerializes` 失败（`expected: <false> but was: <true>`）→ 证明两条断言均为**活断言**而非空断言。
>
> **另记一处 flaky（2026-09-20 全护盾观测到，非该批改动引入）**：`route` 模块 `ApiMonitoringRepositoryConcurrencyTest.concurrentSavesBeyondCapRecordDataLoss`（断言「超出背压上限应触发数据丢失遥测」）在全护盾中偶发失败 1 次；**单独复跑 3/3 通过**，且该用例在此前两次全护盾中均通过、与本次改动面无交集（`route` 不依赖本次改动的 api 模块）。判定为**负载下的时序假设型 flaky**（"超限触发丢失遥测"依赖入队/刷库时序），与 C-7/M-3 无关；建议沿用 `BasePagePageSwitchLockConcurrencyTest` 的同款手段加固（以确定性握手 + 状态断言取代时序假设），列入后续批次。

> **用户拍板三项（PAR-7 / C-5 / C-10，2026-09-17）已全部落地**：
> - **PAR-7（api 补测 + JaCoCo 门禁）**：新增 `AbstractRestJobTest`（6 例：stripHtmlWrapper / applyResponse 回写 Entity / 响应回环 / getRestAssuredConfig 静态装配），重试内核仍由 `AbstractRestJobRetryTest`/`AbstractRestJobRetryLoopTest` 覆盖；api `pom.xml` 加 `jacoco` `report`+`check`，BUNDLE LINE `COVEREDRATIO` 下限 `0.23`（首次实测 23.5% = 427/1818），门禁「只升不降」，doc05 原建议的 0.40 作为目标位待补测上调（**2026-09-20 已完成**：实测 43.1%，下限上调至 0.40）。
> - **C-5（RouteRule 并发契约）**：经专项复核，`RouteRule` 是「配置期单线程写 → 经 `RouteRegistry` 的 `ConcurrentHashMap` 安全发布 → 发布后按只读」的可变配置模型。三处**明确发布后再写**的字段已真修：`mergeSource`/`monitorSessionRef` 改 `volatile`；`stoppedCapabilities`（测试主线程 stop* 写、事件线程 selectCapability 读，且写可能发生在已发布之后）改 **volatile 快照 + 写时复制**（落败者无、读为单次 volatile 读）；`copyForMerge` 同步为「复制-替换」语义。其余配置字段刻意不 `volatile`（发布后只读，非冻结债务），契约写入 `RouteRule` 类头与 `route/spotbugs-exclude.xml` 的「专项复核结论」注释；新增 `RouteRuleCapabilityVisibilityTest`（5 例：并发停不同能力全可见 / 幂等 / 显式全停 / copy 继承且独立 / 快照读确定性）。
> - **C-10（截断元数据：记录而非删死 API）**：原 `CapturedApiCall.markBodyTruncated` 是**无调用方的死 API**。改为**落地**：`MonitorHandler` 把响应体截断从「重试链内部」后移到「读取结果取回后」执行，使「原始长度」与「截断结果」同栈帧可得，并经新 `assertAndRecord(... originalBodyBytes)` 重载回填快照；`markBodyTruncated` 加**防误标**语义（仅当原始长度 > 已存 body 长度才标记）。报告/断言因此能显示「数据被截断」这一事实，避免与真实原因脱节。新增 `CapturedApiCallTruncationTest`（6 例：默认未截断 / 超长标记 / 等于不标 / 小于不标 / 负数不标 / 空 body 标记）。

> **Route 专项评审 第三轮（2026-09-17，C-11 / C-12 / core 专项收口）**：① **C-11 强键注册表归零守卫**：`RouteContextState` 新增 `removeContextFromAllRegistries(BrowserContext)`（幂等，从全部强键表 `CONTEXT_RULES_BY_CONTEXT` / `DISPATCHED_ROUTES` / `STOPPED_CAPS` / `CONTEXT_ENGINES` 移除该 context 并取消其在途任务），在 `RuleRepository.cleanupClosedContext` 末段兜底清零，杜绝新增强键表时遗漏清理导致泄漏的回归；新增 `RouteContextStateZeroGuardTest`（2 例），route 全护盾 29 例全绿。② **C-12 旧包名清洗**：test-automation 下 `framework/web/route/**` 整树（15 文件）`git mv` 至 `framework/route/**`，包声明 `framework.web.route.*` 已随路径对齐（`ApiMonitorOrchestratorTest` 包声明同步修正为 `framework.route.monitor`）；`MonitorFailureReportSinkSpiTest` javadoc、`route-demo-service/application.yml`、`test-automation/spotbugs-exclude.xml` 过期 `framework.web.route` FQCN 修正，全仓该引用清零。③ **core 专项（web 弱键对齐）**：`PlaywrightRuntimeState.disconnectedBrowsers` 由强引用 `newKeySet()` 改为弱引用 Set（与其兄弟 `closingBrowsers` 一致，防崩溃 Browser 强引用滞留）；`AsyncPool` / `ShutdownCoordinator` / 弱键类（`ThreadContextRegistry` / `ScenarioContext` / `ApiCaptureManager` / `closingBrowsers`）经复核无改动必要。详见 doc06 §5.6.2。

---

## 一、总体结论

- 12 份文档提取的问题条目**绝大多数为合理**，未发现明显误判。
- 已修复/误判（跳过）：E-4、R-1、R-2、L-5、A-8。
- 有取舍（仍建议修但需确认取舍）：L-3（脱敏失败回退原文）、W-7/X-7（共享 Browser 默认）。
- 去重后未修复且合理项约 **70 项**，按 P0/P1/P2 分级如下。

---

## 二、P0（必须修复，约 19 项）

| 合并ID | 来源文档 | 问题 | 现状 | 处置 | 建议 |
|---|---|---|---|---|---|
| A-1 | 01 | Checkstyle 仅 `EmptyCatchBlock` 1 条规则，门禁形同虚设 | 已修复（规则扩充至 8 条：EmptyCatchBlock/NeedBraces/EqualsHashCode/MissingOverride/UpperEll/ArrayTypeStyle/FileLength/UnusedImports；存量 1325 条违规经根 `checkstyle-baseline.xml` 祖父化；因 maven-checkstyle-plugin:3.6.0 的 `check` 目标不支持 `<baseline>` 且离线 verify 不产生 skin，「只减不增」由 build.yml 计数比对实现：verify 产出各模块 `target/checkstyle-result.xml`，比对 `<error>` 总数与基线，新增即失败）| 直接修 | TreeWalker 增补 NeedBraces/EqualsHashCode/MissingOverride/UpperEll/ArrayTypeStyle；生成 `checkstyle-baseline.xml`（只减不增），CI 校验基线不增 |
| A-2 | 01 | `maven-install-plugin` 绑 `package`，每轮污染 `.m2` | **已修复(2026-09-17)** | 直接修 | 移出默认构建，仅放独立 `sca` profile（`mvn -Psca verify`）；CI `Build & verify` 已加 `-Psca` |
| A-6 | 01 | OSV CVE 门禁依赖本地二进制，CI 静默跳过 | 已修复（build.yml 装 OSV 二进制 + 运行 cve-gate.ps1，OSV_CVE_GATE_REQUIRED=true）| 待决策(CI) | CI 装 OSV 二进制 + `OSV_CVE_GATE_REQUIRED=true`，`mvn verify` 纳入门禁 |
| C-1 | 02 | 测试上下文线程绑定而非用例绑定，并行下随机串扰 | 已修复（2026-09-14：接入 `FrameworkHooks` 与 `ConcurrentContextExecutor.runOnce` 强制 begin/end；`TestContextHolder.get()` 切用例级 `ScenarioContext.current()`，线程键保留兜底）| 直接修 | `ScenarioContext` 用例级 Map + 线程指针 + Cucumber `@Before/@After` 与 `runOnce` 强制 begin/end，`@After` 后 `assertUnbound` 未解绑即抛错；全护盾 631 例 0 失败验证 |
| C-2 | 02 | 日志脱敏不覆盖 `%ex`（异常栈） | 已修复（`SanitizingThrowableConverter` 接管 `ex` 转换词，两份 `logback*.xml` 注册；异常 message 内嵌 `key=value` 凭据经 `SensitiveDataSanitizer.sanitizeLine` 逐词遮蔽）| 直接修 | 新增 `SanitizingThrowableConverter extends ThrowableProxyConverter`，两份 `logback*.xml` 注册 `ex` 转换词 |
| W-1 | 03 | `cleanupAll` 未登记 `ShutdownCoordinator`，硬杀留孤儿浏览器进程 | 已修复（`FrameworkCore` 静态块已把 `PlaywrightManager.cleanupAll()` 登记进 `ShutdownCoordinator` 的 JVM 退出钩子；并发执行模式下 `cleanupAll` 抛 `IllegalStateException` 主动拒绝时仅 debug 记录、跳过浏览器清理，避免退出期刷异常栈）| 直接修 | `FrameworkCore`/`PlaywrightInitializer` 静态块登记 `cleanupAll()`，catch `IllegalStateException` 仅 debug |
| W-2 | 03 | 截图无脱敏 + `AFTER_EACH_STEP` 全量全页截图（金融合规） | 已修复（serenity.properties 改 AFTER_FAILING_STEP + fullpage false；CSS mask 待补）| 待决策(合规) | 改 `AFTER_FAILING_STEP`；对 `@RoleElement(sensitive=true)` 注入 CSS `filter:blur` 遮罩 |
| W-3 | 03 | trace.zip 仅时间戳命名，未关联 scenario、未挂报告 | 已修复（trace 文件名带 scenarioId 落 `target/site/serenity/traces`，并登记进 Serenity 报告「Additional Info」；Serenity 4.2.0 无 `StepEventBus.addAttachmentToCurrentStep`，改用 `Serenity.recordReportData()` 同源机制；失败清单 Trace 下载列为后续增强）| 直接修 | 文件名 `<sanitize(scenarioId)>.zip` 落 `target/site/serenity/traces`，`StepEventBus.addAttachmentToCurrentStep` 挂报告（4.2.0 实际用 `Serenity.recordReportData()`）|
| P-1 | 05 | `log().all()` 默认开启且绕过脱敏链路 | 已修复（`API_REQUEST_RESPONSE_LOGS_ENABLED` 默认改 `false`；`AbstractRestJob` 经 `LogConfig(new SanitizingPrintStream(System.out), true)` 将 `log().all()` 挂接出口脱敏，不再绕过 `SensitiveDataSanitizer`；`ConfigKeys` 键名对齐生效键）| 已修复 | — |
| P-2 | 05 | api 模块零测试覆盖（30 生产类无 `src/test`） | **已闭环（2026-09-20 达标）**（2026-09-17：9 测试类约 45 例 + `AbstractRestJobTest` 6 例补齐，api `JaCoCo check` 门禁落地、下限 0.23；**2026-09-20**：补测 6 个纯逻辑/配置类共 46 例〔`HttpStatusTest` 全量 80/80、`JsonUtilsTest` 113/123、`ApiLogSanitizerTest`、`FileReaderTest`、`EnvironmentUtilsTest`、`ConfigProviderTest`〕+ 2 个测试资材，实测 LINE **43.1%（792/1836）**，下限**上调至 0.40**〔doc05 既定目标位〕并经 `mvn -pl api verify` 实测通过）| 已修复 | 后续可按模块继续补测上调下限 |
| D-1 | 09 | 凭据明文入库，注释谎称 `ENC(...)` | 已修复（serenity.conf 三处 password 改 `${?DBB_TEST_PASSWORD}` 环境变量注入去明文；`SecretValue` 双格式透明解密：带/不带 `ENC(...)` 的密文均默认解密且绑定本地主密钥，解密失败一律失败快（仅非密文形态才原样返回））| 已决策 | 凭据仍建议走环境变量注入（最安全）；如需入库密文，用 `ConfigCipher` 生成 `ENC(...)` 或裸 base64，主密钥从 `CONFIG_MASTER_KEY` 注入不入库 |
| D-2 | 09 | 主密钥示例值写在配置文件注释 | 已修复（serenity.conf 注释改"主密钥严禁入库"；SecretValue 严格模式见 C-3）| 待决策(密钥) | 删注释示例密钥，改"CI Secrets 注入"；开 `SecretValue` 严格模式 |
| D-3 | 09 | `token.security.url` 内嵌明文 username/key（OTP 接口） | 已修复（serenity.conf 改 `${?DBB_OTP_URL_O63/O38/O88}` 环境变量注入；双格式解密已支持，如需入库可整条 `ENC(...)`）| 已决策 | OTP 接口整体移出配置，改 CI Secrets 注入或整条 `ENC(...)` |
| E-1 | 07 | 报告（含未脱敏截图）直推外部 gh-pages，无脱敏门禁 | 已修复（serenity-report-push.yml 加凭据 grep 扫描 + `.sanitized` 校验 exit 1）| 待决策(CI) | `serenity-report-push.yml` 部署前置凭据扫描 + `.sanitized` 校验；OTP url 移 CI Secrets |
| B-1 | 08 | 默认 `@test1` 仅覆盖 1 场景，回归套件名存实亡 | 已修复（根 pom 与 test-automation pom 的 `<tags>` 统一为 `not @skip`，默认跑所有未显式 `@skip` 的场景；环境依赖型 feature 打 `@skip` 即可默认跳过）| 直接修 | 统一默认标签 `not @skip` 或 `@route`；对齐根 pom 与子 pom `<tags>` |
| B-2 | 08 | 标签双源冲突（Runner `@ConfigurationParameter` vs failsafe `-D`） | 已修复（`CucumberTestRunnerIT` 删除硬编码 `@ConfigurationParameter(key="cucumber.filter.tags", value="@test1")`，标签完全由 failsafe `-Dcucumber.filter.tags=${tags}` 单一来源驱动；Javadoc 注明单一事实来源）| 直接修 | Runner 去掉硬编码 `cucumber.filter.tags`，仅由 `-Dtags` 驱动 |
| CI-1 | 11 | CI 只跑 `mvn -B test`，E2E/报告/SpotBugs/CVE 从未执行 | 已修复（build.yml 升 `mvn -B verify -DskipITs` + 新增 e2e job 灰度）| 待决策(CI) | 升级 `mvn -B verify -Dtags=...`，先加 `e2e` job 灰度 |
| CI-2 | 11 | CI 无 Playwright 浏览器安装步骤 | 已修复（build.yml 加 `playwright install --with-deps chromium`）| 待决策(CI) | `build.yml` 加 `playwright install --with-deps chromium` 并缓存 |
| CI-3 | 11 | 报告归档 `if-no-files-found: ignore` 静默跳过 | 已修复（build.yml 改 `if-no-files-found: error`）| 待决策(CI) | 改 `error`（修复后报告必产出） |

---

## 三、P1（应修复，约 30 项，节选）

| 合并ID | 来源 | 问题 | 现状 | 处置 | 建议 |
|---|---|---|---|---|---|
| A-3 | 01 | ArchUnit 仅 2 个测试，未覆盖主链路分层 | 已修复（`test-automation` 新增 `LayeringArchTest`：coreMustNotDependUpward / frameworkCodeMustNotDependOnJunit4 / frameworkCodeMustNotCallThreadSleep 三规则全绿；routeMustNotDependOnWeb=L6、noCycles=G1 已在 `ArchitectureTest` 覆盖，故不复述。注：放在 test-automation 而非 core 模块——分层规则需跨模块可见全部 framework 类，core 单测 classpath 看不到 web/route 类会使「core 不向上依赖」退化成无对象可查）| 直接修 | `core` 新增 `LayeringArchTest`（coreMustNotDependUpward/routeMustNotDependOnWeb/noCycles/noJunit4/noThreadSleepInFramework） |
| A-4 | 01 | api/codegen 完全无 `src/test`（版本属性注释矛盾已于 doc 12 另立 A-4，已修复） | 已修复（api 已有单测；codegen 纯逻辑类补契约测试：`NlsNameTranslatorTest` 7 + `RolePickerConstantsTest` 4 + `RolePickerSnapshotParserTest` 5，另有既有 `RolePickerClassNameResolverTest`。测试按既有约定置于 test-automation 镜像包——test-automation test classpath 同时含 codegen，是该模块类测试的既定落点；`codegen/src/test` 仍不新建）| 直接修(补测) | 至少补 codegen 纯逻辑（命名翻译/快照解析/常量契约）测试 |
| A-5 | 01 | `junit.platform.version` 注释矛盾、engine/commons 硬编码字面量 | 已修复（根 `pom.xml` 的 `junit-platform-engine`/`junit-platform-commons` 由硬编码 `1.14.0` 改为 `${junit.platform.version}`；launcher/suite/engine/commons 四处统一说明为 `${junit.platform.version}=1.14.0`，删去注释中过时的「1.11.4（离线仓库无 1.14.0）」矛盾表述）| 直接修 | 删过期注释；engine/commons 改 `${junit.platform.version}` |
| C-3 | 02 | `SecretValue` 解密失败静默降级为密文当值 | 已修复（SecretValue.java 默认严格抛 IllegalStateException，-Dsecurity.secret.strict=false 降级）| 直接修 | 默认严格模式，失败抛 `FrameworkException`；`-Dsecurity.secret.strict=false` 才降级 |
| C-4 | 02 | `SoftAssertions` ThreadLocal 残留污染下一用例 | 已修复（`test-automation` 新增 `tests.glue.FrameworkHooks`，`@After` 无条件 `SoftAssertions.clearForCurrentThread()`，对全部场景含 skip/失败/抛错路径生效；与 `ApiScenarioHooks` 清理 `ApiTestContext` 各司其职、均幂等）| 直接修 | `FrameworkHooks.@After` 兜底 `assertClean()`，skip 路径也强制解绑 |
| C-5 | 02 | 异步线程不传播 MDC | 已修复（`core/.../async/AsyncPool` 的 `submitTask`/`schedule`/`runOnMonitorCallbackThread` 三条异步路径均提交线程捕获 `MDC.getCopyOfContextMap()`、worker `setContextMap` 恢复、`finally MDC.clear()`；新增 `AsyncPoolMdcPropagationTest` 断言 MDC 传播到 worker 线程）| 直接修 | `AsyncPool.submitTask` 前 `MDC.getCopyOfContextMap()`，worker `setContextMap`/finally `clear` |
| C-6 | 02 | 配置解析路径分裂、无统一键注册表 | **已修复（2026-09-20 全量收敛）**：core `ConfigKeys` 注册表含重复键 fail-fast 静态校验 + `ConfigKeysRegistryTest` 3/3；Web / API / Monitor 三侧键定义（**184 键**）全部迁入 `ConfigKeys` 作为**唯一事实来源**，三侧退化为读取门面（`WebFrameworkConfig` / `ApiFrameworkConfig` / `MonitorConfig`）；新增 `ConfigKeysGoldenTest`（golden 快照——逐键逐字比对迁移前基线，防机械搬运写错默认值）与 `ConfigUnificationTest#facadesMustDelegateToRegistry`（门面逐字委托）两道守卫 | 已修复 | 新增配置键必须且只能登记 core `ConfigKeys`，由「重复键 fail-fast + golden 快照」双重兜底 |
| C-7 | 02 | `SensitiveDataSanitizer` god-class + 共享可变静态集 | 已修复（核心缺陷）：三个生效规则集（header/body/query）由「共享可变静态 `HashSet` + `clear+addAll`」改为**不可变快照 + `volatile` 整体赋值原子发布**，根治 reload 与并发读取的竞态（`reloadRules` 期间读取不再观察到空/半填充中间态）；新增 `SensitiveDataSanitizerReloadConcurrencyTest`（4 读线程 × 50 次 reload，断言 0 违规）固化。**整类策略链拆分**：2026-09-20 **阶段 0 已落地**（新增 `SensitiveDataSanitizerBehaviorTest` 10 例「行为基线 / 等价网」，覆盖 JSON 嵌套与非法 JSON 兜底、XML 元素+属性、form-urlencoded、自由文本 key=value / Bearer / JWT、URL userinfo+敏感 query、幂等与空值），并**实测抓到一处真实泄漏**——截断 JSON 降级到文本兜底链时 key 形如 `{"password"` 未剥离装饰符，规范化后匹配不上敏感清单，密码明文漏出（与类注释「非法 JSON 绝不原样放行」相悖）；已修（新增 `stripKeyDecoration`，结构化链与兜底链判定口径对齐），基线 10/10。**物理拆分已完成（阶段 1~3）**：`SensitiveDataSanitizer` 1035 行 → **219 行薄门面**，实现拆为 `SanitizerRules`（规则注册表：生效清单不可变快照 + 附加键 + 值识别器/豁免 + 掩码/键规范化/URL 解码等共享原语，356 行）、`BodySanitizers`（体脱敏**策略链**：JSON 树递归 / XML 双通道 / form-urlencoded，`sanitizeBody` 退化为有序策略派发 + 纯文本兜底，261 行）、`FreeTextScanner`（自由文本与凭据扫描：行内 key[:=]value、Bearer/JWT/URL 内嵌凭据、URLEncode 内层凭据，232 行）；纯重构——判定口径 / 链路顺序 / 懒加载时机 / 掩码形态逐字保持，等价网 10/10 仍绿 + 既有 18/2/49/7 全绿 + **全护盾 703 例 0 失败** | 已修复 | 拆不可变策略链 |
| C-9 | 02 | `ConfigSource` 对 `defaultValue` 也解密 | 已修复（默认值路径跳过裸密文启发式，仅显式 `ENC(...)` 才解密；含 2 条契约测试）| 直接修 | 默认值路径跳过解密或显式 `ENC()` 判定 |
| C-10 | 02 | `ApiTrafficLogger` IOException 仅 debug | 已修复（`core/.../apilog/ApiTrafficLogger` 的 enqueue / write 失败由 `debug` 升 `warn`，新增 `FAILURE_COUNT` 计数 + `failureCount()` 观测入口；前 5 次及之后每 100 次 warn、其余 debug 以防爆日志风暴）| 直接修 | 升级 warn + 计数 |
| W-4 | 03 | 崩溃重建不恢复登录态/storageState | 已修复（`BrowserRestartImpl` 两重建入口 `rebuildSharedBrowserIfDisconnected`/`rebuildSharedBrowser` 在 `initializeBrowser` 后调用 `restoreStorageStateFlag()`：若本线程已保存 storageState 则 `CustomOptionsManager.enableCustomOptions()` 置位 `customContextOptionsFlag`，使后续 `getContext()` 经既有 `configureCustomContextOptions` 把已保存 storageState 重应用到新建 Context；数据位于 per-thread `TestContext` 跨崩溃存活。新增 `CustomOptionsManagerTest` 4 例固化构建块。注：文档建议的 `SessionVault` 类代码库不存在，真实登录态由 `SessionManager`(落盘)+`CustomOptionsManager`(per-thread) 承担，故采用复用既有 storageState 应用逻辑的低风险落点，而非新建 SessionVault）| 直接修 | `BrowserRestartImpl.restoreStorageStateFlag()` 重建后恢复登录态 |
| W-5 | 03 | 引擎层完全串行，并发名存实亡 | 已修复（`test-automation/src/test/resources/junit-platform.properties` 提供 Cucumber 引擎级并行配置：`parallel.config.strategy=fixed` + `fixed.parallelism=4` + `max-pool-size=8` + `@isolated` 独占 `resource:global`；`parallel` profile 注入可调并发度旋钮，默认串行、显式 `-Pparallel` 开启，与 `ConcurrentContextExecutor` 用例内并发互补）| 直接修 | 引 `junit-platform.properties`+`@Suite` 驱动引擎级并行 |
| W-6 | 03 | 并发度硬编码上限 16 | 已修复（`web/.../concurrent/ConcurrentContextOptions` 的 `resolvedParallelism` 改读 `WebFrameworkConfig.PLAYWRIGHT_CONCURRENT_MAX`（键 `serenity.playwright.concurrent.max`，默认按 CPU 核数自适应、下限 2 上限 32、可经 `-D`/serenity.properties 覆盖），移除硬编码 16；负配置回退 `FALLBACK_HARD_CAP=16`；新增 `ConcurrentContextOptionsTest` 4 例固化可配置/绑定/兜底语义。注：未用文档所列裸 `Integer.getInteger("PLAYWRIGHT_CONCURRENT_MAX")`，改走框架统一配置门面 `WebFrameworkConfig`（与既有 `PLAYWRIGHT_CONCURRENT_PARALLELISM` 同体系，对齐 C-6 配置收敛方向），键名对应为 `serenity.playwright.concurrent.max`）| 直接修 | `Integer.getInteger("PLAYWRIGHT_CONCURRENT_MAX",32)`，默认按 `availableProcessors` 自适应 |
| W-9 | 03 | 大量硬编码超时/退避常量散落 | 已修复（散落字面量抽为 `WebFrameworkConfig` 键：重试/轮询 `PLAYWRIGHT_WAITS_RETRY_COUNT`(3)/`_RETRY_INTERVAL_MS`(1000)/`_RETRY_INTERVAL_DEFAULT_MS`(500)/`_POLL_STEP_MS`(50)、启动退避 `PLAYWRIGHT_BROWSER_STARTUP_BACKOFF_MS`(2000)、并发执行器关闭等待 `PLAYWRIGHT_CONCURRENT_EXECUTOR_AWAIT_SECONDS`(30)、BrowserStack `PLAYWRIGHT_BROWSERSTACK_CONNECT_TIMEOUT_SECONDS`(60)/`_REQUEST_TIMEOUT_MS`(30000)、崩溃重跑 `PLAYWRIGHT_CRASH_GUARD_MAX_REPLAY`(1)、上下文 trace 写盘 `PLAYWRIGHT_CONTEXT_CLOSE_TRACE_TIMEOUT_SECONDS`(15)、窗口稳定化 `PLAYWRIGHT_CONTEXT_STABILIZE_DELAY_MS`(300)；对应 `PageWaits`/`BrowserStartupImpl`/`ConcurrentContextExecutor`/`BrowserStackManager`/`BrowserCrashGuard`/`PlaywrightContextManager` 改读配置键，默认值逐字一致、行为零变更）| 直接修 | 抽 `WebFrameworkConfig` 键 |
| W-10 | 03 | 崩溃识别依赖异常消息签名匹配 | 已修复（`BrowserCrashGuard.isCrash` 改为<b>三重判定</b>：① 主信号=消息命中<b>可配置</b>崩溃白名单 `PLAYWRIGHT_CONCURRENT_CRASH_SIGNATURES`（默认与历史收窄白名单一致，`|` 分隔、可经 `-D`/serenity.properties 覆盖）；② ③ 佐证信号（受 `PLAYWRIGHT_CONCURRENT_CRASH_CORROBORATION_ENABLED` 控制，默认开）=异常类型为 Playwright/超时类 <b>且</b> 当前线程 Browser 已被 `onDisconnected` 标记断开（Playwright 事件）。仅 ② 或仅 ③ 均不判崩溃，保留 W-15「不按异常类名模糊匹配」防误判铁律；正常业务失败不触发重跑。新增 `BrowserCrashGuardTest` 7 例固化消息命中/非崩溃消息/类型单独不判/可配置签名/佐证关闭回退/句柄损坏不变/重跑上限配置。注：白名单分隔符用 `|` 而非逗号，因默认签名含 "target page, context or browser has been closed" 本身带逗号）| 直接修 | 消息签名+异常类型+Playwright 事件三重判定，签名可配置 |
| G-1 | 04 | 定位器字符串拼接注入面（testId/iframe） | **已修复(2026-09-17)**：`byTestId` 主属性走 `page.getByTestId`（内建转义闭合并入面），其余三种历史兼容属性（`data-test-id`/`data-test`/`data-qa`）用转义后 CSS 经 `.or()` 合并保留兼容；iframe 已参数化 | 直接修 | `byTestId` 改用 `page.getByTestId`；iframe 用 `frameLocator(seg).locator(...)` 参数化 |
| G-2 | 04 | `NlsNameTranslator` 错放 web 而非 codegen | 已修复（`NlsNameTranslator` 已下沉至 `codegen/.../web/page/scan/nls`，`RoleElementPageGenerator` 改引新包；`web` 移除 jieba/pinyin4j 依赖，仅 codegen 声明；web 运行期本无消费者，故无需 SPI 桥接）| 直接修(搬运) | 迁 codegen，web 经 SPI 调用，jieba/pinyin4j 仅 codegen 依赖 |
| G-3 | 04 | Page 反射 newInstance，无编译期校验 | 已修复（`PageObjectFactory` 新增全局 `register(Class,Supplier)` 显式注册表为优先路径；反射回退缺无参构造时抛清晰 `ConfigurationException` 引导登记）| 直接修 | 推广 `register(Class,Supplier)` 显式注册为默认，未注册早失败 |
| G-4 | 04 | 四种 LifecycleStrategy 无默认约定/守卫 | 已修复（简化） | 直接修 | 共享 Browser 模式已移除，`isSharedBrowserMode` 守卫不再适用；`defaultStrategy()` 直接返回 `PROTOTYPE`，移除与浏览器模式的耦合（见 04 文档 §默认策略） |
| G-6 | 04 | `PageWaits` 固定 500ms 轮询无抖动 | 已修复（`waitUntil` 条件驱动 + 读 `PLAYWRIGHT_WAITS_POLL_STEP_MS`，非固定 500ms 盲等） | 直接修 | 指数退避 + `ThreadLocalRandom` 抖动 |
| G-8 | 04 | `RoleElementBinder` 反射 setAccessible 与 JPMS 张力 | 已修复（新增 `ReflectiveField` 工具：优先 `MethodHandles.privateLookupIn`+`VarHandle` 注入，失败回退 `setAccessible`；`RoleElementBinder`/`PageContextState` 字段读写已改用）| 直接修 | `--add-opens` 验证或 `MethodHandles.Lookup` 构造注入 |
| P-3 | 05 | 无连接池/无重试 | 已修复（`AbstractRestJob` 经 `httpClientFactory` 注入 `PoolingHttpClientConnectionManager`（max-total/per-route 可配）；`execute` 新增幂等+5xx+网络错误重试，绝不重试 4xx，次数/退避取自 `ApiFrameworkConfig`）；测试经 `shouldRetry` 纯逻辑 + `executeWithRetry` Mockito 桩覆盖重试循环（SerenityRest 真实 HTTP 因 Serenity 4.2.0 classloader 隔离须跑在 Serenity runner，api 纯 JUnit 不发起真实响应提取）| 直接修 | `HttpClientConfig` 设最大连接；`executeWithRetry` 幂等+5xx 重试 |
| P-4 | 05 | SSL 放宽无审计 | 已修复（新增 `SecurityAudit` 记录放宽事件 + `SecurityAuditTest` 5/5；`AbstractRestJob.applySslPolicy` 放宽时登记审计，prod 环境 `relax=true` 抛 `IllegalStateException` fail-fast）| 直接修 | relax 时 `SecurityAudit.recordRelaxedTls`；prod 环境 relax=true fail-fast |
| P-5 | 05 | JSONPath 两套引擎并存 | 已修复（`BaseStep` 统一 Jayway：删除自写 `resolveJsonPath`，`verifyResponseJsonPath`/`verifyJsonArrayLength` 共用 `JAYWAY_CONFIG` = JacksonJsonProvider/JacksonMappingProvider；新增 `BaseStepJsonPathTest` 5 例）| 直接修 | 删自写 `resolveJsonPath`，统一 jayway |
| R-3 | 06 | `ApiCaptureManager` 节流进程级单例 | 已修复 | 直接修 | `lastResolve` 改 per-context（`ApiCaptureManager.lastResolveByContext`），per-context `contextStores` 隔离路径（G3）不变 |
| R-4 | 06 | 写库失败仅 warn，数据丢失不可见 | **已修复(2026-09-17)**：计数(`droppedCount`/`failedCount`)+报告尾部红色提示（`MonitorDataLossReporter` 累计丢失 + `MonitorFailureReportWriter` 汇总报告尾部红色横幅 + shutdown ERROR 告警），数据丢失可见 | 直接修 | `AtomicLong DROPPED` 计数 + 报告尾部红色提示 |
| E-2 | 07 | 无趋势/历史对比 | 已修复（新增 `RunSummary` + `TrendStore`：每场在 `<reportDir>/trend-history/<buildId>.json` 落快照；汇总报告读取上一场，在 Full Test Results 增加「本次/上次耗时 + 变化率」列，近 5 场失败 ≥3 次打 `FLAKY` 标记；**仅当存在上一场快照时渲染**，无历史时输出与 golden 基线一致；新增 `TrendStoreTest` 3 + `SummaryReportTrendTest` 3）| 直接修 | 新增 `TrendStore`，`target/trend-history` 落快照，flaky 标记 |
| E-3 | 07 | trace 未挂进报告 | 已修复（trace 命名/挂 Serenity 报告已由 W-3 落地；本次补二次汇总报告：`SummaryReportGenerator` 扫描 `traces/trace-<scenarioId>-<ts>.zip` 建索引，失败清单按「归一化场景名 + 尾部数字」前缀匹配挂 Trace 下载列，**仅当存在匹配 trace 时渲染该列**（无 trace 时输出与 golden 基线逐字节一致）；新增 `SummaryReportTraceReportTest` 3 例）| 直接修 | `SummaryReportGenerator` 附加 trace 下载列 |
| E-6 | 07 | 汇总报告绑 verify，CI 只 test | 已修复（`build.yml` 升 `verify`，汇总报告 `summary.report.phase=verify`） | 待决策(CI) | `build.yml` 升 `verify` 或汇总下沉 `test` 阶段 |
| B-3 | 08 | 两个同名 `LoginSteps` | 已修复（`tests.LoginSteps` → `tests.RoleElementLoginSteps`，更新 `RoleElementLoginGlue` 引用，删除旧类；消除"同名同类"隐患，行为零变更。**未强行合并**：两者实现不同（RoleElement 版 vs 传统 locator + RouteDsl mock/monitor + profile 切换），各被不同 Glue 使用，合并会改变 RoleElement 场景登录行为——需在具备该场景可运行环境时另行评估）| 直接修 | 消除同名隐患（重命名）；策略合并待 E2E |
| B-4 | 08 | 步骤内 `Thread.sleep` | 已修复（新增 `tests.utils.AsyncWaits` 统一有界轮询原语（`awaitTrue`/`awaitResult`：超时上界+固定间隔+中断安全）；`RouteDemoServiceSteps`/`RouteDemoCoverageSteps`/`RouteDemoCompositeSteps`/`RouteDemoParallelSmokeSteps` 4 个步骤类的轮询与负向窗口全部改用它；新增 ArchUnit `LayeringArchTest#stepsMustNotCallThreadSleep` 禁 `*Steps` 调 `Thread.sleep`，经**负向探针**验证可命中。**顺带修正既有空转规则**：`frameworkCodeMustNotCallThreadSleep` 原 `callMethod(Thread,"sleep")` 省略参数类型 → 只匹配无参 `sleep()`（不存在），规则形同虚设；修为 `sleep(long)` 后暴露并修复 `AbstractRestJob` 的 P-3 重试退避改用 `LockSupport.parkNanos`）| 直接修 | 换 `PageWaits`/`waitFor*`；ArchUnit 禁步骤层 `Thread.sleep` |
| B-5 | 08 | 步骤内联断言 + 耗时下界断言（flaky） | 已修复（① 移除 `RouteDemoServiceSteps:508` 的 flaky 计时下界 `elapsed>=700`，改为**观测日志 + 宽松上界 SLA**；delay 能力已由 `RouteDemoCompositeSteps` 的 DELAY 维度标记断言确定性覆盖；② 新增 `tests.verify.RouteDemoVerifications` 步骤层断言收口，4 个 `RouteDemo*Steps` 的断言 import 全改经它（步骤层不再直连 `org.junit.jupiter.api.Assertions`）；③ 新增 ArchUnit `LayeringArchTest#stepsMustNotUseJunitAssertionsDirectly`，经**负向探针**验证可命中）| 直接修 | 性能只作观测/上界；业务断言下沉 Service/Verifier |
| B-6 | 08 | 隔离靠手写 `@Before` 重置 | 已修复（core 新增 `StateResolver` SPI + `CleanStateRegistry` + `@RequiresCleanState` + `StateResidueException`；test-automation 新增框架钩子 `CleanStateHooks`：`@Before` 统一复位、`@After` 复位并**断言无残留**（发现残留即抛 `StateResidueException`，把"忘记清理"从"以后某用例莫名失败"变为"当场失败并指明状态"）；4 个 route-demo glue 移除手写 `@Before`/`@After`，改为注册 `StateResolver`；`CleanStateRegistryTest` 6 例。注：`@RequiresCleanState` 为声明式检索（运行时由注册表+钩子驱动，非注解自省——Cucumber 全局钩子无法按 glue 类自省）；route-demo 场景的最终行为一致性仍以对应 IT 为门禁）| 直接修 | `@RequiresCleanState` 注解 + `FrameworkHooks` 统一重置/断言残留 |
| D-4 | 09 | 无环境 profile | 已修复（根 pom 新增 `sit1/sit2/sit3` profile，注入 `env`/`env.code`/`base.url`；test-automation failsafe 将其下发为系统属性。**铁律**：不注入标量 `environment` 系统属性——`BDDUtils` 以 `environment.<ENV>` 读取环境 URL（对象键），标量会整体覆盖导致登录 URL 失配。`SecurityAudit.isProductionEnvironment` 对空白 `env` 判非生产，空默认安全）| 直接修 | 根 pom 加 `sit1/sit2/sit3` profile 注入 `env.code`/`base.url` |
| D-5 | 09 | 无数据工厂 | 已修复（新增 `tests.data.Payload`（classpath `payload/<模板>.json` + `with` 浅覆盖）+ `tests.data.PayloadFactory`（`login`/`createUser`/`uniqueUser`）；唯一化数据使用例间天然不冲突，替代复制 JSON 与 `@Before` 重置；新增 `PayloadFactoryTest` 5 例）| 直接修 | 新增 `PayloadFactory`（模板+`with`+`uniqueUser`） |
| D-7 | 09 | 配置优先级链无文档 | 已修复（`serenity.conf` 头部写明完整取值优先级链：系统属性 > 环境变量 > `-Pprofile` 的 environment-*.conf > serenity.conf/properties > 代码默认值，并登记「新增配置项落哪个文件」的四类归属表；routedemo.conf 合并属 §5.5 P2，未纳入本次）| 直接修 | `serenity.conf` 头写明优先级链，收敛两源一入口 |
| X-1 | 10 | 引擎无并行配置，名义并发实际串行 | 已修复（同 W-5：`junit-platform.properties` + `parallel` profile 接线完成，默认串行、显式 `-Pparallel` 开启）| 待决策(压测) | 同 W-5 |
| X-6 | 10 | 并发度硬编码 16 | 已修复（同 W-6：`ConcurrentContextOptions.resolvedParallelism` 读 `WebFrameworkConfig.PLAYWRIGHT_CONCURRENT_MAX`，可配、默认按 CPU 自适应）| 直接修 | 同 W-6 |
| X-8 | 10 | 重试默认关闭（rerun=0） | 已修复（根 pom `rerunFailingTestsCount` 默认 0→1，CI 可 `-D` 覆盖；注释说明「failsafe 不支持按原因重试，仅环境/崩溃分类重试见 doc10 E-4 与 FrameworkHooks，属后续项」，报告标记 flaky 防掩盖真实缺陷）| 直接修 | 默认改 1，仅环境/崩溃类重试 |
| X-9 | 10 | `forkedProcessTimeoutInSeconds=600` | 已修复（root pom 提为可配置属性 `forkedProcessTimeoutInSeconds` 默认 1800s，failsafe 引用；CI 可按并行度/套件规模 `-DforkedProcessTimeoutInSeconds=N` 覆盖）| 直接修 | 随并行上调或按 feature 分片 |
| CI-4 | 11 | CVE 门禁二进制未安装 | 已修复（build.yml 装 OSV 二进制 + 运行 cve-gate.ps1）| 待决策(CI) | 装 OSV + `OSV_CVE_GATE_REQUIRED=true` |
| CI-5 | 11 | 报告推送无脱敏门禁 | 已修复（serenity-report-push.yml 加凭据 grep 扫描 + .sanitized 校验 exit 1）| 待决策(CI) | 同 E-1 |
| CI-7 | 11 | doc-drift 用 `.sh`，Windows 无法复验 | 已修复（doc-drift-check.yml 改 pwsh check-doc-drift.ps1 + setup-powershell）。**2026-09-20 修复门禁失效（本项真正闭环）**：根 `README.md` 于 `aa02d27` 被批量误删，而脚本要求 `pom.xml` 与 `README.md` **同时存在**（否则 `exit 2`），致该 CI job 在每次 pom 变更时必然失败（门禁形同虚设 + 噪音）——已恢复根 README（精简、版本与 pom 严格一致、不含已删除文件引用），并修正脚本中**失效的 `junit.version` 映射**（根 pom 实为 `junit.jupiter.version`/`junit.platform.version`，原条目只打 WARNING 被静默跳过）+ 补 `junit.platform.version` 校验；实测 10 项全 `OK`、`exit 0`，`--fix` 幂等（不改动已对齐的 README）| 直接修 | CI 用 `.ps1` 镜像或加 Windows runner |
| L-1 | 11 | 异常栈脱敏 | 已修复（同 C-2，`SanitizingThrowableConverter` 接管 `%ex`）| 直接修 | 同 C-2 |
| L-2 | 11 | 异步不传播 MDC | 已修复（与 C-5 同一问题，`AsyncPool` 三条异步路径已传播 MDC） | 直接修 | 同 C-5 |
| L-3 | 11 | 脱敏失败回退原文 | 已修复（`SanitizingThrowableConverter` 在脱敏抛异常时输出 `[LOG SUPPRESSED: sanitizer failure]` 而非原文，高风险字段必须抑制）| 直接修 | 返回 `[LOG SUPPRESSED: sanitizer failure]`（高风险字段必须抑制） |
| L-4 | 11 | 日志乱码靠不用西文标点规避 | 已修复（根因 logback UTF-8 已落地；已删 `tools/clean_log_typographic.ps1`，无调用方）| 直接修 | 保留 logback UTF-8，删 `tools/clean_log_typographic.ps1` |
| L-6 | 11 | `ApiTrafficLogger` IOException 仅 debug | 已修复（同 C-10：warn + 计数，见 C-10）| 直接修 | 同 C-10 |
| M-1 | 11 | `tools/` 32 个一次性脚本留仓库 | 已修复（22 个一次性迁移/审计脚本 `git mv` 至 `tools/.attic/`；保留 `cve-gate.ps1`/`check-doc-drift.{ps1,sh}`/`install-osv-scanner.ps1`/`gen-osv-baseline.ps1`/`validate_picker_js.js`/`picker_merge_test.js` 等构建/CI 关键脚本）| 直接修 | 保留 check-doc-drift/cve-gate/install-osv，其余 `git mv` 移 `.attic/` |
| M-2 | 11 | 配置键三枚举平行无注册表 | **已修复（2026-09-20 全量收敛，同 C-6）**：三侧键定义（184 键）已全部收敛至 core `ConfigKeys` 唯一事实来源，三侧退化为读取门面，golden 快照 + 门面委托校验双守卫 | 已修复 | 同 C-6 |
| M-3 | 11 | `SensitiveDataSanitizer` god-class | 已修复（同 C-7：共享可变静态集/竞态消除 + 并发回归测试；整类策略链拆分待决策）| 待决策(拆分) | 同 C-7 |
| M-4 | 11 | 空目录/根目录垃圾 | **已修复（复核确认，原「未修复」判定过期）**：`common/action,env,persistence` 空目录已不存在（随 A-7/C-12 一并删除）；根目录仅剩 `.gitattributes` / `.gitignore` / `checkstyle.xml` / `checkstyle-baseline.xml` / `pom.xml` 五个合法文件，无垃圾文件（`git ls-files` 与空目录扫描均无命中） | 直接修 | 清空包 + 根 `.gitignore` |

---

## 四、P2（次要，约 25 项，精简）

| 合并ID | 来源 | 问题 | 现状 | 处置 |
|---|---|---|---|---|
| A-7 | 01 | 空目录 `common/action,env,persistence` | 已修复（三个空目录已删除，`framework.common` 仅保留有内容的包） | 直接修（删） |
| A-9 | 01 | Serenity+selenium+Playwright 混搭依赖冗余 | 已修复（实测收敛）：非法通配 `selenium-*` 改显式排除 `selenium-java`+`selenium-chrome/firefox/edge/ie-driver`+`htmlunit3-driver`；`selenium-support/api` 保留（serenity 公开 API 引用）。driver 栈已从依赖树移除，全护盾 1001 例全绿 | 直接修（实测收敛） |
| C-8 | 02 | `LanguageState.globalLang` 静态全局可变 | 已修复（`FrameworkHooks` 每 scenario 前后 `clearScenarioScopedState()` 复位语言态；双轨保留全局轨道以维持跨线程可见性；`FrameworkHooksLanguageIsolationTest` 2/2） | 直接修（随 TestContext 维读） |
| C-11 | 02 | `CONTEXT_SCHEDULERS` 未清理即泄漏 | 已修复（`shutdownGracefully` 遍历关闭并 `clear()`；`removeContextScheduler` 可主动摘除） | 直接修（引用计数/try-finally） |
| C-12 | 02 | `common/persistence` 空目录 | 已修复（随 A-7 一并删除） | 直接修（删） |
| W-7 | 03 | 默认每线程独立 Browser | 已决策：保持默认 false（曾试默认 true → Playwright Java 非线程安全、并发损坏对象注册表，ConcurrentContextNoDeadlockTest 失败；回退后 642 例全绿）| 直接修（保持 false，共享模式 opt-in） |
| W-8 | 03 | `PlaywrightListener` 残留裸 ThreadLocal | 已修复（5 个裸 ThreadLocal 全迁入 TestContextHolder：currentTestResult / CURRENT_SCENARIO_NAME / CURRENT_STEP_TITLE / AFTER_STEP_FIRED / STEP_FAILED；清理统一走 TestContextHolder.remove） | 直接修（收口 TestContextHolder） |
| G-5 | 04 | `WebFrameworkConfig` 大文件 | 已决策：**不拆分**（Java 枚举无法跨文件拆分；拆分须退化为 class+常量并丢失 `values()`，`allConfigKeys()` 需人工聚合 = 重新引入「漏登记」失败模式；1690 行中 ~1570 行是 132 个声明式键，仅 ~110 行行为）。改以**校验**关闭其陈述危害：`ConfigUnificationTest#configKeysAreGloballyUnique`（Web 枚举 × API 常量键全局唯一 + 非空性断言，负向探针验证）；**2026-09-20 键定义全量收敛后**，本枚举已退化为「只含访问器 + 引用 `ConfigKeys`」的薄门面，文件长度不再随键数增长（原 1690 行 → 922 行）| 待决策（拆子枚举） |
| G-7 | 04 | codegen 产物入仓策略不明 | 已修复（`RoleElementPageGenerator` javadoc 写明产物策略：入仓但**独立目录** + 首行 `GENERATED BY RoleElementPageGenerator` marker（无 marker 拒绝覆盖）+ 原子写 + CI drift 校验；三者在代码中已实现，本次补文档定性） | 直接修（策略文档化） |
| G-9 | 04 | 未充分利用 Playwright 自带 `Page` 接口/实现类（未下沉为 `Page` 装饰器） | 已修复（实际落地为 `RecordingPageProxy` 动态代理装饰器，`PageObjectFactory` 已接入；命名与评审建议 `SerenityRecordingPage` 不同但同义） | 进行中 |
| P-6 | 05 | 无统一拦截器机制 | 已修复（route 已统一 `Dispatcher→RouteHandlerRegistry→HandlerExecutor` 拦截/分发链） | 直接修（统一拦截器链） |
| P-7 | 05 | 无响应 schema 校验 | 已修复（新增 `JsonSchemaValidator`（networknt 1.5.1）+ `BaseStep.verifyResponseMatchesSchema` / `verifyResponseJsonPathMatchesSchema`（整份/子文档）；schema 缓存编译结果、缺失即失败快、失败信息不回显响应体；步骤层 2 条绑定；测试 20 例） | 直接修（networknt/json-schema-validator） |
| R-5 | 06 | 录制内存无容量上限 | 已修复（`ResponseStore` 50MB 总上限 + recent 500 + perEndpoint/perUrl 100 淘汰） | 直接修（长跑验证） |
| R-6 | 06 | DSL/RuleRepository 双入口未文档化 | 已修复（`RouteDsl` 类 javadoc 增补「入口选择」决策表：业务用例用 DSL、框架/仓储层用 `RouteRegistry`、二者同写一注册表故可混用但同 pattern 勿双入口注册） | 直接修（javadoc 决策表） |
| R-7 | 06 | 模块命名 `route` 歧义 | 已决策：**保留 `route`，不重命名**（`route` 即 Playwright 平台词汇 `page.route()/Route`，模块内 23+ 处 `import com.microsoft.playwright.Route`；本仓无「用例路由」构造；改名牵涉模块/包名/61 java/pom/ArchUnit/示例应用/标签/文档而收益为 0）。危害改由 `route/package-info.java` 术语定位说明关闭 | 待决策（重命名 traffic） |
| E-5 | 07 | `SerenityReporter.reportStep` 空实现 | 已修复（`@apiNote` 明确其为**有意 no-op**（Serenity 原生已记录步骤，避免重复写入），非未填充接缝；如需写框架特有步骤应新增接入点） | 直接修（文档定性） |
| E-7 | 07 | ZIP 打包内容未说明 | 已修复（`generateZipPackage` javadoc 写明白名单：含汇总 HTML/Serenity 报告/traces/trend-history 等；排除 ZIP 本体、CSV、`.tmp` 临时产物；代码同步加 `.tmp` 跳过） | 直接修（白名单 + 文档） |
| B-7 | 08 | 标签命名不成体系 | 已修复（建立三维标签体系：执行层 `@smoke`/`@regression` + 子系统 `@web`/`@api`/`@route` + 域 + 状态 `@skip`/`@flaky`；17 个 feature 补 feature 级三维标签，删除无语义 `@test1`/`@test`、`@baidu1`→`@baidu`；新增门禁 `FeatureTagTaxonomyTest`（未注册标签 / 缺维度即失败，负向探针验证））| 直接修（三维标签） |
| B-8 | 08 | 文档与实现不一致（package-info 称 `@0test`） | 已修复（`tests/api/steps/package-info.java` 更新：删除过期的 `@0test`/"执行 0 个场景"表述与已删文档引用，改为当前 `tags="not @skip"` 单一事实来源说明） | 直接修（更新+doc-drift 校验） |
| D-8 | 09 | `application.conf` 默认 `ssl.relax-validation=true` | 已修复（默认改 `false`，安全优先；本地/自签证书须显式覆盖） | 直接修（默认 false） |
| D-9 | 09 | 静态 JSON 含明文密码字段 | 已修复（新增 `payload/README.md` 显式标记 4 个夹具为**本地演示服务的非机密约定值**，并约定"真实凭据禁止入仓、变体数据走 `PayloadFactory`"；值保持与演示服务校验值一致以免演示用例失配） | 直接修（文档标注 + 约定） |
| D-10 | 09 | 数据重置无事务性兜底 | 已修复（配合 B-6：`CleanStateHooks@After` 复位后**断言无残留**，残留即抛 `StateResidueException`，失败不再被静默吞掉；业务侧可再对具体状态实现 `StateResolver.isDirty()` 精确判定） | 直接修（配合 B-6 断言残留） |
| X-7 | 10 | 默认每线程独立 Browser | 已决策闭环（与 W-7 同一问题，共享 Browser 模式代码已整体移除） | 待决策（同 W-7） |
| M-5 | 11 | docs 被删致 doc-drift 无可校验 | 已修复（docs/ 重建 13 份架构评审文档）| 直接修（补三层架构文档） |

---

## 五、待决策/需评审项（需你拍板后推进）

1. **CI 策略**：CI-1~CI-5 / A-6 / E-1 / E-6 / CI-7 **已全部落地**（见 §二 表格 + §〇 快照，并经 `build.yml` 实际核查：升 `verify`（含 SpotBugs/CVE/汇总报告）、`playwright install --with-deps chromium`、归档 `if-no-files-found: error`、OSV/CVE 门禁、报告门禁 grep + `.sanitized` 校验、doc-drift 镜像均就位）。→ **2026-09-20 已完成代码侧转正**：`e2e` job 的 `continue-on-error` 已移除（真实浏览器 IT 失败即报红，不再自我豁免）。**仍需人工完成的一步**：GitHub 仓库 Settings -> Branches 分支保护把本 workflow 设为 required status check。⚠️ 注意 `e2e` 现为 **push-only**，若直接设 required，PR 会因该状态永不回报而卡在「Expected — Waiting for status to be reported」——要让它对合并真正生效，需同时把 job 的 `if` 放开到 `pull_request`（前提：SIT/凭据以 Secrets 对 PR 可用，且接受每个 PR 都跑真实浏览器 IT 的时长成本）。两处均已写入 `build.yml` 文件头与 job 内注释。
2. **明文凭据密钥管理**：D-1/D-2/D-3 已修复，`SecretValue` 双格式透明解密 + 默认严格失败快 + 裸密文 key 不匹配即失败快均已落地（关键设计已闭合）。剩余纯运维项：确认各环境（本地/CI）`CONFIG_MASTER_KEY` 注入来源，避免主密钥随配置入库。
3. **共享 Browser 默认（W-7/X-7）已决策**：保持默认 `false`——Playwright for Java 官方明确对象非线程安全，并发共享单 Browser 会损坏对象注册表（复现 `__adopt__`/`pausedStateChanged`，与 playwright-java#1184 一致）；共享模式仅作显式 opt-in。
4. **Engine 级并行 + 并发度（CON-1 已进入）**：W-5/X-1 引擎级并行接线已完成（见 §〇 代码核查结论表 X-1 行）——`junit-platform.properties` + `parallel` profile 默认串行、显式 `-Pparallel` 开启；隔离前置 C-1/X-2、D-6、X-3 已全部闭环。**剩余=压测护栏（X-1 梯度 4→8→16）**：须在 nightly/CI 用真实浏览器+后端+内网跑两周（`-Dparallelism=N -DmaxPoolSize=M` 调参），本地无法执行；建议先以 `@isolated` 标签串行保护不可并行用例（改全局配置 / 清理共享数据 / OTP 限流）。**2026-09-20 已落地本地采集护栏**：`route-demo-service` 新增 `RouteDemoConcurrencyGuardrailTest`（真实浏览器 + demo 后端 + MySQL，梯度 4/8/16，跨 Context 并发、同 Context 内串行），断言「**已完成请求零丢失**（落库数 ≥ 成功请求数）+ 每个并发参与者至少完成一次（防"全失败也算过"）+ 活跃连接为 0 + `shutdown()` 后回落基线」；实测通过（P=4/8/16 期望 12/35/83，实际落库 14/55/150；活跃连接 0）。旋钮：`-Dguardrail.parallelisms=4,8,16`、`-Dguardrail.requestsPerPage=3`。注：历史事故「monitor@50 捕获为 0」正是本护栏要钉住的失败模式——并发丢采在单并发下完全不可见。nightly/内网长稳压测仍建议保留（护栏不能替代长稳观察）。注意：无 context 兜底路径的全局 `currentStore` 仍为单路径，开启并行前须确认 demo 等共享后端已按 D-6 复位。（2026-09-14 实跑确认引擎级并行生效：`parallel_logon_dbb` 在 `thread-1~4` 并发；本地失败仅因 SIT 后端不可达，CONFIG_MASTER_KEY 非阻塞；纯 JUnit5 单测在 `-Pparallel` 下亦并行跑通）
5. **ScenarioContext 用例绑定**：C-1 / X-2 / PAR-1 已闭环（2026-09-14：`@Before/@After` + `ConcurrentContextExecutor.runOnce` 强制 begin/end + `TestContextHolder` 读取切用例级，线程键保留兜底，非大爆炸重构；数据隔离钩子 `ScenarioDataNamespace` 同期接入 `ScenarioContext.end`）。
6. **api/codegen 补测试**：P-2/A-4，工作量大。**2026-09-20 进展**：api 行覆盖率已达 doc05 既定目标位 **0.40**（实测 43.1%，下限已上调，见 §二/§三 P-2 行）；补测过程中发现并修复 `JsonUtils` 两处潜伏缺陷——① `getValue(String,String,TypeRef)` 走 Jayway 默认 JsonSmart provider **恒返回 null**（重载形同虚设），改用 Jackson 映射 provider 后类型化读取真正可用；② `setValue`/`deleteValue` 复用「按 json 串缓存」的共享 `DocumentContext` 并<b>原地改写</b>，会把改写结果泄漏给此后读取同一 json 串的调用方（缓存污染），现变更类操作改用独占实例。两处均补了回归测试（含"变更不污染缓存"用例）。codegen 侧仍以既有契约测试覆盖。
7. **Sanitizer 拆分 / 配置注册表**：C-7/M-3 核心缺陷已修（`SensitiveDataSanitizer` 共享可变静态集 → 不可变快照 + `volatile` 原子发布，竞态回归测试已固，见 §二 C-7 行）；**整类策略链拆分（god-class → `SanitizerEngine`/策略链）进入分阶段实施**——安全关键路径：**阶段 0（行为基线/等价网）已于 2026-09-20 落地**（`SensitiveDataSanitizerBehaviorTest` 10 例；并抓到一处真实泄漏：截断 JSON 兜底链未剥离 key 装饰符致密码漏出，已修）；**阶段 1~3（物理拆分）已完成**：门面 1035 → 219 行，实现拆为 `SanitizerRules` / `BodySanitizers`（策略链）/ `FreeTextScanner`，等价网仍绿、全护盾 703 例 0 失败。C-6/M-2 **已于 2026-09-20 全量收敛闭环**：三侧键定义（184 键）全部迁入 core `ConfigKeys` 作为唯一事实来源，三侧退化为读取门面，由 golden 快照 + 门面委托校验双守卫（见 §三 C-6 行）。
8. **截图脱敏**：W-2 策略已落地（`serenity.properties` 改 `AFTER_FAILING_STEP` + fullpage false——仅失败步截图、非全页，金融合规基线已收窄；`@RoleElement(sensitive=true)` 属性已存在）。**经用户决策：敏感元素 CSS `filter:blur` 遮罩方案本期不实施**（截图范围已收紧至失败步且非全页，叠加 blur 收益有限；blur 半径/属性覆盖属合规细则，待另行评审）→ 本条由「待决策」降级为「已收窄 + 遮罩方案暂缓」。

---

## 六、分批修复计划

- **第 1 批（P0 直接可修，立即启动）**：C-2/L-1（异常栈脱敏）、L-3（脱敏回退抑制）、P-1（log 默认 false+SanitizingPrintStream）、D-8（ssl 默认 false）、W-1（cleanupAll 注册 ShutdownCoordinator）、W-3（trace 关联 scenario）、B-1/B-2（标签统一）、A-1（Checkstyle 规则）、A-5（junit 注释/字面量）、C-9/C-10（配置默认值解密 / ApiTrafficLogger warn）、L-4（删清理脚本）。
- **第 2 批（P0 待决策）**：等上述 8 类拍板后推进（CI/密钥/共享Browser/截图）。
- **第 3 批（P1 直接可修）**：A-3、C-3、C-4、C-5、G-1~G-4、G-6、G-8、P-3、P-4、P-5、R-3、R-4、E-2、E-3、B-3~B-6、D-4、D-5、D-7、X-6、X-8、X-9、CI-7、L-2、L-6、M-1、M-4 等。（其中 `W-4`/`W-6`/`W-9`/`W-10` 已于本批落地，见 §二~§四）
- **第 4 批（P1 待决策 / P2）**：架构重构与次要项。
- 每批修复后跑对应模块测试（优先全护盾 `mvn -o -pl test-automation -am test`）保零回归。
