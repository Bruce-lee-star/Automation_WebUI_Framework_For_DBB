# 14 代码整改专项进度（WEB-P1-* 等）

> 本进度基于 **2026-09-12 工作区代码核查**（非记忆推测），对应评审基线 `e11a847`。
> 这些专项属于框架「代码整改」体系，独立于 `12`/`13` 评审问题清单；但其落地能力是 `12`/`13` 评审时框架已具备的前提。
> 本快照仅记录「模块是否已在代码中落地」，不推测其是由 09-08~09-12 专项新增还是更早存在（需 git 历史确认）。

---

## 一、已落地（代码证据确凿）

### WEB-P1-1 拆分 PlaywrightManager（Playwright 生命周期治理）
- **证据**：`web/src/main/java/.../web/lifecycle/PlaywrightManager.java` + 6 个单测
  - `PlaywrightManagerSharedBrowserTest`、`PlaywrightManagerConcurrencyTest`、`PlaywrightManagerCleanupAllGuardTest`
  - `PlaywrightManagerCloseContextCleanupTest`、`PlaywrightManagerDownloadQueryTest`、`PlaywrightManagerApplyStorageStateTest`
- **状态**：已落地；含共享 Browser、并发、cleanupAll 守卫、storageState 复用等测试覆盖。

### WEB-P1-2 拆分 BasePage / PageObject 层次
- **证据**：`web/src/main/java/.../web/page/base/`
  - `BasePage.java`、`SerenityBasePage.java`（impl）
  - `page/base/delegate/`（`PageWaits`、`PageNavigation`）、`PageInteractions`、`PageViewport`、`PageAccessibility`、`LocatorFactory`、`CookieManager`、`ElementRect` 等
- **状态**：已落地；PageObject 层次化，动作/等待/导航职责分离。
- **后续演进**：Page Object 改为由框架自有 `PageFactory`（`page/factory/PageObjectFactory`）**组合式**创建（见 04 §2.1/§2.2），业务 Page 不再继承 `BasePage` / `SerenityBasePage`；`@RoleElement` 绑定经 `RoleElementBinder` 在工厂创建后注入。
- **Serenity BDD 录制目标模型（04 §2.8，设计 15）**：`SerenityBasePage` 的 `@Override` + `record`/`recordAndReturn` 冗余（约 600 行）应收敛为两层录制——原生操作由 `RecordingPageProxy` 动态代理（`implements` Playwright `Page`）装饰、框架自有方法由可复用 `SerenityRecorder.record(...)` 助手在调用处完成；`PageFactory` 注入被装饰的 `Page`，业务 Page 调原生 API 即自动录制——彻底无继承、单层复用（G-9 进行中）。**Phase 0 已落地**：`SerenityRecorder` 抽取完成，`SerenityBasePage` 改为委托（调用点零改动）。**Phase 1 已落地**：`RecordingPageProxy` 动态代理 + `recordNative` 原生录制实现，边界（D3/R2/R3/R1/D4）已落实，UT 覆盖。**Phase 2 已落地**：`RuntimeProvider.isTestDouble` + `PageObjectFactory` 为 `ManagedPageAware` 注入装饰受管 Page（惰性供应器）；试点 `RouteDemoPage` 改组合式 POJO，调用方零改动；`PageObjectFactoryRecordingTest` 覆盖。**Phase 3 已落地**：抽取 `SerenityPageRecorder` 门面（`SerenityBasePage` 约 40 个 Layer B 录制方法体逐字下沉、异常转换/数据键不变），`SerenityBasePage` 退化为薄委托壳（公开契约零变更）；`SerenityPageRecorderTest` 覆盖。

### JUnit5 升级（Serenity + junit-jupiter）
- **证据**：各模块 `pom.xml` 均引入 `junit-jupiter-api` / `junit-jupiter-engine`
  - `api`、`web`、`test-automation`、`route`、`reporting`、`pom.xml`（根）
  - `route` 测试为纯 JUnit（不依赖 Serenity）
- **状态**：已落地。

### axe-core 可访问性解耦
- **证据**：`web/src/main/java/.../web/accessibility/`
  - `AxeCoreScanner.java`、`AxeCoreScriptProvider.java`（内嵌 `axe.min.js`）、`AxeRule`、`AxeNode`、`AxeCoreException`
  - `AxeCoreListener` + 并发测试 `AxeCoreListenerConcurrencyTest`、`AxeCoreScriptProviderTest`
- **状态**：已落地；accessibility 扫描独立成包并含并发测试。

### lifecycle 跨子包治理
- **证据**：`web/src/main/java/.../web/lifecycle/` 下子包齐全
  - `PlaywrightRuntime`、`ScenarioLifecycle`、`context/`、`browser/`、`concurrent/`、`provider/`、`serenity/`、`state/`、`config/`、`page/`
- **状态**：已落地；生命周期职责按子包收敛。

### 共享 Browser 模式彻底移除（DEV-V3 收尾）
- **证据**：`BrowserRegistryImpl.keyFor` 恒返回 `threadId:configId`（无 `shared:` 分支）；`WebFrameworkConfig.PLAYWRIGHT_SHARED_BROWSER_ENABLED`、`PlaywrightManager.enableSharedBrowserMode` / `isSharedBrowserMode` 已删除；`ConcurrentScenarioExecutor.prepareSharedBrowser` 重命名为 `prepareConcurrentEnvironment`（不再启用共享模式）。
- **状态**：已落地；并发统一为每线程独立 Browser（N 并行 = N 线程 = N Browser），隔离性由每线程 `BrowserContext` 保证。根因：Playwright for Java 非线程安全，共享单 Browser 跨线程并发损坏客户端对象注册表（`__adopt__`/`pausedStateChanged`）。
- **验证**：真实浏览器并发测试 `ConcurrentContextExecutorTest`（10 例）+ `ConcurrentContextNoDeadlockTest`（2 例）全绿（BUILD SUCCESS）。

### ROUTE-P1 route 模块解耦
- **证据**：`route/src` 独立模块；`route/util/ApiMatcher.java` 等
- **状态**：已落地；route 为独立纯 JUnit 模块，与 web/api 解耦。

---

## 二、待核查 / 未落地（无代码证据）

### WEB-P1-6 DI 二期
- **证据**：搜索 `javax.inject` / `jakarta.inject` / `extends AbstractModule` / `new Injector` 返回 **0** 匹配
- **状态**：当前代码未见 Guice/DI 框架落地 → 标 **未落地 / 待核查**（不臆测已完成）。

---

## 三、说明
- 上述「已落地」为基线 `e11a847` 代码现状；是否为 09-08~09-12 专项新增，需 `git log` 确认本快照未推测时间线。
- 本清单与 `12`/`13` 评审问题清单互补：`12`/`13` 关注「闭环未打通」（凭据明文、CI 未跑 E2E、并行未开等）；本清单关注「框架代码整改专项的模块落地」。
- 若需把某专项的「设计意图 / 拆分方案 / 验收」也补成独立 `14_*` 子文档，请指出具体专项，我据代码实况补写（不凭记忆）。
