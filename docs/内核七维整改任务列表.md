# 内核七维健康度整改任务列表

> 来源：`docs/框架内核架构评审报告.md`（评审日期 2026-09-10，6 模块 58,325 行主代码）
> 评分：依赖解耦 9 / 核心抽象 8 / 资源生命周期 8 / 配置环境 6 / 扩展 Hook 6 / 日志断言 5 / 易用性 4
> 目标：对照评审逐项修复，每改必带单测 + 全护盾回绿（企业级标准）

---

## 一、评分合理性核验（结论：总体合理，与代码实证一致）

| 维度 | 评 | 核验结论 | 一句依据 |
|---|---|---|---|
| 依赖管理与解耦 | 9 | ✅ 合理（最高分） | core 仅 3 外部依赖零运行器耦合；route 零 runner 引用；ArchUnit + Enforcer 门禁齐全 |
| 核心抽象与封装 | 8 | ✅ 合理 | `PlaywrightManager` 纯门面 + 6 角色接口 + `RuntimeProvider` seam 真多态；扣分项：全局 static 门面、`retry` 吞根因、`BasePage`/`PageElement` 仍胖 |
| 资源管理与生命周期 | 8 | ✅ 合理 | `ShutdownCoordinator`/`AsyncPool`/`BrowserCrashGuard` 成熟；扣分项：`HikariDataSource` 未登记（缺陷2）、`computeIfAbsent` 非原子 |
| 配置与环境管理 | 6 | ✅ 合理 | `ConfigSource` 四级优先级 + `SensitiveDataSanitizer` 质量高；致命短板：无 `EnvironmentProfile` 抽象（缺陷1 成因）、无缓存、密文判定过宽 |
| 扩展机制与 Hook | 6 | ✅ 合理 | `ListenerRegistry` 双通道发现 + SPI 解耦成熟；扣分项：`FrameworkListener` 零方法标记接口、无自有 `TestResult` 模型 |
| 日志与断言基建 | 5 | ✅ 合理（偏弱） | `VerboseLogging`/截图策略到位；缺 FileAppender、无软断言、无结构化/MDC 日志 |
| 易用性与开发者体验 | 4 | ✅ 合理（最弱） | 无 README、4 模块单测为 0（54% 无自测）、route 38 处吞异常、`WebFrameworkConfig` 1514 行等巨型类 |

**综合判断**：7 项评分与评审证据、代码实证一致，无需上调/下调。整改应按"先致命缺陷、后弱维度"推进。

---

## 二、致命缺陷（优先级高于维度，按严重度排序）

| ID | 缺陷 | 级别 | 验收标准 | 状态 |
|---|---|---|---|---|
| **F1** | route mock/modify 无环境闸门（可能污染 pre-prod/prod 数据） | 最高危 | 原定：`RouteDsl.mock/modify/delay` 仅当 `env∈{dev,sit,test}` 且 host 命中白名单才放行，违反 fail-fast + WARN | ➖ 不做（决议：不引入环境维度闸门。风险改为由使用方在执行侧把控——mock/modify 属测试代码显式能力，框架不再按环境判定放行/拒绝） |
| **F2** | `HikariDataSource` 未登记 `ShutdownCoordinator`（连接泄漏 + 意外写库） | 高 | `createDataSource` 返回前登记关闭任务（order 800，晚于 flush/engine）；JVM 退出即关闭 | ✅ 已落地 |
| **F3** | `cleanupAll()` 为 `public`，并发下误调用会关停所有线程浏览器 | 高 | 降级为包级私有 / framework-internal，并加运行时并发断言（仅套件结束允许） | ✅ 已落地 |
| **F4** | route 断言失败仅置标志不 rethrow（假绿，R-18 P0） | 高 | 断言失败必上报 Serenity 置用例失败；端到端用例验证 FAIL | ✅ 已落地（链路由 ROUTE-P0-1 实现；本次补 `RouteAssertionFailureSurfacesToScenarioTest` 端到端锁定"必置用例失败"验收） |
| **F5** | `SHARED_BROWSER_MODE` 类加载期固化（配置未就绪即锁死） | 中 | 环境就绪后再解析；解析失败 fail-fast 而非静默固化 | ✅ 已落地（删除类加载期 `final` 字段，改 `isSharedBrowserMode()` 双重检查锁惰性解析+缓存；`parseSharedBrowserMode` 非法非空值抛 `ConfigurationException` fail-fast；ArchUnit 内部状态集合同步移除该字段） |
| **F6** | 启动路径 `Thread.sleep(1000)`（`PlaywrightInitializer:530`） | 中 | 改为有界轮询 `LockSupport.parkNanos` 或 `CompletableFuture` 等待进程退出 | ✅ 已落地（`waitForDownloadProcess` 改用 `Process.waitFor(timeoutMs, TimeUnit.MILLISECONDS)` 做有界等待——底层 `LockSupport.parkNanos`、进程退出即唤醒、可中断，移除魔法 `Thread.sleep(1000)` 轮询） |

---

## 三、维度整改（按分数升序，先弱后强）

| ID | 维度(分) | 任务 | 验收标准 | 状态 |
|---|---|---|---|---|
| **D7-1** | 易用性(4) | 补 README + `docs/`：分层图 / 5 分钟上手 / 配置项总表（由 `ConfigKey.description` 自动生成）/ FAQ | 新人可脱离源码跑通最小例子 | ⏸ 暂缓（决议：本轮不做） |
| **D7-2** | 易用性(4) | core + route 首批单测，JaCoCo 阈值入 CI（报告 P1：自测从 0 到有） | core/route 单测 >0，`jacoco` check 阈值生效 | ✅ 已落地（core 首批 2 例；JaCoCo 阈值待接入 CI） |
| **D7-3** | 易用性(4) | 立规范：框架内禁止静默吞异常，`ignored` 必须带计数器或 WARN | ArchUnit/Checkstyle 规则或至少 `RouteHandler` 等重灾区别清零 | ✅ 已落地（根目录新建 `checkstyle.xml`，`EmptyCatchBlock` 规则；route 24 + core 7 + web 14 + reporting 1 = 46 处清零；codegen 存量 79 处以 `checkstyle.skip` 显式跳过并登记待办） |
| **D3-1** | 日志断言(5) | logback 增 `RollingFileAppender` + MDC（scenarioId/threadId/env） | 日志按 scenario/线程落盘；并行日志可串联 | ✅ 已落地（core 新增 `common.logging.LogContext` MDC 门面；`PlaywrightListener.testStarted/testFinished` 绑定与 finally 解绑；core `logback.xml` 与 `logback-test.xml` 增 `RollingFileAppender`（按天+50MB 滚动、压缩、1GB 上限），pattern 含 `%X{scenarioId}/%X{threadId}/%X{env}`；`LogContextTest` 6 例含端到端落盘校验） |
| **D3-2** | 日志断言(5) | `FrameworkAssertions`（硬）+ `SoftAssertions`（collect→scenario 末 assertAll 入 Serenity） | 统一断言门面；软断言收集失败统一上报 | ✅ 已落地（core 新增 `common.assertion`：`FrameworkAssertionError`（继承 `AssertionError`，测试引擎识别为失败而非错误）、`AssertionFailure`、`FrameworkAssertions`（硬，含 `assertStatusCode` 等语义化断言，null 安全）、`SoftAssertions`（软，ThreadLocal 收集 + `assertAll()` 聚合抛出 + `remove()` 清理）；`StepFailureAggregator.checkAndMarkSoftAssertionFailures` 复用既有 seam（`StepEventBus.testFailed` + Serenity 报告 + result 置 FAILURE），挂载于 `testFinished` 两个重载并在 finally 兜底清空） |
| **D3-3** | 日志断言(5) | HTTP 请求/响应脱敏落盘 `target/logs/api` | 经 `sanitize` 后写入；零敏感泄露 | ✅ 已落地（core 新增 `common.apilog.ApiTrafficLogger`：默认关闭 opt-in；URL/头/体全部经 `SensitiveDataSanitizer` 脱敏后才落盘，脱敏在调用线程同步完成、写盘经 `AsyncPool` 异步不阻塞 route 事件线程；按天分文件 + 100MB 上限保护；接入 route 捕获统一入口 `ApiTrafficContext.storeApiCall`。配置键用 `framework.` 前缀且直读系统属性/环境变量——实测 `ConfigSource` 的 Serenity SPI 源会缓存属性快照致开关非确定） |
| **D2-1** | 配置(6) | ~~`EnvironmentProfile` 抽象~~ | ~~框架可判定/校验当前环境~~ | ➖ 不做（决议：不引入环境剖面抽象；曾实现后按决议整体撤回。其附带产出 `common.config.FrameworkFlags` 保留——框架开关统一 `framework.` 前缀 + 直读系统属性/环境变量，规避 Serenity SPI 源缓存快照导致的开关非确定，D2-2/D3-3 依赖） |
| **D2-2** | 配置(6) | 密文判定收紧为仅 `ENC(...)`；裸 base64 需显式开关 | 普通配置值不再被误判为密文 | ✅ 已落地（`SecretValue.decryptIfNeeded` 仅 `ENC(...)` 始终解密；裸 base64 需 `framework.secret.allow-bare-base64=true` 显式开启；既有 `ConfigCipherTest.decryptIfNeededDecryptsBareCiphertext` 已更新为锁定新契约的 `bareCiphertextRequiresExplicitOptIn`） |
| **D2-3** | 配置(6) | `ConfigSource` 启动后冻结的不可变快照缓存 | 热路径不再每次 `getenv`+SPI 遍历 | ⏸ 暂缓（决议：本轮不做） |
| **D2-4** | 配置(6) | logback 脱敏 Converter（出口强制，非调用方自觉） | 日志出口统一脱敏 | ✅ 已落地（core 新增 `common.logging.SanitizingMessageConverter` 继承 logback `MessageConverter`，在两个 logback 配置中以 `<conversionRule conversionWord="msg">` 接管 `msg` 转换词 —— 所有 `%msg`/`%m` 输出前统一经 `SensitiveDataSanitizer.sanitizeFreeText`，调用方无法绕过；脱敏异常回退原始消息（日志不丢）；保留运维级总开关 `framework.log.sanitize.enabled`（默认 true）以备性能应急） |
| **D4-1** | 扩展(6) | `FrameworkListener` 提升为带默认方法生命周期接口（before/after Scenario/Step/onFailure） | 业务只实现框架接口；官方 Adapter 桥 Serenity | ✅ 已落地（`FrameworkListener` 由纯标记接口升级为 5 个 **default** 生命周期方法（beforeScenario/afterScenario(name,failed)/beforeStep/afterStep/onFailure），既有标记实现零改动仍可编译；新增官方 Adapter `FrameworkListenerBridge` 做 Serenity 事件 → 框架接口的桥接 + **异常隔离**（单监听器抛错不影响其它与主流程）；`PlaywrightListener` 在 testStarted/testFinished(finally)/stepStarted/stepFinished/testFailed 接线，`afterStep` 用 per-thread 标志幂等防重复通知，per-thread 状态在场景收尾 remove 防线程复用串扰） |
| **D4-2** | 扩展(6) | 定义框架自有 `TestResult`/`StepResult` 模型，`SerenityReporter` 退化为单向适配器 | 换报告引擎不需重写 | ✅ 已落地（core 新增 `common.result`：`TestResult` 枚举（SUCCESS/FAILURE/ERROR/PENDING/SKIPPED/UNKNOWN + 语义判定）、不可变 `StepResult`、`ResultReporter` 上报端口、`ResultReporters` 广播门面（异常隔离 + 无实现 no-op）；reporting 新增 `SerenityResultAdapter` 双向映射并收敛引擎方言（UNDEFINED→PENDING、IGNORED/ABORTED→SKIPPED、COMPROMISED/UNSUCCESSFUL→FAILURE）；`SerenityReporter implements ResultReporter` 退化为单向适配器并自注册；`PlaywrightListener` 场景结果改经端口广播）<br>补充：新增 `SummaryResultReporter` 消费该端口产出汇总报告（`target/framework-summary.{json,txt}`，含场景/步骤计数、结果分布、耗时、最慢场景 Top10、失败清单；由 `ShutdownCoordinator` order 1000 在 JVM 退出时落盘）；`PlaywrightListener` 补步骤级 `reportStep` 接线（步骤模型含 title/result/duration）。**注意**：数据只在测试经 Serenity 监听器生命周期（BDD/Cucumber）运行时才产生，纯 JUnit 用例不产生（已实测：web 模块 Serenity 驱动用例产出 1 场景/3 步骤；test-automation 592 例纯 JUnit 为 0） |
| **D5-1** | 资源(8) | `ShutdownCoordinator` 下沉（消除 core→common 反向依赖冲突） | 资源登记契约清晰，架构门禁不再被迫取舍 | ✅ 已落地（`ShutdownCoordinator` 由 `framework.common` 迁至 `framework.core.lifecycle`，10 处引用批量更新；依赖方向收敛为**单向** common→core（与 `LanguageState` 同向），core 侧不再被迫放弃登记 —— `ThreadContextRegistry` 现已登记 JVM 退出兜底清理（`ORDER_TEST_CONTEXT=950`，最后执行）；新增 ArchUnit **G2** 门禁固化「core 不得依赖 common」，防止回退成环）<br>附带修复：两个日志端到端用例补轮询等待（落盘异步导致负载高时偶发假失败），测试日志滚动阈值 50MB→256MB（避免运行中途滚动把刚写入内容归档） |
| **D5-2** | 资源(8) | `ThreadLocalTestContext.computeIfAbsent` 改原子 `ConcurrentHashMap.computeIfAbsent` | 无 get-then-put 竞态 | ✅ 已落地（原实现为「get → 判空 → 计算 → put」四步非原子操作，并发下 supplier 被重复执行且后写覆盖先写、调用方状态静默丢失；改为 `store.computeIfAbsent(...)` 原子实现，字段类型由 `ConcurrentMap` 显式为 `ConcurrentHashMap` 保证走原子重写；`TestContext` 接口补充<b>原子性契约</b>（supplier 至多执行一次、所有调用方同一实例、supplier 不得回改本上下文）；`ThreadLocalTestContextConcurrencyTest` 3 例，**并已反向验证有效**——临时还原竞态实现后该用例报 `expected:<1> but was:<32>`，恢复后通过）<br>附带修复：两个日志端到端用例改走测试内独立 appender（`TestLogCapture`），不再依赖共享 `target/logs/framework.log`（后者受全局吞吐与滚动影响，曾多次致全量运行偶发失败；脱敏用例耗时同时由 10s 降至毫秒级） |
| **D1-1** | 核心抽象(8) | `PageWaits.retry` 改为条件驱动（委托 `Locator.waitFor`）+ 首次异常作 suppressed | 轮询消失；根因栈保留 | ✅ 已落地（① <b>根因保留</b>：各次尝试异常全保留，最后一次作 cause、<b>首次及中间作 suppressed</b>；旧实现只把最后一次作 cause，首次（往往才是根因）与中间异常全部丢失；② 新增条件驱动 `retryOnReady(selector)` —— 等待委托 `Locator.waitFor`，就绪即返回，不再"无论就绪与否都睡满固定间隔"的轮询；③ `retryWithValidation` 由盲睡整段间隔改为<b>等校验条件成立即继续</b>）<br>附带：`LocatorFactory` 补 `bySelector(bp, selector)` 选择器入口 —— 原工厂只有 `getBy*` 语义定位、无 CSS/XPath 通道，导致需要选择器时只能绕过工厂直取 `BasePage.locatorInternal`（跨层调用内部 seam）；补齐后<b>全部定位入口（语义 + 选择器）统一收敛于工厂</b>。<b>2026-09-11 深化（依赖方向反转）</b>：选择器解析（iframe 路由 + shadow `>>>` 穿透前缀）改由 `bySelector` <b>自身实现</b>，经 `BasePage` 现有 seam（`ensurePageValid` / `getCurrentFrame` / `getPage` / `shadowPrefix`——新增包级私有，返回 shadow 栈拼成的 `>>>` 前缀，与 `peekShadow` / `getShadowDepth` 同族）；`BasePage.locatorInternal` 相应退化为委托 `bySelector` 的薄门面（public 签名保留，`PageElement` / `PageElementList` 等子类调用点零改动）。原"内部委托 `locatorInternal`"描述已过时——依赖方向由「工厂→基类」反转为「基类→工厂」，BasePage 进一步门面化，逻辑零回归；全护盾 601 例 0 失败 0 错误 0 跳过 BUILD SUCCESS。 |
| **D1-2** | 核心抽象(8) | 抽跨 web/api 统一 `ActionOutcome`/`Assertion` 抽象 | 两域统一动作/断言 | 🔧 进行中（`Assertion` 维度由 D3-2 的 `FrameworkAssertions`/`SoftAssertions` 统一门面承担，仅待两域接入；`ActionOutcome` 经评估暂不入框架，保留为后续选型；web 仍沿用 `ElementException` 抛异常范式、api 仍沿用 `perform(Entity):void` + 写 Entity + Hamcrest `verify*` 双轨） |
| **D1-3** | 核心抽象(8) | `PlaywrightManager` static 调用逐步迁实例调用（保留兼容壳） | 全局 reset 负担下降 | ✅ 核心已达成（被 WEB-P1-6 吸收）：门面 35 法整体委托 `PlaywrightRuntime.instance().<role>()`，重资源状态早在 WEB-P1-1 迁 `PlaywrightRuntimeState`，全局 reset 经 `cleanupAll()`+`resetInstance()` 收敛，门面层已无方法体可迁。**A 最小步已落地**（web 全护盾 198 例全绿、零语义变更）：`getFrameworkState()`(4)/`parkMillis`(1)/`takeScreenshot`(1) 三类纯工具/单例中继去门面、直连底层。剩余 **B**（`config()`→`PlaywrightConfigManager.config()` 14 文件 69 处；`isSharedBrowserMode()` 缓存迁 `BrowserRegistry`）降级为**可选/后续**，不强制铺开（收益有限、大面有回归风险）；其余 `PlaywrightManager` 公开契约壳（53+ 文件依赖）+ `cleanupAll` 并发守卫 + `ContextKey` 常量 + `provider` seam **保留壳** |
| **D6-1** | 依赖解耦(9) | `HikariConfigFactory` 移至 route 或独立 persistence 模块，core 回归零存储依赖 | core 不再依赖 HikariCP | ✅ 已落地（`HikariConfigFactory` 由 core `common.persistence` 整类迁至 route `route.persistence` 包（其唯一真实消费方 `ApiMonitoringRepository`），core 删除该类 + `HikariCP` 依赖 + `h2` test 依赖，回归零存储依赖；route 测试随类迁移并补 `h2` test 依赖；`ApiMonitoringRepository` 改 import `route.persistence.HikariConfigFactory`（原全限定名）；ArchTest 注释同步更新。依赖方向保持单向 route→core，无新循环，ArchUnit 规则（L6 等）无回退） |
| **D6-2** | 依赖解耦(9) | SpotBugs 基线设期限（新增违规必须为 0）+ web 73 条按包拆分到人 | 债务被消除而非冻结 | ✅ 已落地（**基于真实分析数据重建** `web/spotbugs-exclude.xml`，而非沿用旧冻结基线）。做法：空 filter 跑 SpotBugs 得到 web **当前全部真实违规 43 条（72 实例）**，与 git 原基线 73 条做集合对比，结论——**原基线 73 条中 44 条已过时**（约 60% 属无效冻结，主因 `web.page.scan.*`（RolePicker 族）已物理迁入 framework-codegen 模块 + PlaywrightManager/BasePage/PlaywrightInitializer 等重构消除）、**14 条遗漏**（原基线生成后代码演变引入）。据此：① **实质消除 7 条**并从基线移除——VA_FORMAT_STRING_USES_NEWLINE×4（`PageObjectFactory`/`ThucydidesStepsListenerAdapter`/`FrameworkConfigManager`/`ListenerPerfStats` 的 format 串 `\n`→`%n`）+ DM_DEFAULT_ENCODING×3（`AxeCoreScanner` 的 FileWriter、`BrowserStackLocalManager` 的 InputStreamReader、`PlaywrightListener` 的 PrintStream 补显式 UTF-8）；② 清理 44 条过时 suppress（解冻）；③ 补登 14 条遗漏。最终基线 = 当前真实违规 40 条（43−3），**按 10 个包组拆分、标注 owner/SLA/消除方式**（@web-core/@web-test，SLA 2026-Q4~2027-Q1）。验证：根 pom `spotbugs-check` 绑 `verify`+`failOnError=true`（新增违规必须为 0）经 `spotbugs:spotbugs` 复跑 **REMAINING=0**（基线完整覆盖、无遗漏）；web 全护盾 **198 例全绿** 零回归。债务为「限期消除清单」而非「永久冻结」） |
| **D6-3** | 依赖解耦(9) | serenity 升级路径 + 回归验证清单 | 不再被动跟随上游降级 | ✅ 已落地（新建 `docs/serenity-upgrade-path.md`：① 当前钉版事实表（serenity 4.2.0 / cucumber 7.31.0 / selenium-support 4.23.1 / jackson 2.18.3 / byte-buddy 1.17.8 覆盖 serenity 父 1.14.18 / guava 33.5.0 覆盖 33.2.1 / freemarker 2.3.33 随 serenity-model）；② 被动降级根因（CORE-P0-1 为适配 jackson 上界把 serenity 降到 4.2.0，评审报告 L174）；③ 摩擦点矩阵 F1–F7（jackson/byte-buddy/guava/freemarker/selenium-support/cucumber/add-opens 七类 requireUpperBoundDeps 冲突的解法与决策点）；④ 升级 SOP（在线环境执行，offline 不可下载新 serenity）；⑤ 回归验证清单（web 6 测试 + reporting 2 测试 + SerenityResultAdapterTest + 2 AxeCore 测试 + route 2 E2E + 全护盾 + ArchTest）；⑥ 回滚预案。根 `pom.xml` 的 `serenity.version` 属性加指针注释，版本已集中便于受控升级。实际版本跃迁（4.2.0→目标版）待在线环境按 SOP 执行——本任务范围为「建立路径」非「执行升级」） |

---

## 四、执行顺序（本次推进）

1. **F2** `HikariDataSource` 登记 → 同时落地 **D7-2**（core 首批单测，带 H2 内存库验证）。
2. 续 **F3** cleanupAll 降级（静态分析调用点后改造 + 运行时并发断言 + 测试）。
3. 续 **F4** route 断言失败必置用例失败（R-18 P0，端到端）。
4. 续 **D3-1/D3-2/D3-3** 日志落盘 + MDC + 断言门面 + HTTP 脱敏落盘（高频痛点，高杠杆）。✅ 已完成
5. 续 **D2-2** 密文判定收紧（仅 `ENC(...)`）✅ 已完成。
   ~~**D2-1** EnvironmentProfile~~ 与 ~~**F1** 环境闸门~~ 按决议**不做**（不引入环境维度）；其附带产出 `FrameworkFlags` 保留。
6. 其余按表推进（下一优先：**D2-3** `ConfigSource` 不可变快照缓存 / **D2-4** logback 脱敏 Converter，或 **D7-1** README）。

> 图例：🔧 进行中　⬜ 待办　✅ 已落地　➖ 不做（决议）
