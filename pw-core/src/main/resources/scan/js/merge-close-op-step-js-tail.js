 function mergeInto(arr){ if(!arr) return;   var seen = {};   (s.currentStep||[]).forEach(function(p){     var k = window.__mergeKey(p);     if (k && seen[k]) return;     if (k) seen[k] = true;     arr.push(p); });   var __ins = -1;   for (var __i = 0; __i < arr.length; __i++) {     var __pc = arr[__i] && (arr[__i]._pageClass || arr[__i].pageClass);     if (__pc === a.closedCls) __ins = __i; }   if (__ins < 0) __ins = arr.length - 1;   arr.splice(__ins + 1, 0, closeMarker); } if (Array.isArray(window.__currentStep)) { mergeInto(window.__currentStep); } else { window.__steps = window.__steps || [];   var last = window.__steps[window.__steps.length-1];   if (!last) { last = {pageClass:a.closedCls, picks:[]}; window.__steps.push(last); }   if (!last.picks) last.picks = [];   mergeInto(last.picks); } window.__steps = window.__steps || []; window.__steps.push({op:'close', pageClass:a.closedCls});}