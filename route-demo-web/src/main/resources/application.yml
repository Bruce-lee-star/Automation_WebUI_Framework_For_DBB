server:
  port: 8899
  servlet:
    context-path: /web

spring:
  application:
    name: route-demo-web
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB

logging:
  level:
    com.example.demoweb: DEBUG
